# HANDOFF — chinese-classics-corpus

> 狀態快照。行為規範見 [`CLAUDE.md`](./CLAUDE.md)，結構導航見 [`MAP.md`](./MAP.md)，資料契約見 [`SCHEMA.md`](./SCHEMA.md)。
> 最後更新：2026-09-01

## 進行中：越絕書 307 段（2026-09-01 開工）

**配額前提**：2026-09-01 Claude 7D **98%**（週牆邊緣）、5H 剛開新窗 20%；codex 300m 58%。**批量與通讀全走 codex，Opus 只寫 SPEC／驗收器／探針與驗收。**

| 階段 | 狀態 |
|---|---|
| 切批次（17 批，每批單一體例群） | ✅ commit 一併落地 |
| 分派表＋`READING-GATE.md` | ✅ 六群四道閘門 |
| 通讀 g01（b03 b08，G1 地志 168 段）／g02（G2 敘事 68 段）／g03（G3–G6 71 段） | ⏳ codex 背景串行中，log 在 `delegation/yuejueshu-reading.log` |
| `SPEC.md`＋`accept.py`＋擾動探針 | ⬜ **下一手，必須 Opus**。收到通讀後寫 |
| 發包 10–17 批 → 回收 → 回填 | ⬜ 走 `scripts/run-delegation.sh`（codex） |

**分派表（章名 → 群 → 閘門，段數已核對 ＝ 307）**：

| 群 | 章 | 批 | 章／段 | 閘門 |
|---|---|---|---|---|
| G1 地志登錄 | 記吳地傳第三 記地傳第十 | b03 b08 | 2／168 | §2.3 |
| G2 吳越史事敘事 | 荊平王內傳第二 吳內傳第四 卷五 紀策考第七 記范伯第八 陳成恒第九 占夢第十二 春申君第十七 | b02 b04 b06 b07 b10 b15 | 8／68 | §2.2 |
| G3 謀議・經濟論說 | 計倪內經第五 外傳計倪第十一 九術第十四 枕中第十六 | b05 b09 b12 b14 | 4／29 | §2.6 |
| G4 術數・器物 | 記寶劍第十三 記軍氣第十五 | b11 b13 | 2／20 | §2.5①／§2.7②（**兩章走不同配套，不可合併**） |
| G5 自序問答 | 本事第一 篇敘外傳記第十九 | b01 b17 | 2／17 | §2.3 |
| G6 論贊 | 德序外傳記第十八 | b16 | 1／5 | §2.2 |

**這部書與前面幾部不同的三點**：

1. **它不是敘事書。**掛在雜史類下，但 168 段（55%）是「某某者，某某也，去縣若干里」的地志登錄簿，形態近《水經注》的《水經》本文層。**六群在卷次上是交錯的**（G5→G2→G1→G2→G3→G2→G1→…），所以批次是用 `--split-before` 逐群邊界切的，17 批每批只含一個群。
2. **《吳越春秋》剛標完是污染源，不是資產。**兩書大量同事異敘（子胥奔吳、會稽之圍、九術、文種賜死）。`READING-GATE.md` 陷阱第 7 條寫死三禁：不得當跨批盲測材料（呂覽因相似度 0.19／0.27 作廢兩組的先例）、不得搬判讀、不得拿它 96% 的命中率當本書預期值。
3. **引句對拍有兩顆地雷**：本書全書寫 `句踐`（106 處零例外），《吳越春秋》全書寫 `勾踐`；另沿用吳越春秋剛補的 15b（不得吞掉 `曰：「` 的內層引號）。底本另有 `□` 闕字 17 處、`〈〉` 校勘異文夾注 6 處（**全屬異文，不是評斷語，承重理由不得取自夾注**）、章名 `卷五` 是〈請糴內傳第六〉的抓取殘留（照抄不改寫，`verify.py` 已有對應 warn）。

**下一手的具體動作**：通讀三份齊了以後 ①`scripts/check-reading-attribution.py` 查引句歸屬 → ②寫 `SPEC.md`（分派表要進 SPEC 讓 `accept.py` 按章名歸群，不按批次）→ ③寫 `accept.py` 與 `_selftest/probe_spec.py`，錨點一律現場解析 SPEC 表格、**斷言要驗數量不驗非空** → ④`--check-spec` 0 FAIL 且探針全數會叫，才發包。

## 已完成：吳越春秋 493 段全數標完（2026-09-01）

**結果**：12／13（I 掛零）、命中 96%／判空 4%、A 類條件十批 0 FAIL、`verify.py` 0 errors、索引已重生。commit 93fb2b4。

群命中率：G2 吳事敘事 200/201（100%）、G1 越事敘事 231/239（97%）、G4 譜系神話 23/26（88%）、G3 繫年條目 10/15（67%）、G5 輯佚校本 8/12（67%）。**高命中先問體裁**：本書是敘事體雜史，刪掉哪一則少的多半是對人的看法而非史料事實，與晏子 99%／列女傳 96% 同族；反向的 G3／G5 各 67% 與慎子逸文同因（斷片短到不承重）。

**這一輪留下的兩件跨書資產**：

1. **`scripts/check-reason-quotes.py` 的彎引號盲點已修**。它原本只抽 `「…」`，而本庫底本引號體例逐批不同（judge 跟著底本走是對的）。吳越春秋 125 段裡 104 段被誤報成「有格無引句」，總結行卻仍印「0 條不在本段」——**綠得像通過，實際只比對了 16 段**。與晏子 S11 同型：斷言還活著，覆蓋率悄悄掉了。**往後任何彎引號底本的書都受惠，不必再修。**
2. **「引句不得跨越內層引號拼接」是新的一類 judge 失誤**。judge 把「敘述＋`：`＋人物話語」接成一條引句、吞掉內層 `“`，該引句在底本裡逐字不存在（內容仍在本段，只有標點被併掉）。試點批 b02 出 6 條，補上 SPEC 硬規則 15b 後 **b03–b09 共 368 段同判者同引號體例零誤差**——spec 補一格精準改一格的第二個實例（前例：公孫龍子 IX 5→0）。**敘事體古籍寫 SPEC 時建議直接帶上這一條。**

**尚未做（非阻塞）**：`reading/g03.md`（G2/G3 通讀）已 commit 但**未併入 SPEC**——它原本的用途是發包前補錨點，而發包已完成且 0 FAIL，故不再需要。若日後要重判或做第二輪查核，g03 的 A/B 側候選仍是現成材料，**但引句要先由自己回 `raw/original.txt` 對拍**（§8 自述不算通過）。

<details>
<summary>發包過程（保留備查）</summary>

| 階段 | 狀態 |
|---|---|
| 分派表（G1–G5）／SPEC／accept.py／擾動探針 | ✅ 41/41 族斷言確認會叫、`--check-spec` 0 FAIL |
| g01 通讀（b01＋b10，38 段，G5＋G4） | ✅ 已併入 SPEC |
| g02 通讀（b02–b05，239 段，G1） | ✅ 已併入 SPEC（commit 99fdd4d） |
| g03 通讀（b06–b09，216 段，G2＋G3） | ✅ 已落地（commit 768b71e），**尚未併入 SPEC** |
| 試點批 b02＋b10＋b01（125 段） | ✅ 已回收驗收：`accept.py` 0 FAIL、判空 14 段（11%） |
| 試點校準回填 SPEC（硬規則 15b） | ✅ 已補，`--check-spec` 0 FAIL、探針 41/41 |
| 其餘七批 b03–b09（368 段） | ✅ 已回收，`accept.py` 0 FAIL、零引句誤差 |
| 回填與收尾 | ✅ `make-scaffold` → `apply-delegation` → `psych_survey` → `verify` → `build-index` |

**回收管線的固定順序（下一部書照抄）**：`accept.py out/*.json` → `scripts/check-delegation-out.py --slug X` → `scripts/check-reason-quotes.py --slug X` → **`make-scaffold.py`（必須在 apply 之前，否則 `FileNotFoundError`）** → `apply-delegation.py --slug X --tagged-by codex-gpt-5.6-sol` → 手寫 `meta.json` 的 `psych_survey`（`apply-delegation.py` 不會寫，`domains_null` 要自己填）→ `annotate.py stats X` → `build-index.py` → `verify.py`。

**兩個這次踩到的小坑**：`split_paragraphs(text)` 吃字串不吃路徑（在 `scripts/corpus_text.py`，不是 `common.py`）；用 Python 寫 `meta.json` 要指定 LF，否則 Windows 預設寫出 CRLF，git 會警告改寫。

**踩雷點**：

- 探針改寫 SPEC 一律 `read_bytes`／`write_bytes`（`read_text` 會把 LF 轉 CRLF，破 SHA-256）。
- 驗收器**按章名歸群不按批次**——b06 橫跨 G2 與 G3。
- SPEC 自我矛盾 `--check-spec` 抓不到（它只對拍數字與引句、不讀立場）：把某段升為硬錨時，要同步把它從灰區移走。g02 這次的文種賜死就是。
- 數量帶寬本庫已連錯九次，位置錨點零校準全過。帶寬只寫「別把整張表填滿」，不要寫成硬門檻。

**配額**：2026-09-01 收工時 Claude 5H 99%／7D 96%（撞牆邊緣），codex 300m 窗僅 26%。**批量工作一律走 codex**。

## 現在在哪

**第一批 68 部 + phase 2 小學 4 部全文已入庫。標註管線已跑通，已標 50 部（22,545 段），其餘 26 部 `psych_survey` 仍是 `null`。phase 2 小學四部全數標完，兵家五部（六韜／司馬法／三略／吳子／尉繚子，共 299 段）、法家三小部（慎子／商君書／諫逐客書，共 204 段）與**韓非子（781 段，全庫最大單書）**2026-08-10 全數標完；**管子（668 段）2026-08-11 全數標完，是本庫第一部由兩個判讀者分段完成的書**（b01–b23 codex，b24 claude-sonnet-4-6，`tagged_by` 逐段分離）；**名家與黃老小部四部（公孫龍子／尹文子／鄧析子／于子，共 182 段）2026-08-11 全數標完**；**儒家著述組五部（素書 6／忠經 18／新序 218／孔叢子 159／申鑒 105，共 506 段）2026-08-15 全數標完**；**風俗通義（230 段）2026-08-15 全數標完，13／13 全覆蓋，並逼出第七道體裁閘門 §2.7「來源之外」（考辨體）**；**獨斷（152 段）2026-08-15 全數標完，94% 判空、4／13，是第一部不立新閘門而用既有三道合成判完的書**；**蔡中郎集 2026-08-21 全數標完（本次判讀 207 段／133 章，另沿用獨斷 154 段、〈外集卷四〉110 段留 null），12／13、判空 33%，是連續第二部不立新閘門的書（碑誄體）**；**象數／占筮兩部 2026-08-22 全數標完——太玄經 880 段 13／13 全覆蓋、判空 75%，京氏易傳 784 段 6／13、判空 97% 且 XII 全書掛零；兩部都是兩層文本、都用「一書兩道閘門」判完（連續第三、第四部不立新閘門），而同一格 XII 在兩部上是四分與全禁兩個極端**；**戰國策（1,034 段，三巨頭第一部）2026-08-23 全數標完——65 批分三梯發包、13／13 全覆蓋、判空 56%，A 類 FAIL 只有 1 個而且錯的是 SPEC 不是判者（見第 17 條）**；**水經注（933 段，三巨頭第二部）2026-08-24 全數標完——65 批三梯一日跑完、13／13 全覆蓋（本庫第 8 部）、判空 70%，A 類 FAIL 0／WARN 0 零校準，XII 95 段為本庫絕對數最高且全數越過 §2.3 閘門，命中段 100% 屬酈道元注文而非《水經》本文（見第 17 條）**；**呂氏春秋（736 段）與墨子（606 段）2026-08-25 全數標完——**兩部都在 SPEC 裡把章分派給五個體例群、五群各走一道既有閘門，「一部書一道閘門」從此確定是巧合不是規則**；墨子的 G4 墨語 88／88 是本庫至今唯一零判空的體例群，把 §2.2 閘門的上界從列女傳的 4% 推到 0%**；**晏子春秋（218 段）2026-08-25 全數標完——兩群分派表、12／13、**判空只 2 段（1%），是本庫命中率最高的整部書**，並在擾動探針上當場抓到一族「至少解析到一條」型的死斷言**。**

**2026-08-26：`wenxin-diaolong`（文心雕龍 424 段／52 章）全數標完——13／13 全覆蓋、命中 34%、A 類 19 條七批 0 FAIL，並逼出第八道體裁閘門 §2.8「怎麼寫之外」（文論體）。**七群分派表把群數推到本庫最多，而 G3 在 SPEC 階段就先拆成技術／人兩側（12% vs 53%）——**分派表的用途從此有兩個：選對閘門，以及逐群推數量帶寬**（B 類 22 條只錯 2 條，是本庫帶寬最好的一次）。（先前判「codex 7D 98% 這一窗不發」是誤判，實測兩窗都是 0%，錯法見該節。）**這是本庫第一部文論。**52 章對不上 50 篇的三項異常一律在標註層處理、`raw/original.txt` 一個位元組都不動（前例 `hanfeizi`／`huainanzi`），其中〈諧讔〉／〈諧隱〉的三對逐字重出（相似度 0.939／0.841／0.813）被 `--split-before 諧隱` 推進兩個互看不到的批次，**是本庫至今最好的跨批盲測材料**。詳見下方專節。

**2026-09-01：`wuyue-chunqiu`（吳越春秋 493 段）全數標完——12／13（只 I 掛零）、命中 96%、五群分派表、A 類十批 0 FAIL。**是本庫第五部用分派表的書，也是命中率僅次於晏子的一部：G2 吳事敘事 200/201、G1 越事敘事 231/239，而 G3 繫年條目與 G5 輯佚校本各 67%——**高低兩端都是體裁事實不是判準差異**（繫年條目與慎子逸文同因，斷片短到不承重）。這一輪的兩件跨書資產見本檔開頭「已完成」節：`scripts/check-reason-quotes.py` 的彎引號盲點（第五個靜默失效實例，且是跨書共用腳本），以及 SPEC 硬規則 15b「引句不得跨越內層引號拼接」（**spec 補一格精準改一格的第二個實例**，b02 6 條 → b03–b09 368 段零誤差）。

**2026-08-24：`qunshu-zhiyao`（群書治要）整部移出本庫**，raw、meta、83 個發包批次與 catalog 條目一併刪除，理由見第 17 條。三巨頭剩下的兩部已全數標完，**巨型書階段結束**。

**2026-08-24：自 religions-history 收入五部諸子**——`huainanzi` 397 段／`mozi` 734／`wenzi` 190／`jiayi-xinshu` 279／`lujia-xinyu` 184，合計 **1,784 段**。使用者 2026-08-24 明示「收過來本庫沒關係，目前功能不太一樣」，**這是對 `CLAUDE.md`「已在宗教庫者一律不重複收」的一次明確豁免，只對這五部有效，不是通則**。`raw/` 逐位元組複製、SHA-256 與來源庫對拍相同，未經本庫爬蟲；每部 `meta.json` 的 `notes` 記了出處與裁決。`huainanzi` 入庫時去重一章——來源的〈墜形訓〉與〈墬形訓〉是 Wikisource 重導向造成的位元組相同重出，`assert` 確認內容全等後刪前者、保留正名，23→22 章、checksum 重算（前例：韓非子〈八奸〉／〈八姦〉）。

**2026-08-25：`mozi` 那一份已作廢重抓，上句的 734 段／1,784 段合計不再成立（現為 606 段／1,656 段）。**理由是複製來的 ctext 版有兩個結構缺陷：〈經上〉只剩 8 條 105 字（傳世約 98 條，殘存的是尾段端／似／辯／知／爲／異／循所聞／執所言）、〈經下〉45 條 719 字且起於「物盡同名」而非篇首；副帶問題是 53 篇篇名全是英文 slug（`canon-i`、`fortification-of-the-city-gate`）。墨經正是本書最依賴條級粒度的一群，帶著這份底本發包等於半群作廢。改自 Wikisource 重抓（`CLAUDE.md` §4 的批量正途），**53 篇逐章對拍字數確認沒有整段增刪**：除該二篇（+646／+355）外累計絕對差 1,498 字全屬異體字與校勘差異（爲／為、衆／眾、𠊱／候、㯕／撕），〈備城門〉-267 與〈號令〉+256 是兩本章界劃分不同（difflib 相似度 0.617，缺的那塊原封出現在〈號令〉裡），總字數守恆。子頁自動發現按 Unicode 碼位排序不是篇次，故在 catalog 用既有的 `wikisource_subpages_explicit` 欄寫死 53 篇順序。**殘留缺陷一項**：〈經上〉〈經下〉在 Wikisource 是各一整塊未斷條，故各只切出 1 段（〈經說上〉65 條、〈經說下〉76 條正常分條），**這兩篇併入下方 C 組的句讀切分器待辦**；緩解事實是墨經的承重內容（德目定義）在〈經說上下〉已有條級粒度，b09 只作旁證。宗教庫 `religions-history` 仍保留原 ctext 版一份，兩庫不去重（使用者 2026-08-24 裁定）。

`verify.py`：**76 部、10,821,040 bytes、0 errors、19 warnings**（警告數 6 → 14 是因為擴檢，不是變差；14 → 12 是重抓戰國策與群書治要修掉的，見下方章標籤一節；12 → 15 是新入庫的方言、急就篇、說文解字帶進來的同類章標籤警告；**15 → 16 是 2026-08-11 修好截斷偵測器後咬到的焦氏易林**，見已知風險；16 → 19 是新收五部裡 `wenzi`／`jiayi-xinshu`／`lujia-xinyu` 的序號章標籤）。

| 部 | 段數 | 已判讀 | 命中領域 |
|---|---|---|---|
| `sunzi-bingfa` | 91 | 91 | 11／13（未命中 I、XIII） |
| `jiuzhang-suanshu` | 720 | 720 | **1／13**（XII 1 段；2026-08-09 重判，原 3／13 已撤銷） |
| `haidao-suanjing` | 24 | 24 | **0／13**（全空） |
| `renwuzhi` | 229 | 229 | 10／13（未命中 I、IX、XII） |
| `qianfulun` | 268 | 268 | 12／13（未命中 XIII） |
| `yantielun` | 346 | 346 | **13／13**（全覆蓋，但 V 305 段＝88%） |
| `yanshi-jiaxun` | 255 | 255 | 12／13（未命中 I；**IV 86 > V 47**） |
| `shishuo-xinyu` | 1,132 | 1,132 | **13／13**（全覆蓋且尾巴不薄；**V 316 只佔 28%**） |
| `shenyijing` | 61 | 61 | 4／13（**VII 13 段獨大**；77% 判空） |
| `dongmingji` | 63 | 63 | 7／13（**每格只有 1–3 段**；86% 判空，本庫最高） |
| `gu-sanfen` | 78 | 78 | 4／13（**V 34 段＝命中的 97%**；55% 判空） |
| `nanjing` | 243 | 243 | **1／13**（僅 VIII 5 段；98% 判空） |
| `shanghanlun` | 728 | 728 | 8／13（**IX 7 段＝本庫單一書最高**；98% 判空） |
| `jinkui-yaolue` | 796 | 796 | 5／13（**99% 判空，本庫最高**；XII 2 段是醫家三部唯一） |
| `lienuzhuan` | 208 | 208 | 12／13（**96% 命中、4% 判空，本庫最低**；未命中 I） |
| `shuowen-jiezi` | 6,070 | 6,070 | 10／13（99.3% 判空；命中 42 段，10 段在許慎敘） |
| `shiming` | 945 | 945 | **12／13**（89% 判空但 100 段命中——`reference` 唯一真陽性） |
| `fangyan` | 385 | 385 | **2／13**（98.7% 判空；5 段中 3 段在序跋） |
| `jijiupian` | 282 | 282 | 7／13（96% 判空；**v1 作廢重跑，卷二命中 15→4**） |
| `liutao` | 60 | 60 | 5／13（63% 判空；**命中按六韜分卷切開，文韜武韜 14／17，虎豹犬三韜 30 篇只中 1**） |
| `sima-fa` | 67 | 67 | 4／13（64% 判空；仁本＋天子之義 17／25，定爵嚴位用眾 7／42；**VIII 掛零，ritual 4 段是兵家五部唯一**） |
| `san-lue` | 65 | 65 | 6／13（**只判空 15%，兵家五部最低**；下略 19／19 全中；**兵家唯一的 I 與唯一的 XIII**） |
| `wuzi` | 43 | 43 | 4／13（58% 判空；治兵 0／8、應變 1／10 vs 圖國 9／10；**XI 掛零，五部唯一**） |
| `weiliaozi` | 84 | 84 | **8／13**（43% 判空；前半 41／54 vs 後半各「令」5／30；**〈將理〉5／5 貢獻兵家唯一的 IX**） |
| `shenzi` | 75 | 75 | **11／13**（37% 判空，但**七章正文 13／14 vs 逸文 34／60**；未命中 III、XII） |
| `shangjunshu` | 125 | 125 | 8／13（46% 判空；論說章 vs 條文章對半分，〈墾令〉3／20、〈境內〉0／5；**XI 17 段為法家獨有密度**） |
| `jian-zhu-ke-shu` | 4 | 4 | **2／13**（只落 V 3、VIII 2；全庫最短，法家唯一站在制度受害者一側的文本） |
| `hanfeizi` | 781 | 781 | **13／13**（23% 判空；**全庫第一部十三格全中**、規模最大的單書。七種體裁七條線：論說篇近全中 vs〈飭令〉0／3；儲說經段全空、說段逐則判；**〈解老〉37／37 且 12／13 格，全庫最寬的單章**；目錄章 0／18。`narrative` 396 段為全庫最大敘事區塊） |
| `guanzi` | 668 | 668 | **13／13**（24% 判空；七類體裁七條線：D 黃老修養 25／25 全中 vs G〈地員〉0／27；E 輕重 128 段判空 55%；**〈國准〉／〈國準〉跨批錨點二元閘門 2／2、domain 集合 1／2**。**本庫第一部兩個判讀者分段完成的書**：b24 17 段由 claude-sonnet-4-6 補判） |
| `gongsun-longzi` | 36 | 36 | 5／13（53% 判空；**II＝0——全書談名實而「自我與認同（含名與實）」掛零**；IX 亦 0，見下方第 10 條的鏡像陷阱。17 個命中裡 16 個引注、10 個純由謝希深注成立） |
| `yinwenzi` | 80 | 80 | **8／13**（31% 判空；V 41、VII 23；**II 5 段全是名聲 vs 實情的正面樣本**，與公孫龍子的 II＝0 構成同一判準的兩端） |
| `dengxizi` | 38 | 38 | **8／13**（37% 判空；V 12、VII 12 並列；**`narrative`＝0，spec 事前預測並驗證**） |
| `yuzi` | 28 | 28 | 6／13（43% 判空；VII 8、VIII 8；**篇題注 5／14 vs 正文 11／14**；黃老三部 146 段只出 1 個 XII） |
| `sushu` | 6 | 6 | 6／13（**0% 判空**，全庫最短的滿貫書；VII 6 段全中；**XII 掛零——「非其神圣，自然所鐘」判 XI**；俊／豪／杰三等分型判 VII＋VIII） |
| `zhongjing` | 18 | 18 | 8／13（6% 判空，只空〈兆人章第六〉；**V 15＋VII 13 雙主軸**；XII 恰好落在事前指名的兩章；**IV 只 1 段——仿孝經體例不等於滿是家庭**） |
| `xinxu` | 218 | 218 | **11／13**（11% 判空且**全部集中在善謀兩卷**，刺奢／節士／義勇 0% 判空；V 152＝70%；**X 只 4 段而節士＋義勇 44 段之死掛零**；XII 恰 2 段；III、IX 掛零。沈猶氏同材料跨批獨立判出同一組領域） |
| `kongcongzi` | 159 | 159 | **11／13**（13% 判空；**知人與用人是主軸**，V 78＝49%、VII 34、VIII 31；判空四倍落差來自章的體例——名辯章 50%、雜謀章 43% vs 刑教／嘉言等七章 0%；**`04` 刑教章 0／8 判空**；XII 恰 2 段、否定側六段零違反；IX 掛零、X 只 2 段；**三組跨批盲錨兩組完全吻合**） |
| `shenjian` | 105 | 105 | **12／13，本庫覆蓋最廣**（17% 判空但總率不可讀——〈時事〉41%／〈俗嫌〉7%／〈雜言上〉0%／〈雜言下〉6%；VII 49＝47%、V 31、XI 21；**XII 8 段全是肯定感應，同書破除術數六段全落 XI 且零違反**；序跋 5 段中 4 段因序者的品格斷語命中；只 III 掛零） |

| `fengsu-tongyi` | 230 | 230 | **13／13 全覆蓋，本庫達成全覆蓋的最小樣本**（33% 判空；VII 92＝40%、V 75、XI 28、XII 21；**同書命中率相差 25 倍**——愆禮／過譽／十反／窮通四篇 98% vs〈山澤〉4%；〈正失〉五章 [1] XII／[2] XI 段位交替零例外；逼出 §2.7 考辨體閘門） |
| `cai-zhonglang-ji` | 471 | 361（本次 207＋沿用獨斷 154；〈外集卷四〉110 段 null） | **12／13**（只 I 掛零。**本次判讀 207 段判空 68＝33%**，沿用段 94%，**總判空率 59% 不可讀須分層**；V 93＝VII 93 雙主軸；**X 22 段零詞面誤撈**——整部寫死者卻只 22 段命中；〈荅詔問災異〉10 段驗證災異對策三分；〈月令問荅〉86% 判空 vs 碑誄章多篇 0%） |
| `duduan` | 152 | 152 | **4／13**（**94% 判空**，本庫第三高；V 4、VII 3、IV 1、XI 1；`formalization` 123＝81%、`expression` 與 `worked_instance` 全書掛零；**XII＝0 由祭祀章群 16 段的位置條件擋住**；`天子正號之別名` 18 段只中 [9]；不立新閘門，用 §2.3＋§2.6②＋§2.7 的合成） |
| `jingshi-yizhuan` | 784 | 784 | **6／13**（**97% 判空**，與醫家三部同級；V 8＝XI 8 並列、VII 6、II 2、VIII 1、III 1；`formalization` 607＝77%、`expression` 1 段唯一；**XII 全書掛零且是事前寫死的**——六親／君臣／身體部位／性命／遊魂一律是欄位名目；20 個命中裡 5 個純由注文成立；連續第四部不立新閘門，與太玄同為兩層結構） |
| `taixuanjing` | 880 | 880 | **13／13 全覆蓋**（75% 判空；VII 111 獨大、V 55、VIII 36、XI 30；**兩層文本兩道閘門**——贊辭層 810 段用 §2.1 替換序號、玄傳層 70 段用 §2.3；贊辭層命中 26% 高於玄傳層 17%，因為測辭是太玄的「小象傳」；**XII 四分**（破除 XI／認證 XII／過度 XII／技術 `[]`）後全書只 4 段、術數詞面零外溢；連續第三部不立新閘門） |
| `zhanguoce` | 1,034 | 1,034 | **13／13 全覆蓋**（56% 判空；**V 250＋VII 228 雙主軸**＝說客論權力該怎麼用、評人可不可信；VIII 101、IV 44；IX 4／XIII 6／XII 9 為薄格；`narrative` 579 與判空段數相同不是巧合——刪掉只少一件史事的就是純敘事段。**命中理由由夾注成立 126 段（鮑彪 89／正曰 40／補曰 27／鮑本 7），約 28% 屬南宋鮑彪與元吳師道而非本文**；走 §2.2 閘門不新立閘門；本輪逼出 CLAUDE.md「判空錨點要讀完夾注」與驗收器 A12） |
| `shuijingzhu` | 933 | 933 | **13／13 全覆蓋**（70% 判空；V 107＋VII 104 雙主軸、**XII 95 為本庫絕對數最高**、X 48、VIII 74；`observation` 815＝87% 是地志體指紋；**命中段 100% 屬酈道元注文而非《水經》本文**；A 類 FAIL 0／WARN 0 零校準） |
| `lushi-chunqiu` | 736 | 736 | **13／13 全覆蓋**（29% 判空；V 286＋VII 220 雙主軸、II 122、VIII 94、XI 56；**五個體例群分派五道既有閘門**，逐群 0%／5%／85%／86% 差距極大，總判空率不可讀；感應五分後 XII 只 12 段且零落在月令 85 段；跨批盲測一組成立） |
| `mozi` | 606 | 606 | **12／13**（43% 判空，只 XIII 掛零；VII 191＋V 164 雙主軸、VIII 74、**XII 68**、XI 30；**五群四道閘門**——G1 96%／G2 82%／G3 27%／**G4 墨語 100%（本庫唯一零判空群）**／G5 14%；G3 群內 `大取` 67% vs `經說下` 4% 差 16 倍；XII 與 `narrative` 兩條帶寬猜錯、逐段查證判對） |
| `yanzi-chunqiu` | 218 | 218 | **12／13**（**判空只 2 段＝1%，本庫命中率最高的整部書**；只 I 掛零；V 175＋VII 113 雙主軸、XI 27、IV 20；**兩群兩道閘門而命中率幾乎相同**——G1 敘事群 99%／G2 論說群 98%，八卷 94%–100%；XII 三側寫死後全書只 3 段，其中 1 段是無錨點處判者獨立判出的真陽性；50 段帶生計詞面而 I 一段都沒有） |
| `wenxin-diaolong` | 424 | 424 | **13／13 全覆蓋**（66% 判空；VII 42＋V 41 雙主軸、VIII 20、II 15、VI 15；**七群分派表加一個群內兩側**——G3 創作論在 SPEC 階段就切成技術側 12%／人側 53%，差 4.4 倍；逼出第八道閘門 §2.8「怎麼寫之外」；B 類 22 條只錯 2 條，是本庫帶寬最好的一次；引句機械檢查 0 誤差。詳見下方專節） |
| `wuyue-chunqiu` | 493 | 493 | **12／13**（**命中 96%／判空 4%**，僅次於晏子；只 I 掛零；V 309 獨大＝63%、VIII 94、VII 77、IV 52、VI 44；**五群分派表**——G2 吳事 100%／G1 越事 97%／G4 譜系 88%／G3 繫年 67%／G5 輯佚 67%；`narrative` 332＝67% 是敘事體雜史指紋；A 類十批 0 FAIL） |

其餘 **26 部** `psych_survey` 仍是 `null`——依 SCHEMA §5，`null` 是「未通讀」不是「沒有」。

**十五部的意義各不相同**：孫子證明方法找得到東西（連自己試點都漏標 3 個領域）；九章證明方法說得出「沒有」（719/720 段為空，2026-08-09 重判後）；海島是刻意挑的第三部，用來判斷九章的 `discourse_mode` 缺口是不是算書通例——是（見下方待決事項 1），順帶給出第一個 **0／13 全空**的結果；人物志是第一部論說書，用來檢驗判準離開算書語境還切不切得動；潛夫論是第一部批判性論政書，用來檢驗 V 這條判準在負面診斷式論述下是否還穩；顏氏家訓是刻意挑來**證否 V 的**；世說新語是第一部敘事體，用來兌現硬規則第 5 條；神異經是第一部**作者不在場**的文本；洞冥記是神異經的對照組（同類別但有情節）；古三墳是第一部**內部體例高度異質**的書；醫家三部（難經／傷寒論／金匱要略）是第一次**成組發包同一體裁**，用來測 IX 這一格；列女傳是**上界對照組**，連續六部判空率 ≥55% 之後刻意挑一部預期會大量命中的書，用來確認判準沒有整體漂向保守。後續各節見下方。

**人物志（229 段）的三個結果**：

1. **10／13 不代表覆蓋廣**。VII 83 段、VIII 81 段、II 64 段、V 57 段吃掉絕大部分，而 IV、X 各只 1 段，XIII 2 段，III、XI 各 4 段。尾巴薄到不能當本書特徵，只能記「有觸及」。往後讀 `domains_hit` 一律要配 `domain_para_counts` 看，光看命中數會高估。
2. **`formalization` 首次出現在非算書**。〈材能〉4–11 共 8 段是「某能→某材→在朝某任→為國某政」的對照表，把「誰該任什麼職」寫成可查表的程序。判準同 vocab 舉的「儀禮喪服」。這證明該值綁的是「規範被寫成可執行程序」，不綁數學。
3. **`Z-wisdom` 吸走內容的第二個實例**。25 段 psych_domains 為空，多數不是沒東西，是內容屬「如何察人／察人為何難／辯論技術」的方法論外殼。與九章的差別在本書外殼底下仍有大量對人的實質主張，整體不會被讀成「沒思想」——但這再次支持待決事項 2。

**潛夫論（268 段）的三個結果**：

1. **12／13，唯一真空是 XIII（安頓·修復·平安）**。這個零不是漏標：王符寫的每一種痛（邊民內遷、寡婦被強嫁、賢者不得用）都收在「該怎麼改制度」上，沒有一處轉向「人可以如何與之相處」。孫子也未命中 XIII——**XIII 在兩部世俗實務書都缺，可能是本庫這一類文本的通例**，往後留意。
2. **空白的第三種成因：體例摻雜**。69 段空白裡屬 `Z-wisdom` 外殼的只是少數，多數來自〈誌氏姓〉的姓氏譜系（18 段）與附錄的版本層陳述（42 段）。九章是被 `Z-wisdom` 吸走、人物志是方法論外殼、本書是「這些段根本不是同一種文本」——**下結論前要先分清空白屬哪一種**。
3. **附錄佔 19%，必須從分母裡拿掉**。268 段有 52 段是後漢書本傳、清人序跋、歷代著錄與佚文。本文 216 段空白只有 27 段，附錄 52 段空白 42 段。算領域密度一律用 216。

**鹽鐵論（356 段）的四個結果**：

1. **13／13 全覆蓋，但這個數字最需要配 `domain_para_counts` 讀**。V 305 段（本文 346 段的 88%），I 只 1 段、XIII 3 段。全覆蓋來自體裁不是思想廣度——60 篇朝廷辯論把「國家該做什麼」一路推到人性、鬼神、孝養、刑獄、邊民家人，每個領域被掃到一次，但絕大多數段落回頭仍落在 V。
2. **XIII 命中，推翻上一部立的假設**。潛夫論曾推測「XIII 在世俗實務書是通例性真空」。本書 3 段命中（〈毀學〉包丘子、〈貧富〉「不遇，退而樂道」、〈地廣〉「回也不改其樂」）。差別不在體裁而在**發言位置**：本書的文學／賢良是在野一方，被迫替自身貧賤辯護，於是必須說出一套自處之道；王符雖也不遇，卻整部朝制度說話。**XIII 的有無取決於文本裡有沒有人替自己的處境說話。**
3. **「人能不能改變」第三次出現，結構完全不同**。人物志與潛夫論都是作者自己前後矛盾；本書是**陣營對立且反覆**：〈殊路〉3／5 vs 4／6、〈疾貪〉3 vs 4、〈和親〉3 vs 4——三對無一例外，大夫／御史恆取不可改，文學／賢良恆取可化。此命題已從「某書內部的不一致」升格為「有陣營歸屬的立場分歧」。
4. **辯論體子準則守住了，但要補成三分法**：辯論**技術**→`Z-wisdom` 不標；辯論**倫理**作為規範→VII（〈論誹〉6）；**進言不被聽見／被誤解**→III（〈箴石〉2、〈相刺〉5／6／8）。三者原文外觀相近，沒有這條三分法就會全部標空或全部標滿。

六次全文讀相對於**試點**的錯法一致：都只往「補上漏標」修（人物志漏掉 V 的 57 段；潛夫論預期 V 為主成立，但完全沒預期到 XII 有 32 段、IV 有 16 段，也沒預期到 I 會命中）。**但「偏誤方向是穩定的低估」這個結論已於 2026-08-09 被九章重判推翻**——見下節。低估與高估是兩種獨立的失敗，判準過寬時會系統性高估。

**顏氏家訓（255 段）：V 過寬的疑慮已解除**

前六部裡 V 從未低於半數（鹽鐵論 88%、潛夫論 65%），無法分辨那是文本特徵還是判準過寬。顏氏家訓是**為了證否 V 而挑的**：它談南北朝士族的家內事，若 V 的浮濫來自判準，家內事會被大量讀成階級與權力。

結果 **IV 86 段 vs V 47 段**，V 沒有壟斷。逐條檢視 47 段 V，全部落在朋友、賓客、擇友、我群他群偏見、為政與領導，**沒有一段把父子兄弟婚嫁讀成階級權力**。

> **結論：鹽鐵論與潛夫論的高 V 是文本特徵，不是判準過寬。那兩部本來就在論朝政。**
> **但前提是 spec 必須明寫「家內事歸 IV、朝廷事歸 V」——不寫的話這道分工不會自己發生。**發包 spec 用一張分流表把 13 個領域逐格指定歸屬（見 `delegation/yanshi-jiaxun/SPEC.md`），這是本次沒有重蹈覆轍的直接原因。

其餘四個結果：

1. **XIII 的假設再次被支持**。15 段，且分布不是散的：〈终制第二十〉4 段全中、〈止足第十三〉3 段全中、〈省事第十二〉3 段、〈序致第一〉與〈勉学第八〉各 1 段——全是顏之推第一人稱交代自身遭遇，或教子孫在改不了的處境裡如何安身。鹽鐵論立的「XIII 取決於文本裡有沒有人替自己的處境說話」在第二部書上成立。
2. **`Z-wisdom` 陷阱這次沒踩**。〈勉学第八〉1–17 段命中（VIII 12 段為主，兼 II 名實、X 失所、XI 晚學能否改變、XIII 亡國後父子分定供養與教學之責），18–30 段的文字音義考證尾巴幾乎全空。人物志與潛夫論都在這一格整篇判空過；差別在 spec 逐句舉了四個正反例，要求分辨「哪一句在談方法、哪一句在談人」。
3. **三篇特殊體例寫成「預設 `[]` 但逐段確認」而非「整篇 `[]`」，撈出 7 段真例外**。書證第十七 91% 空，但 4 段離開考據去評人（斥俗儒臆造病因、南方學士視北方傳記為偽書而不讀、譴責文士把三婦添鄭衛淫辭、小學者拘守小篆）；音辭第十八 70% 空，3 段是南北士庶口音相非笑與家內督正兒女。**整篇一刀切會全部漏掉。**
4. **本庫第一個密集 VI 叢集**。全書 VI 只 14 段，11 段在〈風操第六〉居喪一節——那節在處理哀情該不該發、發到什麼程度、真哀還是假哀。把喪親換成中性物件整節就垮，場景承重。另〈歸心第十六〉19 段 XII 全中，是目前 XII 密度最高的篇章：一部世俗家訓裡夾一篇護法論，再次說明書級單一標籤打發不了內部異質性。

唯一落空的領域是 **I（存在與意義）**：這部書從頭到尾在講人該怎麼活，卻從不問人為何而活。

**世說新語（1,132 段）：敘事體第一次被檢驗**

前七部全是論說、算書或訓誡，沒有一部靠故事承載判斷。硬規則第 5 條（敘事段也要判領域，故事是載體）從未被大規模檢驗過。世說新語整部由人物軼事構成，是這條規則第一次要在 1,132 段上兌現。

`discourse_mode`：`narrative` 903（80%）、`proposition` 210、`observation` 200、`expression` 180、`prescription` 44、`ritual` 3、`formalization` 0、`worked_instance` 0。

1. **規則 5 成立。** 74% 的段落判出領域，13 格全中，且尾巴不薄——最小的 I 有 8 段、III 16、IX 17、XII 21、XIII 25，都成群而非孤例。〈傷逝〉19 段 X 中 18、〈惑溺〉7 段 III 中 6、〈棲逸〉17 段 XI 中 10，這些都不是命題句寫出來的，是從情節讀出來的。**敘事體不需要另一套判準，需要的是另一道閘門。**
2. **中性物件替換測試在敘事體上不能用，改用「刪掉這一則」閘門。** 軼事裡的人就是內容本身，換掉就沒有這一則了。改成：**把這一則刪掉，讀者少知道了什麼？少的是史料事實 → `[]`；少的是對人的看法 → 給領域。** 這道閘門在〈賞譽〉切得最乾淨：純品題語（「勁松下風」「廊廟之器」）判空並走 `observation`，評語導向薦官任用才給 V。全書 200 段 `observation` 高度集中在 賞譽 93、容止 23、品藻 19、文學 15，正是「只有一句人物評語、沒有事件」那一類。
3. **V 316 段只佔 28%，是目前 V 佔比最低的一部有規模的書**（鹽鐵論 88%、潛夫論 65%）。主軸是 V 316、II 229、VII 226、VI 139。
4. **「拿門類名當答案」的警告，一半落空一半成立。** 落空的：〈賞譽〉156 段提示 II，實得 V 30 > II 25 且 48% 判空；〈容止〉提示 II，實得 II 12 但 48% 判空。成立的：〈品藻〉II 40 主導、〈傷逝〉X 18、〈惑溺〉III 6、〈棲逸〉XI 10、〈捷悟〉〈巧蓺〉〈術解〉如預期多半判空。**提示表寫成「提示不是對映」是對的——照抄會在最大的那一門上錯得最重。**
5. **I（存在與意義）第一次成群命中，8 段**（前七部合計只有鹽鐵論 1 段）。分布不散：〈文學〉4 段全是玄學本體之問，〈任誕〉3 段全是「什麼樣的一生對自己已經夠了」，〈棲逸〉1 段是阮籍以無為與盛衰窮達叩隱者。**I 的觸發條件因此可以初步寫出來：要嘛文本在做形上學，要嘛有人替自己的一生定價。**兩者前七部都沒有——論政書問「該怎麼治」、家訓問「該怎麼活」，都不問「為何而活」。
6. **XIII 的既有假設第三次成立，且支持面最寬**。25 段散在 12 個門類（言語 5、雅量 5、黜免 4、賢媛 3，其餘各 1）：不是集中在某一篇，是凡有人在改不了的處境裡開口就會出現。〈黜免〉9 段中 4 段命中（被廢黜者的自處）是密度最高的一格。
7. **III（愛與親密）16 段，本庫第一次不是零星**（前七部最多 4 段）。集中在〈惑溺〉6 與〈賢媛〉5。差別在體裁：論說書談夫婦是談倫理位置，軼事書才寫得出「他實際上怎麼對待她」。
8. **`formalization` 與 `worked_instance` 全書零命中，`ritual` 只 3 段**。八值表在敘事體上實際只用到六值。這不是缺陷是體裁指紋——算書那兩部反過來是 `worked_instance` 壓倒性。
9. **spec 卡了 `modes` 上限卻沒卡 `domains`，codex 就從那個縫鑽。** b01 8 段有 6 段疊三個領域，b22 42 段只有 2 段。補上「預設一到兩個，第三個要在 `reason` 裡寫得出拿掉它少講了什麼」並點名兩種典型湊法（把背景條件當領域：因家族勢大而隱忍 → IV；政爭裡有人死 → X）之後，其後各批 3+ 領域幾乎歸零。**與顏氏家訓同一個教訓：沒有數字的規訓不會生效。**

**神異經 61 段／洞冥記 63 段：作者在不在場，與同類別不同輪廓**

這兩部同批送出，是一組對照。神異經仿山海經逐條記荒外物產，**沒有情節、沒有敘事者立場**，是本庫第一部作者不在場的文本；洞冥記體裁近親，差別只有一項——**有人物與情節**（武帝貫穿全書、東方朔反覆出場）。要驗的是判準會不會空轉，以及敘事框架會不會把判準往上推。

1. **判準沒有空轉。** 神異經 77% 判空、洞冥記 86% 判空（本庫最高），但每一條判空都寫得出它少了什麼。反向證據更關鍵：兩書合計出現「食之令人益壽」「飲此酒人不生死」「望仙」「羽化」數十處，**一條都沒有被填進 X 或 XII**；「其國大旱」沒有被填進 IX；出現「人民」「君」的地理條目沒有被填進 V。本草式功效與地理陳設沒有觸發任何反射命中。
2. **敘事框架沒有把判準往上推，反而暴露它與內容無關。** 洞冥記 44% 段落是 `narrative` 或 `expression`（28+10 於 63 段），遠高於神異經（`narrative` 1 段），**判空比例反而更高**。「有情節不等於有內容」——獻獸後獸骨飛去、青鴨化為三小童都有人物有事件，讀者少知道的只是一件異事。**`narrative` 是體裁指紋，不是命中訊號。**
3. **兩部同 category 的書，領域輪廓幾乎不相交。** 神異經 VII 13 段獨大（用怪物給人格類型取名字：饕餮、渾沌、窮奇、善人），XII 1；洞冥記 **VII 與 XII 都是零**，命中散在 II 2、III 3、VIII 3、X 2、IV 1、V 1、XIII 1，每格 1–3 段。交集只有 IV 與 V 各一段。**`category` 在領域層面沒有預測力**——這是守則 1「禁止用書名或類別決定標註價值」在同一類別內部再次成立。
4. **神異經的命中在地理上壓在西方三卷。** 西南／西／西北 19 段中 9 段命中（47%），東荒／東南／南荒 28 段只有 3 段（11%），北荒與東北荒全空。原因是《春秋》四凶（渾沌、檮杌、饕餮、窮奇）連同苗民、共工全被安置在西方。**道德內容是隨四凶傳說一起進來的，不是均勻散在全書**，書級單一標籤會整個抹掉這件事。
5. **兩個邊界案例已記在各自的 `psych_survey` 裡，不要當穩固前例引用。** 神異經西南荒經三則第 1 則（名曰聖／哲／賢的全知異人）判 `[]`——它羅列的是能力與效驗，不是人該怎麼活；這是「聖」字沒有觸發反射的證據。洞冥記卷四第 6 則（巨靈化青雀）判 III 是全書最薄的一條，形式上屬判空類「奇物變形飛去」，只靠「愛悅於帝」與「帝乃起青雀臺」兩句收束撐住。
6. **`expression` 在洞冥記 10 段是本庫最高，但只有 4 段判出領域。** 第一人稱不等於自我揭露——東方朔自陳遠國見聞仍是一串地名物名。

**古三墳 78 段：內部體例高度異質，spec 要按體例切而不是按批次切**

本書由五種體例混成（序 1／太古河圖代姓紀 2／三易卦位方陣 24／傳（1）（2）（3）25／皇策辭與兩政典 26），`text_role=contested`。spec 因此改成**逐體例給預設答案**，並明點三處最易整批判錯的分界。

1. **三處分界全部分開了。** (a) 易表 24 段滿是「君臣民后妻夫父子」字眼但只是在排格子 → 全判 `[]` + `formalization`；(b) 傳（1）9 段與傳（2）8 段是訓詁 → 判 `[]`，傳（3）8 段把每個卦象轉成聖人職責（求諫、察姦佞、正疆界、設溝洫）→ **全部命中 V**；(c) 太古河圖代姓紀第 1 段判 **I**（宇宙生成論），第 2 段判 `[]`（譜系紀年），**「混沌」「太極」沒有反射給 XII**。三傳若被一視同仁，要嘛少 8 段真命中，要嘛多 17 段假命中。
2. **這其實是兩本書釘在一起。** 41 段在建體系（判空），37 段是訓誥政典（判 V）。`discourse_mode` 同樣乾淨分裂：`formalization` 52 對 `prescription` 28。命中 35 段中 34 段是 V＝**97%，本庫單一領域集中度最高**（超過鹽鐵論 88%）。
3. **`formalization` 52 段（67%）是本庫首見量級，且與 `domains=[]` 高度共現**（52 段中 41 段判空）。它是 `Z-wisdom`（方法與體系）在 `discourse_mode` 層的投影。**但共現不等於等價**——傳（3）那 8 段就是同時 `formalization` 又滿載 V 的反例。
4. **I 第二次命中。** 加上世說新語的 8 段，I 的觸發條件可以再收窄：**要嘛文本在做形上學，要嘛有人替自己的一生定價。**這兩類在論政書、算書、家訓、博物誌裡都不出現。
5. **XII 只有 2 段，都不在宇宙論那一章**，而在兩篇政典裡把祭祀當統治責任的地方，因此與 V 同現。這正是要的：XII 給的是人與神聖的關係或信仰實踐，不是宇宙論本身。
6. **`category` 已由 志怪 改為 術數（2026-08-09）。** 讀完全書才確認它沒有任何志怪成分。**分類修正刻意延到標註完成之後才做，以免影響判讀**——這個順序往後照用。已同步改 `scripts/catalog/chinese-classics-ws.json`（canonical 來源）與 `meta.json`。

**這三部共同的方法結論：三批都是零校準回合。** 世說新語 23 批做了兩次 spec 補丁，這三部一次到位。差別不在模型，在**體裁的判準表面積**：博物誌只有一道閘門要卡；古三墳體例多但每種體例的規則單純，按體例寫就窮盡了；敘事體有情節、有評語、有背景條件三種湊法要防，所以非補不可。另外，洞冥記的 spec 事前列了五類「會命中的典型」，實得 9 條中 8 條落在其中（另 1 條是我漏列而 codex 抓到的）——**事前列命中典型同時給出正例與反例，比只寫閘門更能防兩個方向的錯，往後 spec 照這樣寫。**

**醫家三部 1,767 段：IX 這一格終於有答案，而答案不在病候裡**

難經 243 段（98% 判空、1／13）、傷寒論 728 段（98% 判空、8／13）、金匱要略 796 段（99% 判空、5／13），合計 1,742 段判空（98.6%）。這是本庫第一次**成組發包同一體裁**，目的是測 HANDOFF 先前寫下的假設：「IX 在已標十一部裡合計不到 20 段，醫書是唯一可能讓 IX 成群的體裁」。

1. **假設成立，但成立的位置與預期相反。** IX 共 9 段：傷寒論 7、金匱 2、難經 0。**九段全部在勸人護身的地方**（張仲景原序第 1 段、〈伤寒例第三〉六段、金匱臟腑經絡第一 #3），**沒有一段在病候描述裡**。1,700 多段辨證論治、方劑、服法、預後判語一段 IX 都沒有。**IX 不是「提到病」，是「有人在病裡，而文本在寫那件事」——這條分界現在有 1,767 段的證據集。**
2. **醫書裡人的內容出現在作者評論同行的時候。** VIII 共 16 段，全部落在「上工／中工／下工」與責備醫者草率的地方。難經 5 段（「實實虛虛……如此死者，醫殺之耳」「知一為下工，知二為中工，知三為上工」）、傷寒論 9 段、金匱 2 段。**三部分別發包、判讀者互不知情，卻指向同一個結構。**
3. **「神」「魂」「魄」「精」「元氣」在醫理裡是生理術語，不給 XII。** spec 事前逐句點名了難經的四處硬撈風險（「無魂者當死也，人雖能行，名曰行尸」「脈有根本，人有元氣，故知不死」「藏者，人之神氣所舍藏也」「五至曰死，六至曰命絕」），四處全數判 `[]`。三部唯二的 XII 在金匱末篇兩條**理由不屬醫學**的食忌——「凡心皆為神識所舍，勿食之，使人來生復其報對矣」與「父母及身本命肉，食之令人神魂不安」。**判準咬的是禁忌的理由，不是禁令本身。**同一章另外 151 條「肉中有朱點者不可食之」全空。
4. **spec 預測錯了一半，而且錯在保守的方向——這是本批最重要的方法結果。** 傷寒論 spec 把張仲景原序寫成「全書密度最高的地方」並逐段點名，那部分完全命中（5 段中 4 段）；但對〈伤寒例第三〉只寫了一句「可考慮給領域，判不出來就判空並在 reason 說明」。結果〈伤寒例〉貢獻 9 段命中、含 IX 的 6 段，**超過原序**。**事前列出的「會命中的典型」是下限不是上限；對不確定處寫「可考慮 ＋ 判不出來就判空並說明理由」，比寫死「這裡多半判空」更能讓真命中浮出來。**這條修正了上一節（洞冥記）「事前列命中典型」那條的邊界。
5. **章名層與體例套語的誘因必須在 spec 裡指名道姓。** 三個實例都生效了：金匱婦人三篇 59 段章名寫著「婦人」→ 事前明寫「不要反射填 III／IV」後全數判空、III 掛零；難經八十一難每難都以「……何謂也？」開頭 → 事前指定給 `observation`／`proposition` 而非 `expression`，結果 expression 掛零；傷寒論來源章名簡繁混用（「辨脉法第一」「平脉法第二」「伤寒例第三」）→ 事前要求逐字照抄，728 段錨點零缺漏。**不能指望判讀者猜到那是刻意保留的。**
6. **`discourse_mode` 與領域不可互推，這一批給出最乾淨的反證。** 先前四部各有一個壓倒性主導 mode 且高判空（古三墳 `formalization` 67%、洞冥記 `narrative` 44%、難經 `proposition` 62%、傷寒論 `observation` 51%）；金匱四個 mode 分佈接近均勻（`prescription` 358／`observation` 333／`formalization` 261／`proposition` 197），判空率反而是本庫最高的 99%。**mode 的分佈形狀不承載任何領域訊息。**
7. **`formalization` 414 段（傷寒論 153 ＋ 金匱 261）超過本庫前十二部合計。** 全部來自方劑組成與煎服法。spec 事前明寫「數量會很大，不要因為它不像制度表就迴避」。**`formalization` 綁的是「有可執行的程序」，與領域無關——這條現在在算書、術數、醫書三種體裁上各驗證過一次。**
8. **`domains` 上限用「寫得出拿掉會少什麼」比用數字更有效。** 1,767 段裡只有 2 段標到三個領域（傷寒論〈伤寒例〉24、25），兩段都照規則寫出了理由；零段 `modes` 疊到三個。相較世說新語 b01 未寫此規則時 6/8 段疊三領域。

**流程層面：三部全部零校準回合。** 只有傷寒論 b01 發為試點（它同時含林億校序、張仲景原序與辨脉法第一，是判準最該分開的兩端），試點乾淨分開後直接放行 b02–b07，難經與金匱沿用同一道閘門全書放行。**同體裁成組發包時，試點只需做一次，其餘書共用校準結果。**

**同批修掉一個從未生效的護欄**：`scripts/run-delegation.sh` 的 clobber 偵測器因 Git Bash 的 `sha256sum` 輸出 `<hash> *<path>`（binary 模式的星號被讀進檔名，回查取到空 hash）而**無條件誤報**，14 次發包全部跳警告。逐一比對確認零實際污染後修正並雙向實測。**無條件誤報的護欄比沒有護欄更糟，它訓練人忽略警告。**（修好之後第一次真正被行使是列女傳 b02——`b01.json` 已在磁碟上、偵測器全程靜默，這才是「偵測器有在工作」的實證。）

**列女傳 208 段：判準沒有漂向保守，而且頌曰不是重複**

神異經以下連續六部判空率 ≥55%（洞冥記 86%、金匱 99%），到這裡已經無法分辨那是文本特徵還是判準整體往保守漂。列女傳是**為此挑的上界對照組**：西漢劉向 104 則女性傳記，104 傳 ＋ 104 頌，同一道 §2.2 刪除閘門、同一個發包管道、同一個模型。

結果 **208 段裡 199 段命中（96%），判空 9 段（4%，本庫最低）**，12／13 領域，平均 1.95 個領域／段（本庫最高）。VII 145、V 99、IV 69、III 31、VIII 24。

> **九章算術 99% 判空與列女傳 96% 命中之間的差距是內容差距，不是判準差距。**同一道閘門在兩端都給出乾淨的極值，判準沒有漂。

1. **`I`（存在與意義）是唯一的零，而且這個零很硬。** 整部書在主張人該怎麼活，卻沒有一段在問人為何而活、意義從何而來。**「主張某種活法」與「追問活著的意義」是兩件事**——這條分界在一部 96% 命中的書上比在判空的書上更清楚。
2. **頌曰的判法是全書一半的段落，也是 spec 最吃重的一次決定。** 事前定的規則是「把頌曰當獨立一段，用同一道閘門判它自己的文字；不要因為傳文已標過就判空，也不要因為是四言讚辭就反射填 VII」。結果頌與傳的領域輪廓幾乎重合（VII 69/76、V 42/57、IV 34/35、III 15/16）——**頌曰確實在復述同一批評斷，而復述不是判空的理由。**
3. **全書 9 段判空全部是頌曰，傳文 104 段零判空。** 九段（陶荅子妻、楚老萊妻、齊杞梁妻、晉弓工妻、齊傷槐女、齊威虞姬、齊鍾離春、周幽褎姒、陳女夏姬）的 reason 形狀完全一致：「頌只列事件，沒有一句對人的評斷」。spec 只舉了其中 1 例（陳女夏姬），另外 8 段是判讀者獨立抓到的。**閘門寫對之後，同形狀的段落會自己聚起來，不需要事前窮舉。**
4. **孽嬖 15 傳（30 段）是硬撈風險最高處，兩道護欄都守住了。** 妲己、褎姒、驪姬、夏姬這一組只有 2 段判空，VII 28、V 26 — 譴責與稱許照同一道閘門判，負面評斷一樣是對人的看法。但 **III 只有 2 段、IV 只有 3 段**：spec 事前明寫「出現性、婚姻、淫亂不等於 III」「章名有母妻不等於 IV」，在最容易反射的地方沒有反射。
5. **推翻一條前四部形成的相關性觀察。** 先前記錄「`observation` 佔比高的書判空率高」（傷寒論 51%／98% 判空）。列女傳 `observation` 110/208（53%），判空只有 4%。**mode 分佈與領域密度無關這條，現在在正反兩端各有一次證據。**mode 側寫：`narrative` 120、`observation` 110、`proposition` 95、`prescription` 6、`ritual` 1、`expression` 0（對話包在敘事框架裡時不給 expression，這條事前寫明後 208 段零違例）。
6. **試點的驗收條件應該是事先講好的幾格，不是整體印象。** 本書零校準回合、只發一批試點，放行依據是三個事前指定的檢查點：b01 的 III 落點逐段回查原文（10 段全部說得出寫的是兩人之間的哪件事）、XII 只有 2 段（感生神話「履巨人跡而有娠」「玄鳥墜卵」全數沒觸發）、零段疊三領域。**「看起來判得不錯」不是驗收，寫得出來的幾格才是。**（全書最終 13 段疊三領域，每段都寫出了「拿掉它會少講什麼」。）

## 2026-08-09：九章算術全書重判（本庫第一次撤銷既有命中）

**28 段命中 → 1 段。** 撤銷 27 段（V 26、VIII 1），只留劉徽序首段的 XII。

**新判準（往後所有書都適用）**：

> **把題目／文句裡的社會場景換成中性物件，它還成不成立？**
> 成立 → 場景只是包裝，填 `[]`。不成立（垮掉，或問的東西整個變了）→ 場景是本質，才給領域。

**為什麼原判準錯**。原判準是「題目若陳述『誰該承擔多少』就標 V」，看起來嚴謹，實際是聯想式的：看到爵次、戶口、賦稅這些社會詞彙就命中，沒有問**場景是否承重**。「大夫、不更、簪褭、上造、公士五人共獵五鹿，以爵次分之」換成「五個容器按 5:4:3:2:1 分水」之後，題目一模一樣、術一字不改——爵位不承重。均輸按戶數×路程攤賦稅同理，本質是加權平均。九章算術是一部數學書，官員、分地、賦稅是作者用來出題的方式，正如現代課本用「小明小華分蘋果」，沒人會說那題在探討兒童社會關係。

**負對照組的地位變強不是變弱**。原本 HANDOFF 寫「九章證明方法說得出沒有」；重判後 719/720 全空，那 28 段命中本身就是雜訊，這個結論比原本更硬。

**過程與可稽核性**：spec 在 [`delegation/jiuzhang/SPEC.md`](./delegation/jiuzhang/SPEC.md)，10 卷逐卷發包外部 agent（codex）判讀，輸出對照表在 `delegation/jiuzhang/out/*.json`（含全部 720 段的 reason），回填由本庫用既有錨點機制做，外部 agent 不碰 `translations/`。撤銷的段落在 `annotations.json` 的 `note` 留有「2026-08-09 重判撤銷（原標 X）」。

**發包時記錄的兩次失敗，往後校準判準時當反面樣本**：

1. 第一輪：看到「這是數學題」就整卷判空，沒讀懂題目在做什麼 → 720 段只中 28
2. 第二輪：矯枉過正，用「依不依賴社會性前提」去撈，把買賣耗損利息全標成 VIII → 76 段只判空 1 段

兩次都錯，因為兩次都在「猜要標多少」而不是「這一段在做什麼」。正解是讀懂之後照實記——讀懂之後發現它就是數學，那就記 `[]`。

## 已完成

| 項目 | 檔案 | 說明 |
|---|---|---|
| 方法論試點 | `pilots/2026-07-28-sunzi-jiuzhang.md` | §1–5 是試點，**§6／§7 是全文讀後的修訂，以修訂為準**。§7 另記兩個 vocab 層發現（見下方「待使用者決定」） |
| vocab 驗證 | `pilots/2026-07-28-discourse-mode-worked-instance.md` | 九章＋海島兩部驗證 `discourse_mode` 覆蓋缺口。含建議定義與邊界，**表未動，等定案** |
| 段落切分單一來源 | `scripts/corpus_text.py` | `make-scaffold` 產錨點、`verify` 驗錨點都用它。**兩邊算法若分家，para_index 會靜默漂移、每條標註指向錯段** |
| 標註骨架 | `scripts/make-scaffold.py` | 由本文生成錨點，人只填 null 欄位。手寫 `annotations.json` 是壞錨點的來源 |
| 標註 | 15 部的 `translations/<slug>/annotations.json`（見上方「現在在哪」的表） | 5,242 段全數判讀完 |
| 發包 | `scripts/make-delegation-input.py`（切批）→ `run-delegation.sh`（串行發包 codex）→ `check-delegation-out.py`（回填前雙向錨點檢查）→ `apply-delegation.py`（回填） | 錨點三支都以 `split_paragraphs` 為唯一來源，對不上就整批不寫。`run-delegation.sh <slug> b01 b02 ...` 吃 slug 參數，**取代 `delegation/shishuo-xinyu/run-batches{,2}.sh` 兩支寫死 slug 的版本**，往後發包一律用它 |
| 資料契約 | `SCHEMA.md` | 三層標註模型、`discourse_mode` 八值（v0.2 加 `worked_instance`）、`text_role: reference`、`annotations.json` 格式、負面結果欄位、跨庫對齊。§1.1 補了段落級的白話說明 |
| 行為規範 | `CLAUDE.md` | 與 religions-history 的分界、六條工作守則、七條 anti-pattern |
| 書目 catalog | `scripts/catalog/chinese-classics-ws.json` | **73 條**（phase 1 共 68、phase 2 共 5），其中 `guangyun` 標 `excluded`，**實收 72 部**。全部走 Wikisource |
| 下載器 | `scripts/download-wikisource.py` | 含 `--survey` 預檢模式（1 請求／部）先掃結構再花內容請求；`natural_sort_key` 會把「卷篇第部回」後的中文數字轉成阿拉伯數字再排（2026-08-09，見下方第 7 類問題）；`main()` 會過濾 catalog 裡標 `excluded` 的條目 |
| 驗證 | `scripts/verify.py` | SHA-256／CRLF／過小檔／章標籤唯一性／同文章節／`psych_survey` 欄位存在／**索引新鮮度**（重生到 tempdir 對拍，剔除時間戳行，差異即 error） |
| 索引 | `scripts/build-index.py` → `00-overview/INDEX.{json,md}`（書級）＋ `DOMAINS.md`（標籤索引總表 ＋ 缺口報告）＋ `domains/<id>.{md,json}`（段級反向索引，13 頁 3,647 筆） | 由資料生成，**不得手改**；忘了重生會被 verify 擋下 |
| 全文 | `translations/`（**72 部**：phase 1 共 68 ＋ phase 2 小學 4） | 已 commit |

### 使用者已定案的三項

1. `discourse_mode` 七值 —— 通過（定案當時是七值；v0.2 加 `worked_instance` 後為八值，見 SCHEMA）
2. 標註粒度下放段落級 —— 通過（說明已寫進 SCHEMA §1.1）
3. 小學 5 部 —— **改排 phase 2，不是排除**。理由寫進 SCHEMA §3：排除＝憑書名判定無價值，那正是本庫要修正的錯誤
4. **ctext 路線整條移除** —— 使用者指示「取得簡單一點」。`chinese-classics-ctext.json` 已刪，申不害不收

## 抓取過程修掉的七類真問題（都會毀掉段落級錨點或污染正文，別再退回去）

| 問題 | 症狀 | 修法 |
|---|---|---|
| 篇名標題被丟棄 | 孫子兵法抓回來只有 1 章 | `extract_main_text` 補收 h2/h3/h4，前綴 `## ` |
| 平行上傳整卷重複 | 韓非子 864,988 bytes／80 章（實際重了三套） | catalog `subpage_drop_pattern` + 內容雜湊去重 |
| 卷級子頁沒往下切到篇 | 鹽鐵論只有 10 個「卷N」，60 篇全埋在裡面 | 子頁本文再切一層標題；竹書紀年以 `no_subpage_split` opt-out（698 條逐年紀事切下去全是「元年」互撞） |
| 重定向對切分後才撞成同文 | 外儲說右上／右上 只差一行標題，前置雜湊擋不住 | 切分**之後**再依本文雜湊去重，保留較具體的標籤 |
| **一個卷級子頁只含一篇時，篇名整個丟掉** | 顏氏家訓〈勉学第八〉〈書證第十七〉的章標籤是「卷第3」「卷第6」 | `split_by_headings` 要 ≥2 個標題才肯切，所以獨篇子頁保留了卷名。新增 `adopt_sole_heading()`：只有一個內層標題時不切、改拿它當標籤（2026-08-09） |
| **維基文庫授權樣板被當成正文抓進來** | 章末多一段「此**南北朝**作品在全世界都属于公有领域……」，18 部、48 段 | `CHROME_PATTERNS` 原本寫死 `此作品在全世界都`，但樣板會把朝代名插在「此」與「作品」之間，一路漏接。改抓宣告本身：`公有(領\|领)域` ＋ `此.{0,8}作品在全世界都`。1931 年以前的古典漢語正文不可能出現「公有領域」四字，誤傷風險為零（2026-08-09） |
| **中文數字卷名按 codepoint 排序，全書章序整個亂掉** | 洞冥記章序是「卷第一 卷第三 卷第二 卷第四」；方言、急就篇、穆天子傳、越絕書同病 | `natural_sort_key` 只認阿拉伯數字，遇到「卷一／卷第一」這種中文數字就退回字元碼排序（一 七 三 九 二 五 八 六）。新增 `_cn_to_int()` ＋ `_CN_ORDINAL`（**只轉「卷篇第部回」後緊接的中文數字**，不碰書名裡的三國志、六韜）。已落地的 5 部全部重抓；洞冥記已標註，改用「重抓 → 重建 scaffold → 從 `delegation/dongmingji/out/` 重新回填 → diff」的流程處理，**判讀零差異**（2026-08-09） |

**第 7 條那個 bug 值得單獨記一筆：沒有任何既有檢查會抓到它。**錨點是 `(chapter, para_index)`，章序亂了標註仍然對得上；`verify.py` 檢查 SHA-256、CRLF、章標籤唯一性、同文章節，就是不檢查**章序**。它是用眼睛讀章標題發現的。往後新抓一部書，`ls` 一次章標籤確認順序，這一步沒有自動化替代品。

另外處理過：6 部抓到消歧義／目錄頁（用 `action=parse&prop=links` 查真正目標後改 catalog 標題）、3 部同頁內標題重複（自動補全形序號）。

第五類是標顏氏家訓當天才發現的，當時骨架已產、尚未標註，重抓＋重生骨架零損失；若已標完再發現，255 段錨點要全部重對。

原本這裡寫的是「這一類問題只有在標到那部書時才會浮現」——**現在不必再靠人工發現**。verify 舊版只擋純數字標籤（`1`、`01`），擋不住「卷第3」，顏氏家訓才會漏到標註前一刻。2026-08-09 把它擴成擋所有「只有序號」的標籤（`卷三`／`第三卷`／`卷第3`／`篇第7`），警告數 6 → 14（重抓兩部後為 12）。

## 目前剩下的 12 個警告（章標籤只有序號）

**12 個全部同一類：來源就沒有篇名。重抓修不掉。**

> **更正（2026-08-09 稍晚）**：本節原本寫成兩類，說其中六部（`dongmingji`、`jinlouzi`、`mu-tianzi-zhuan`、`qianfulun`、`wuyue-chunqiu`、`yuejueshu`）是「篇名被爬蟲丟掉、重抓就會好」——**錯**。用 `download-wikisource.py --slug X --survey`（1 請求／部）逐部查了子頁標題，來源端本來就叫「卷一」「卷第一」「第001卷」，爬蟲沒有丟掉任何存在的東西。**正確版是：12 個警告全屬「來源就沒有篇名」，處理方式只有一種——人工補篇名對照表，標註那部書時再做。**

實際查到的來源子頁標題（可覆查）：

| slug | 來源子頁標題 |
|---|---|
| `dongmingji` | 卷第一 / 卷第二 / 卷第三 / 卷第四（4/4 皆序號） |
| `mu-tianzi-zhuan` | 卷一–卷六 ＋ 序一 / 序二 |
| `yuejueshu` | 卷一–卷十五 |
| `qianfulun` | 卷一–卷十 ＋ 附錄一–四 |
| `jinlouzi` | 卷一–卷六 ＋ 序 / 書後 |
| `wuyue-chunqiu` | 9 個有名的外傳／內傳 ＋ 第001卷…（兩套命名混在同一來源） |

部分警告是分數形式（`jinlouzi 1/18`、`yuejueshu 11/19`、`qianfulun 2/40`）是因為爬蟲會再進子頁切內層篇名——**切得出來的那些頁有標題，切不出來的那幾頁是原頁就沒有**，不是爬蟲挑食。

**連帶撤銷一條假警告**：原本寫「別在不知情的狀況下重抓潛夫論，否則〈卷九〉〈卷十〉標籤會改名、既有標註對不上」。既然來源就叫卷九卷十，重抓不會改名，**潛夫論可以安全重抓**。

`zhanguoce 卷12` 與 `qunshu-zhiyao 6/347` 重抓後警告消失是真的，但那兩部屬另一個成因（來源有篇名、舊版爬蟲沒取到），不能外推到這 12 個。**外推前先跑 `--survey` 看來源實際長什麼樣，這一節就是沒跑就分類的後果。**

## 靜默失效：2026-08-09 抓到並修掉的三處

三件事共通點是**沒有任何東西報錯**。管線全綠、腳本 exit 0、檢查全過，但資料是壞的。往後新增檢查時優先想「哪種壞法不會叫」。

1. **重抓會整份覆寫 `meta.json`，`psych_survey` 因此被沖掉。**
   鹽鐵論重抓後書級 survey 變成 `null`，是修回填腳本時撞到 `TypeError` 才發現的。verify 只檢查該欄「存在」不檢查是否為 `null`，所以過得了。
   *修法兩邊都做*：`download-wikisource.py` 覆寫前把既有 `psych_survey` / `verified` 讀出來保留；`verify.py` 新增「有 `annotations.json` 且有段落已判讀，但 `psych_survey` 是 null」→ **error**（不是 warning）。
   **書級 survey 是讀完全書才生得出來的，沖掉等於白讀一部。撞到時從 git 撈回，不要重新推導。**

2. **codex 把整批判完了，卻沒有把檔案寫到磁碟。**
   b22 第一次執行的收尾訊息是「43 段已逐段判完；我正在把判定寫入輸出檔」，然後 process 正常結束，`out/` 是空的。exit code 0。
   *修法*：發包 prompt 明寫「本次交付物就是那個 .json 檔本身」「寫完後把該檔讀回來確認存在且 rows 數正確」，並把這次事故當反例寫進去。driver 每批結束檢查檔案是否存在。

3. **codex 把別批已完成的輸出整份重判覆寫。**
   多鏈並行時，某個實例把 `out/b22.json` 改掉了（11 段領域變、3 段 modes 變、42 條 reason 全改）。成因是發包 prompt 只禁「`out/` 以外」，而 b22 正在 `out/` 底下，而且我還請它拿 b22 當格式範例。
   *修法*：prompt 點名「你唯一可以建立或修改的檔案是 `out/<本批>.json`」並說明別批是已驗收成果；driver 每次呼叫前後取 SHA-256 比對，動到別批就在 log 裡吼。
   **順帶得到一個免費的重測**：兩次獨立判讀〈雅量〉，VI 都是 31/42。那個偏高是該門的真實形狀，不是拿門類名當答案。

4. **codex 印出一份執行計畫、問「確認後可以說『開始』」，然後結束。** b03、b10、b15 三次，磁碟上沒有檔案，exit 0。非互動式呼叫沒有人會回它「開始」。
   *修法*：prompt 開頭寫「**直接執行，不要先提出計畫徵求確認**——這是非互動式呼叫，你回問就等於整批白做」，並把這次事故當反例寫進去。三批重跑後全部成功。

**driver 的世代**：現行版是 `scripts/run-delegation.sh`（吃 slug 參數，一支通用）。它取代了 `delegation/shishuo-xinyu/` 底下兩支寫死 slug 的版本，**那兩支已於 2026-08-09 刪除**（v1 的 clobber 偵測會把「別的鏈剛建立的新檔」誤報成被覆寫，且 prompt 缺上面第 4 條的反例；v2 修好了但仍要逐書複製）。要看事故現場原貌就翻 git 歷史，**不要再在 `delegation/<slug>/` 底下放 driver。**

## 待使用者決定（三條皆已定案，保留紀錄）

3. ~~**群書治要裡摘自宗教庫典籍的 225 段要不要標**~~ —— **已定案：整部不收（2026-08-24 使用者裁決）**，詳見第 17 條的移除紀錄。

   原本待裁的是「論語 126／孝經 17／孟子 17／老莊列 65 共 225 段（8.3%）算不算與 religions-history 重複」，
   我當時建議 A 讀（標）。**裁決前把同一個檢查往下做完整部，發現待裁範圍嚴重低估：不是 8.3%，是 84%。**
   使用者的裁決依據就是這個數字，不是原本的兩讀論證——**先量全書再送裁，別只送發現的第一塊。**

1. ~~**`discourse_mode` 加不加「題例／worked instance」值**~~ —— **已定案並落地（2026-08-09）**。`vocab/discourse-modes.json` 升到 **v0.2**，加入第八值 `worked_instance`，並回填九章 491 段＋海島 13 段（海島 idx 16 是李淳風校勘記，不屬題例，排除）。驗證見 [`pilots/2026-07-28-discourse-mode-worked-instance.md`](./pilots/2026-07-28-discourse-mode-worked-instance.md)。加值當下 religions-history 完全沒有引用該表（無 `vocab/` 目錄、零 `discourse_mode` 欄位），所謂「共用表」是單邊的，無既有標註需重判——這點寫進了 changelog。
2. ~~**`Z-wisdom` 排除是否照舊 / 要不要加 `crosscurrents_hit`**~~ —— **已定案：照舊，不加欄位（2026-08-09 使用者裁決）**。

   裁決原文：「零命中就零命中 沒東西就沒東西 沒必要硬湊」。

   **一併撤回的還有「附錄段從密度分母拿掉」那條機制**（潛夫論 52 段附錄佔 19%）。附錄本來就在書裡，不另設分母。

   **提案被否決的理由值得記住，因為它會再犯**：`crosscurrents_hit` 的真正動機是「怕九章算術被讀成沒思想」——那是替結果找補，不是資料需要。本庫守則本來就寫明負面結果必須寫入、`domains_null` 要記，零命中是有效結果不是缺陷。**往後遇到「這個結果看起來不好看」的衝動，預設答案是照實記，不是加欄位。**

   原始觀察保留備查（不再是待辦）：九章思想密度最高的一段（〈方田〉割圓術劉徽注）零命中因它屬 `Z-wisdom` 支流；人物志 25 段空白同因；潛夫論 69 段空白裡只有 4 段屬 `Z-wisdom`，其餘是譜系與版本層。這些寫在各書 `psych_survey` 的敘述欄即可，不進 schema。

## 小學四部發包中：第五種體裁閘門與 shuowen b18 試點

**四部共用一道新閘門，但陷阱清單各寫各的。**字書不是要告訴讀者一件事，是要登錄一個字／詞，替換測試（§2.1）與刪除閘門（§2.2）都咬不到。閘門改成：**這一條除了「這個字怎麼寫、怎麼念、本義是什麼、哪本書這樣用過」之外，還說了關於人的什麼？**四部的拉力方向不同，所以 spec 的陷阱段不共用——說文是字義聯想（「孝：善事父母者」），釋名是聲訓（〈釋親屬〉〈釋喪制〉〈釋疾病〉〈釋言語〉逐篇對應到領域表的每一格，是本庫至今最大的硬撈風險），方言是地域對照被讀成 V，急就篇是姓名字表加兩層注（顏師古注 ＋ 王應麟補曰）。**四部的判空風險則一律集中在序跋**：許慎〈敘〉、顏師古〈急就篇注敘〉、郭璞〈方言序〉、〈跋李刻方言〉都是連續散文，不適用條目的判法。

**試點選 `shuowen b18` 而不是 b01**，因為它同時含最空與最滿的兩端：47 個部首的普通字條，加上第 530 章（章名逐字是 `15`，即許慎〈敘〉），而且整組干支部（甲乙丙丁…亥）都在裡面，是「陰陽五行說解會不會被誤判成 XII」這個陷阱的最佳測試點。**五條驗收條件在發包前就寫定**（列女傳那一節的教訓：試點的驗收條件應該是事先講好的幾格，不是整體印象），286 段結果全過：

1. 270 段判空（94%），reason 都指出是字義登錄或字形分析
2. 第 530 章 14 段中 11 段命中，對得上原文
3. 章名 `15` 逐字照抄，沒被「修正」成「卷十五」或「敘」
4. 零段疊三領域
5. **`XII` 只中 2 段，都是真祭儀**——車部軷祭（出行前告神、立壇、祭牲）與酉部祼禮（束茅置圭灌鬯）；亥部「十月，微陽起，接盛陰」與甲部的物候說解確實判空，reason 明寫「陰陽物候不標信仰領域」「屬干支構形訓詁」

**這次的可複用寫法是第六條：試點批次要挑「同時含兩端 ＋ 含最尖銳的那個陷阱」的批，不要預設挑 b01。**b01 只是切批順序上的第一批，跟判準難點沒有關係。

### 探針結果：`reference` 排除令只對三部之二成立

27 批於 2026-08-09 19:57 全數跑完，錨點與值域全過。結果**不是四部一致的零命中，而是分成兩端，分界不在類別在方法**：

| 書 | 段數 | 判空 | 命中 | 命中落點 |
|---|---|---|---|---|
| `shuowen-jiezi` | 6,070 | 99.3% | 42 | 許慎敘 10 段，其餘散在 30 個部首各 1–3 段 |
| `fangyan` | 385 | 98.7% | 5 | 序跋 3 段，本文只有卷三 2 段 |
| `shiming` | 945 | **89.4%** | **100** | 釋喪制 13、釋州國 12、釋親屬 12、釋言語 11 |

**說文與方言支持 SCHEMA §3 的原判，釋名推翻它。**釋名用聲訓，解字時必須說出「為什麼這樣叫」，而理由往往就是對人的判斷——「昆孫；昆，貫也，恩情轉遠，以禮貫連之耳」是世代親情遞減的命題（IV）、「鰥…愁挹不寐目恒鰥鰥然」寫喪偶失眠（VI／X）、「孝子之意澹然衰思益衰」直接觀察哀傷隨時序遞減（VI）。同一部書內，登錄名物的章判空，解釋人倫與喪制的章命中。V 的 40 段多是地域性格斷語（「楚…人性急，數有戰爭」「魯，魯鈍也…民性樸魯也」），屬 V 的「我群他群與偏見」。

方言的 2 段本文命中同理：它登錄各地稱名時記下農夫、庸役、商人與奴婢被冠以醜稱且視為卑賤——那是實際存在的群體貶抑，不是詞彙聯想。

**守則 1 在 `reference` 類別內部一樣成立**：同為字書而輪廓相去十倍，不可憑類別互相推定。SCHEMA §3 那句「硬跑標註只會產出噪音」是**未讀原文寫下的假設，現已部分證否**，該處待改寫。

### `jijiupian` v1 判讀不合格：SPEC 的陷阱清單漏掉了整個卷二（**v2 已完成回填**）

急就篇 v1（282 段、23 命中）**抽查未過，已作廢**，v1 輸出留在 `delegation/jijiupian/out-v1/` 備查。

失效原因是 spec 缺口而不是模型隨機出錯：**六條硬撈風險全是為卷一（姓名字表）寫的，卷二是器物飲食衣服農商的名物篇，它的陷阱從沒被點名。**結果卷二 15 段命中裡有系統性硬撈——

- 「貰，賖也。貸，假與也。出曰賣，入曰買」六個商業詞的字義，被判 VIII「實際描述商業營生」
- 「市，亦買也…四丈曰匹。兩邊具曰幅」訓詁加尺度定義，被判 VIII「商業經營與收益」
- 「綌，謂葛之麤者。紵…枲，麤麻也」四種麻布的分別，被判 V／XIII，理由寫「貧乏者沒有良好綿帛，只能用粗麻自行包裹」——**原文沒有任何一個字提到貧乏或包裹**

前兩者是 §2.1 講的「用詞彙聯想代替判讀」；第三者更嚴重，是替原文補上它沒說的話。**已在 spec 補第 7、8 條**（第 7 條用上述三段當反例，明寫分界線是「文本自己說出一句關於人的話才算，只是這個詞指的東西被某種人使用不算」；第 8 條要求寫 `reason` 前確認那句話在原文找得到）。三批同時重跑，避免同一部書內出現兩套判準。

**這次的教訓可複用：spec 的陷阱清單要按書的內部分卷各寫一遍。**急就篇卷一是姓名字、卷二是名物、卷三卷四是官職疾病，三種拉力完全不同，只為第一種寫陷阱等於後面沒防護。抽查時也要**按章分層取樣**，只抽前幾段會整批漏掉卷二。

**v2 結果（2026-08-09 回填完成）：282 段判空 271（96%），命中 11 段、7／13 領域。**補丁確實咬到了目標處，而且只咬那裡：

1. **卷二命中 15 → 4，被撤的 11 段全是詞彙聯想式硬撈。**三個事前點名的反例（貰賖貸賣買、市與匹幅、四種麻布）在 v2 全數判空；其中「貧乏者只能用粗麻自行包裹」這句原文沒有的話沒有再出現。**第 8 條（寫 `reason` 前確認那句話在原文找得到）防的是與詞彙聯想不同的一種錯——替原文補上它沒說的話**，這是本庫第一次為這種錯寫規則。
2. **補丁沒有波及卷一。**卷一 6 段命中全部落在開篇勸學正文與命名制度（分章課試任吏 VIII、史游勤心盡忠 VII、學僮當急就此學書 VIII、父名子與冠字及姓別婚姻 II+IV、詳備則師逸功倍 VIII、勉令勤習以致顯達 VIII），143 段姓名字表其餘全空。**修卷二的陷阱沒有讓卷一跟著變保守**——這是「按分卷各寫一遍」相對於「整體收緊判準」的差別。
3. **`observation` 276 段（98%）是本庫最極端的單一 mode 佔比**，來自逐條「某，某也」的訓詁體例。`narrative` 37 段全是姓氏源流敘事（某氏出於某國、其後因姓某焉），判空率不因有敘事而下降——洞冥記立的「`narrative` 是體裁指紋不是命中訊號」在字書上再次成立。
4. **一個邊界案例已記進 `psych_survey`，不要當穩固前例引用**：卷二 #112「言園圃種米，及殖果蓏，貧者食之，以免飢饉」判 IX+XIII。IX 站得住（有人在飢饉裡，文本在寫那件事），XIII 則是注家客觀說明園圃功用、沒有人替自己的處境開口，與鹽鐵論以下立的 XIII 觸發條件不完全相合。

**小學四部至此全部標完**，四部合計 7,682 段，輪廓分成兩端（釋名 89% 判空／12 領域 vs 說文 99.3%、方言 98.7%、急就篇 96%）。**分界不在類別在方法**：聲訓解字必須說出「為什麼這樣叫」，理由就是對人的判斷；登錄字形、方俗異名與姓名字則不必。

## `lushi-chunqiu` 已標完（2026-08-25）：736 段、13／13 全覆蓋、A 類 0 FAIL

160 章 736 段切成 18 批，通讀報告在 `delegation/lushi-chunqiu/reading/`。**五個體例群分派給既有五道閘門，沒有新寫閘門**：E1 月令 12 章 85 段（§2.3）／E2 技術登錄 6 章 41 段（§2.3＋§2.6②）／E3 論說 138 章 596 段（§2.6）／E4 敘事寓言 61 章 169 段（§2.2，內嵌於 E3 不另計）／E5 感應占候 4 章 14 段（§2.7）。

**感應這一格切成五側，比太玄的四側多一側「說辭側」**——`開春`[2] 那種感應語出自游說者之口，判的是遊說主題不是感應本身，故要求 V 或 VII 且不得含 XII。技術側 `[]`／認證側 XII／破除側 XI 不得含 XII／過度側 VII 照舊。

SPEC 最重的一組對照是 `上農`[1]（VII＋VIII 且不得含 V）對 `用民`[4]（判空）：兩段都在講「怎麼讓民聽話」，前者說出人是什麼樣的，後者只給政策鏈。另有 `制樂`[1] 與 `應同`[2] 必須同時含 XI＋XII、`仲夏紀`[4] 與 `仲冬紀`[4] 齋戒文字近乎逐字重出故兩段皆判空（月令格位而非主張）、`worked_instance` 全書 0 段、`domains` 長度 ≤ 3。

**三組重出段落用 difflib 量過，兩組寫進 SPEC 當硬條件、一組刻意留在 SPEC 之外當盲測錨點**：

- `應言`[1] 與 `應言`[2] 相似度 0.99（[1] 只多了「七曰：」四字篇次標記）→ 寫成必須判齊
- `任數`[4] 與 `慎人`[4] 相似度 0.08（同一則孔子絕糧，一在論術一在論遇）→ 寫成必須判異
- **`謹聽`[3] 與 `觀世`[1] 相似度 0.47、六個長共同塊、被切在不同批**——**刻意不寫進 SPEC**，留給互相看不到的兩次 API call 獨立判讀，事後比對才是獨立證據。同型盲測還有 `審己`／`過理`（齊湣王）與 `壅塞`／`不苟`（戎王女樂）兩組

**規格閘門已過**（`CLAUDE.md` §2.1 末段要求的兩步）：

- `accept.py --check-spec` 0 FAIL。錨點一律現場解析 SPEC 的四張表與 A 類條文，本檔不手抄任何章名、段號、引句。自檢抓到並修掉五處，其中**五群表 E2／E3 段數各差 2 是只驗總數看不出來的一型**（四群仍加總 736），因此加了「依章名歸群」逐群對拍
- `_selftest/make_cases.py --verify` 完美輸出 0 FAIL、17 種變異全部只響它該響的碼。**預期一律寫成完整 FAIL 碼可重集合**：`用民`[4] 同時是判空錨點與三章分裂試金石（A4＋A13）、`仲冬紀`[1] 同時在整章判空表與 XII 禁用清單（A2＋A9），這兩刀本來就該響兩下

**回收結果（18 批全回、`codex-gpt-5.6-sol`）：736 段命中 523、判空 213（29%）、13／13 全覆蓋，A 類 FAIL 0。**逐群命中率相差極大而兩端都查證過：E1 月令 0／85、E2 技術登錄 2／41、E3 論說 509／596（85%）、E5 感應 12／14——**籠統報 29% 判空完全不可讀，雜家一定要逐群拆開報**。領域 V 286／VII 220 為最大兩格，XII 只有 12 段且**一段都沒落在 E1 月令**（「其帝太皞，其神句芒」85 段的詞面陷阱事前寫死後零外溢），五分感應表的「同段兩向並存兩格都給」被觸發兩次（`制樂`[1]、`應同`[2] 同時 XI＋XII）。

**b01 試點的查證重點是過度命中而不是假陰性**（E3 12／12 全中）：IX 只出現在〈本生〉〈重己〉的養生論證段，四段〈貴公〉零 IX；VII 在〈貴公〉一律拆成可觀察行為才給。b02–b09 再抽 10 段自由判空段（`慎小`[4]、`功名`[3]、`古樂`[2]、`愛士`[4]、`禁塞`[1]、`懷寵`[3]、`論威`[3]、`盡數`[2]、`大樂`[1]、`決勝`[1]）逐段核對，無假陰性。

**B 類唯一偏離：XI 事前猜 15–40，實得 56。**抽 12 段查證全部成立，SPEC 帶寬已改成 40–70 並記誤差。**錯的是我的體裁認知不是判準**——我把 XI 想成「破除占候」那一側（`博志`[3]「非鬼告之也，精而熟之也」確是典型），但呂覽的 XI 大宗在〈察今〉變法、〈當染〉所染、〈尊師〉學能改變人、〈首時〉時遇、〈知分〉不受外力支配、〈執一〉順變、〈必己〉與時俱化——**整部書在談變・時・化・染**，那本來就是 XI 的正位。這是本庫數量帶寬第七次猜錯，第七次也是猜窄。

**三組盲測只有一組算數，另兩組要據實作廢。**`謹聽`[3]（b12）與 `觀世`[1]（b16）相似度 0.47、由互相看不到的兩次 API call 判出**相同的 V＋VIII**（modes 不同：`narrative`＋`proposition` vs `proposition`＋`prescription`）——這是真的跨批一致性證據。另兩組事後回查相似度只有 0.19（`審己`[5]／`過理`[2]）與 0.27（`壅塞`[2]／`不苟`[3]），**是同一史事的兩次不同敘述而非逐字重出，判讀不同不構成分歧、判讀相同也不構成一致**，不列為證據。**教訓：埋盲測錨點時要先確認它真的是重出，相似度門檻至少 0.45，靠主題平行挑出來的不算。**

已跑完 `make-scaffold.py` → `apply-delegation.py --tagged-by codex-gpt-5.6-sol` → `psych_survey` → `build-index.py` → `verify.py`（76 部 0 errors）。

## `mozi` 已標完（2026-08-25）：606 段、12／13、**本庫第一部零判空體例群**

53 章 606 段切成 17 批。**五個體例群分派給既有五道閘門，沒有新寫閘門**（雜家之後第二次）：G1 前期雜論 7 章 30 段（§2.6）／G2 十論 24 章 193 段（§2.6）／G3 墨經 6 章 213 段（§2.1 中性物件替換）／G4 墨語 5 章 88 段（§2.2 刪掉這一則）／G5 城守 11 章 82 段（§2.5 怎麼打之外）。**與呂覽的差別是五群用了四道不同閘門**（呂覽的五群集中在 §2.3／§2.6），這是「分派表」機制第一次跨到四道。

**五群命中率相差七倍，兩端都查證過**：G1 29／30（96%）、G2 159／193（82%）、G3 59／213（27%）、G4 88／88（**100%**）、G5 12／82（14%）。**籠統報全書 57% 命中完全不可讀**，五群帶寬事前全部寫死、回收五條全中。

**G4 墨語 88／88 零判空，是本庫至今唯一零判空的體例群。**耕柱 22、貴義 20、公孟 24、魯問 21、公輸 1 沒有一段判空。§2.2 的閘門問「把這一則刪掉，讀者少知道了什麼」，而墨語每一則都是墨子當場對人下斷語（不是史料事實），整群都在承重側。**列女傳 4% 判空原本是這道閘門的上界，墨語把上界推到 0%**；同書同基準同判者的 G5 是 14%、G3 是 27%，三群並列是 §2.2 末條「兩端的差距是內容差距不是判準差距」目前最強的證據。

**群內落差比群間落差更需要注意：G3 墨經裡 `大取` 36／54（67%）與 `經說下` 3／76（4%）差 16 倍**（`經說上` 17／65、`小取` 1／16）。同屬墨經、同一道閘門，差別在文本形態——`經說下` 是對「經」條的逐條技術訓釋（光影、力、堅白、同異之辯），把序號拆下來不成話；`大取` 整篇在論愛與利的權衡（「斷指以存腕，利之中取大，害之中取小」），拆下來仍是對人的斷語。**§2.6 末段「報總判空率之前先逐章拆開」在群內部又重演一次。**

**XII 68 段是本庫數量帶寬第八次猜錯，也是絕對誤差最大的一次（事前寫 25–50）。**分布集中在明鬼下 16、天志中 11、天志上 8、天志下 7（合計 42），其餘散在尚賢中 5、公孟 5、非攻下 3、尚同中 3、法儀 2、節葬下 2 等。**抽查 18 段（含全部非天志明鬼章）逐段判對**，每段都逐字引到文本自己把意志或賞罰歸給天鬼：`法儀`[3]「天必欲人之相愛相利」、`尚賢中`[7]「天鬼賞之，立爲天子」、`非攻下`[4]「天命殛之」「天乃命湯」、`大取`[1]「天之愛人也，薄於聖人之愛人也」、`耕柱`[2]「鬼神之明智於聖人也」、`公孟`[7]「鬼神爲神明，而爲禍福」。**處置照守則是記誤差不鬆判準。**這是 §2.7 四側切法「認證側」的最大規模實例——同一格在兵家五部 0、京氏易傳 0、太玄經 4、墨子 68，六部量出這個跨度是它有區辨力而非詞面反射的證據。**錯的是我拿其他諸子的密度去猜一個把神聖賞罰當論證骨幹的學派。**

**`narrative` 114 段同樣超出 40–80，同屬猜錯**：69 段在 G4 墨語（78% 是 `narrative`，對話軼事集的體裁指紋），明鬼下另有 7 段舉證性鬼故事。B 類 14 條：11 中、2 超帶寬、1 條（V 與 VII 為最大兩格）無法自動量化，人工確認成立（VII 191／V 164）。

**G2 十論最大的風險是套語不是內容，事前寫死後沒有整批塌掉。**`今天下之士君子` 出現在 8 章 19 段；`兼愛中`[7] 與 `非命上`[8] 前十七字幾乎逐字相同（相似度 0.683，全書跨批最高），主張的卻是兼相愛與非有命兩件事。SPEC 寫死「判的是套語後面接了什麼，不是套語本身；領域由『當……』『不可不……』轉折之後的句子決定，前半不供領域」，回收後兩段判出正確且不同的領域。全書跨批 ≥0.45 共 22 對，其餘 21 對高在套語；十論上中下之間章級相似度只有 0.023–0.276，是同題異敘不是重出。

**跨批盲測不一致，而且不一致的那一側可以指名。**`尚同上`[1]（b02）與 `尚同中`[1]（b03）相似度 0.667，是全書唯一一對同題同內容近逐字重出（其餘 21 對都是套語）——b02 判 IV＋V＋VII，b03 判 V＋VII，**分歧在 IV**：`尚同中`[1] 自己的 reason 引了「家人離散不能相和合」卻沒給 IV，依原文 **b02 那側較忠實**。同型於韓非子〈揚榷〉／〈揚權〉10 段 7 段一致。記錄不擋收。

**A 類 19 條在 17 批全數 0 FAIL、零 clobber**（每批回收即 `git status` 查改動並單獨 commit）。驗收器 `delegation/mozi/accept.py`（1,346 行）由 Claude sub-agent 撰寫、由我獨立重跑後才發包，錨點全部現場解析 SPEC 表格，零硬編章名；`_selftest/make_cases.py --verify` 完美輸出 0 FAIL、23 種變異全部只響它該響的碼。

**⚠️ 本次最重要的事故：改 SPEC 把一族檢查靜默關掉，而自檢照樣印「0 FAIL / 0 NOTE」。**發包前 `--check-spec` 抓到我 SPEC 裡一條事實錯誤的 B 類宣告（我寫盲測那一對是「全書唯一通過 ≥0.45 門檻」，實測 22 對），我改寫了那一句——**而 S13 這一族的 parser 咬的正是被我刪掉的字面**，`s['blind']` 從此恆為 `None`，整族斷言停跑。**我一度把那個 0 NOTE 當成通過回報出去。**發現的線索不是自檢，是全書 B 類報告裡少了「跨批盲測」那一行。修法是讓 parser 咬新寫法並把原本只記 NOTE 的斷言全升成 FAIL（對子總數、頂對子是誰、頂對子相似度容差 0.005、章級帶寬上下界、段級超標對數與最高值），**再用 `_selftest/probe_s13.py` 逐條擾動 SPEC 證明六條都真的會叫**。

> **這條要當通則帶到下一部書：改過 SPEC 之後的第一次乾淨自檢，必須用擾動證明檢查還活著，不能假設。**「0 FAIL」與「0 FAIL 且 0 NOTE」的差別在於後者可能是檢查沒跑。凡驗收器用字面 regex 去咬 SPEC 散文的地方，SPEC 一改就可能斷；`probe_s13.py` 那個模式（擾動 → 斷言會叫 → 還原 → 確認位元組不變）可以直接複製到別的書。**附帶一個 Windows 陷阱**：探針改寫 SPEC 一律走 `read_bytes`／`write_bytes`，`read_text`／`write_text` 會把 LF 換成 CRLF，本庫 `.gitattributes` 強制 LF。

**已知殘留缺陷（不影響驗收，列入 C 組待辦）**：〈經上〉〈經下〉在 Wikisource 各是一整塊未斷條，故各只切出 1 段（傳世約 98 與 82 條），此二段按整塊判讀、各命中 1 格。墨經的承重內容在〈經說上〉65 條與〈經說下〉76 條已有條級粒度，句讀切分器處理後應回頭重判此二段。

已跑完 `make-scaffold.py` → `apply-delegation.py --tagged-by gpt-5.6-sol` → `psych_survey` → `build-index.py` → `verify.py`（76 部 0 errors）。

## `yanzi-chunqiu` 已標完（2026-08-25）：218 段、12／13、**判空只 2 段，本庫命中率最高的整部書**

215 章 218 段 8 卷切成 7 批。**兩個體例群分派兩道既有閘門**（呂覽、墨子之後第三次用分派表）：G1 敘事群 155 章 158 段（§2.2 刪掉這一則）／G2 論說群 60 章 60 段（問上・問下，§2.6 怎麼治之外）。**驗收器一律按章名歸群，不按批次**——本書七批有五批跨卷，按批次歸群必錯。

**這是第一部兩群命中率幾乎相同的書：G1 157／158（99%）、G2 59／60（98%）。**呂覽五群 0%–86%、墨子五群 14%–100%，晏子只差一個百分點；八卷逐卷拆開同樣平坦（諫上・諫下・問上・雜上・雜下・外篇重而異者 100%，問下 96%，外篇不合經術者 94%）。**但這不代表分群多餘**——問對體若被 §2.2 的問句問成「刪掉這一則少了什麼史料」，60 段會整批塌成判空。**分派表擋掉的是問錯問句，不是命中率差異；「分完群發現兩群一樣」也是結果。**這部書沒有登錄層、沒有條文層、沒有技術層（`formalization` 與 `worked_instance` 全書掛零），整部是諫諍與問對輯錄，每一則都是晏子當場對人下斷語——**與墨子 G4 墨語 88／88 是同一個結構**（§2.2 末條：兩端的差距是內容差距不是判準差距）。

**判空只有 2 段，而且兩段都經得起查。**① `景公問天下有極大極細，晏子對第十四`[1] 是我事前寫死的唯一判空錨點——鵬與蟲的博物問答，通篇沒有一句落到人身上；② `景公問諸侯孰危晏於對以莒其先亡第九`[1] **不在任何錨點表上**，是判者自己在 G2 判出的純地緣存亡預測，沒有一句對人性下斷語。**後者是 §2.6 閘門在無指引處自己成立的一次**（同風俗通義〈世間多有精物妖怪百端〉那一筆）。

**XII 的三側切法是這部書最需要事前寫死的一格，回收後零外溢。**破除側 8 段（必含 XI 且不得含 XII）／認證側 2 段（必含 XII）／技術側 4 段（非空但一格都不得填 XII），全部照判。祠、禱、巫、彗星、祟、神、上帝的詞面遍布諫上諫下——單是 b01 的 25 段就密集出現——而全書 XII 只有 3 段。**第三段是判者獨立判出的真陽性**：`景公問聖人之不得意何如晏子對以不與世陷乎邪第二十二`[1] 不在任何錨點表上，「四時易序，神祇並怨」與「神降福而不靡」是文本自己把怨與福給了神祇。破除側的止禳彗星、辭祠靈山河伯一類段落全部落 XI 而無一含 XII，是「否定占候→XI／肯定感應→XII」那一組的又一部複驗書。

**XI 27 段超出事前帶寬 10–25，是數量帶寬第九次猜錯**（誤差 2 段、方向猜少了）。原因與體裁直接相關：晏子諫諍的主軸就是把禍福從天命挪回君主可改的行動，破除側 8 段之外還有大量非占候的諫辭在做同一件事。逐段查證判對，處置照舊記誤差不鬆判準。**其餘 11 條 B 類全部命中**：判空 ≤15 段（2）、XII 2–6（3）、X 5–15（14）、`narrative` ≥140（159）、`formalization` 0–2（0）、`ritual` 5–20（12）、命中領域 11–13 格（12）、V 與 VII 為最大兩格（175／113）、零段 domains 0–2 格（1）。

**I（存在與意義）掛零是一個經得起反查的負面結果。**全書 50 段帶生計詞面（倉廩、賦斂、饑饉、貧、粟、凍寒），判的是 V 38／VII 23／IV 7／XI 5／II 5／IX 4——**I 一段都沒有**。晏子論賑饑、辭邑、拒更宅，承重的是君主該不該取與晏子如何自處，不是有人在追問為何而活。同 §2.4「IX 不是提到病，是有人在病裡」的操作方式。

**跨批盲測本書主動不設，不是遺漏。**全書 218 段兩兩比對，跨批最高相似度只有 0.392（門檻 0.45）；外篇兩卷雖是內篇的重出，與對應章最高只到 0.385——**是同一史事的兩次不同敘述，判讀不同不構成分歧、相同也不構成一致**。硬挑一對當盲測是自欺（呂覽教訓）。**三個灰區段判者全部自裁並在 reason 寫出選了哪一讀**，判空錨點未被推翻。

**A 類 16 條、39 個錨點，七批全數 0 FAIL / 0 NOTE，零校準全過。**發包前三道閘門全綠：`--check-spec` 0 FAIL 並印出解析涵蓋（39 錨點／8 區段／灰區 3 段）、`_selftest/make_cases.py --verify` 20 個變異全對答案、`_selftest/probe_s13.py` 20 條擾動全部叫且 SPEC 位元組還原相同。

**⚠️ 探針當場抓到一族已經停跑的斷言，這是本次最有價值的一筆。**S11（灰區段落必須存在於語料）原本只驗「至少解析到一條」，我把灰區三個 bullet 之一的格式改壞後，另外兩條照樣命中、自檢照樣全綠——**那正是墨子事故的形態：斷掉的表現是更綠不是報錯**。改成「灰區區段底下每一個 bullet 都必須解析得出來，數量對不上就 FAIL」之後才會叫。

> **這條要當通則帶到下一部書：任何 `if not xs` 型的存在性斷言都有這個破口。凡是從 SPEC 解析出清單的地方都要驗數量，不要驗非空。**墨子那次是 parser 咬到被刪掉的字面而整族停跑，這次是 parser 還活著但覆蓋率悄悄掉了三分之一——兩種都印 0 FAIL。

**變異測試那邊也修了一筆**：三個讓段落從 rows 消失的變異必然連帶打破卷的章數與段數，照 `CLAUDE.md`「完整 FAIL 碼集合」的規定改宣告 `{A1, A16}` 兩碼，並順手刪掉 `accept.py` 裡一段永遠到不了的 A16 逐列分支（`rows` 只收已在 `corpus.para` 裡的鍵，`corpus.juan` 由同一份解析建起，該條件恆為假）。

**新增跨書工具 `scripts/check-reason-quotes.py`，補上 `CLAUDE.md` 規定卻一直沒有機械檢查的那一項。**`check-delegation-out.py` 只驗鍵與值域、不看 reason 文字，而蔡中郎集的破口正是 reason 引句來源混雜（本段逐字／他章對照／通讀者概括三種）。新腳本檢查每條 reason 裡每一段 4 字以上的「…」是否逐字出自該段自己的正文（`……` 節引逐段比對），並單獨列出「有 domains 卻一句都沒引」的段。全書實測 **519 段引句，1 條不在本段、0 段有格無引句**：`景公路寢台成逢於何願合葬晏子諫而許第二十`[1] 把「兆在路寢之台，墉下」寫成「兆在路寢當墉下」，語意無誤、該段另一句承重句逐字，IV＋V 判斷成立——**記錯不鬆判準、不重發包**。

已跑完 `check-delegation-out.py` → `make-scaffold.py` → `apply-delegation.py --tagged-by gpt-5.6-sol` → `psych_survey` → `build-index.py` → `verify.py`（76 部 0 errors）。

## `wenxin-diaolong` 已標完（2026-08-26）：424 段、**13／13 全覆蓋**、A 類 19 條七批 0 FAIL

**管線全跑完**：通讀 → `SPEC.md` → `accept.py` ＋ `_selftest/` → `--check-spec` 0 FAIL → `make_cases.py --verify` 31 個變異全對答案 → `probe_s13.py` 40 條擾動全部觸發且 SPEC 位元組還原相同 → `run-delegation.sh` 串行發包七批（每批回收即單獨 commit＋push）→ `check-delegation-out.py` ＋ `check-reason-quotes.py`（509 段引句 **0 誤差**，本庫第一部零誤差的書）→ `make-scaffold.py` → `apply-delegation.py --tagged-by gpt-5.6-sol` → `psych_survey` → `build-index.py` → `verify.py`（76 部 0 errors／19 warnings）。逐書數字寫在 `translations/wenxin-diaolong/meta.json` 的 `psych_survey.verdict`，此處只留可重用的方法結果。

**第八道閘門 §2.8 已寫進 `CLAUDE.md`，因為回收量得出效力。**G3 創作論同群拆兩側：技術側 14 篇 93 段 12%、人側 5 篇 34 段 53%，同群同閘門同判者差 4.4 倍；逐章兩端〈養氣〉6／6、〈程器〉7／7、〈諧讔〉9／9 對〈麗辭〉〈章句〉〈通變〉〈定勢〉〈總術〉全零。七群命中率：G1 47%／G2 32%／G2′ 80%／G3技 12%／G3人 53%／G4 62%／G5 67%／G6 0%。全書命中 34%、判空 66%。

**三件寫回 `CLAUDE.md` 的方法結果：**

- **分派表的第二個用途是推帶寬。**B 類 22 條只錯 2 條（G3 人側 55–80% 實得 53%、XI 5–15 實得 4），是本庫數量帶寬至今最好的一次——因為七群各給一條帶寬，從本書自己的體例推，不是從別部書外推（對照墨子 XII 猜 25–50 實得 68）
- **靜默失效的第三種形態**：條文在 SPEC 散文寫得出來，但 parser 只表示得出否定形，肯定那一半從來不會叫。A14 要求 `序志`[9] 的 `modes` 必含 `expression`，而 `parse_requirement` 只產得出 `forbid_m`；同輪還有 `詔策`[5] 的「非空（不指定格）」沒有羅馬數字、`nonempty` 恆假卻仍計入宣告的 27。修法是端到端補 `require_m`／`said_nonempty`，再加 S17 反過來驗「散文說的每一件事，結構裡都找得到」
- **盲測第一次回「不一致」，而形態是包含不是相反**：`諧讔`[1] `['VI']` vs `諧隱`[1] `['V','VI']`（相似度 0.939），兩批引同一句判 VI，其中一批多掃到同段第二句判 V。**分歧在逐句掃描完整度不在判讀方向**，同管子〈事語〉[2] 那型。回報盲測要分相同／包含／相反三種寫

**〈原文集注〉8 段全判空**，事前寫死的包含條件 `domains([1]) ⊇ domains([2]) ∪ domains([3])` 自然成立。**〈書記〉30 段只中 3 段**（`書記`[7]–[27] 上界 5 沒撞），那一篇實走 §2.3 登錄體。**〈誄碑〉X 只用 1 格**（上界 2），複驗蔡中郎集碑誄體那一格。**I 6 段是沒寫任何錨點卻判出來的格**（〈序志〉「形同草木之脆，名踰金石之堅」一路），是閘門在無指引處自己成立的第三次。

### 開工當時的紀錄（判斷依據仍有效，保留）

424 段／52 章／48,401 字切成 7 批（`b01`–`b07`，每批 4,987–7,905 字）：

```
PYTHONIOENCODING=utf-8 python scripts/make-delegation-input.py --slug wenxin-diaolong --split-before 諧隱
```

**`--split-before 諧隱` 是承重參數，不是整理**——見下方第二點。

**這是本庫第一部文論，七道閘門沒有一道是為它寫的。**`READING-GATE.md` §1 的分派表因此明寫「是規劃者的假設，要由通讀來檢驗」，§7 灰區必須誠實記下切不動的地方。分派表依〈序志〉[6] 自述的結構分六群（章／段合計 424）：G1 文之樞紐 5 篇 30 段（〈正緯〉另走 §2.7）／G2 文體論 20 篇 205 段（§2.3＋§2.7）／G2′ 諧隱 1 篇 5 段／G3 創作論 19 篇 127 段／G4 批評論 5 篇 40 段／G5〈序志〉9 段（§2.2）／G6〈原文集注〉8 段（§2.7）。**提議的第八道閘門**（形狀同 §2.4／§2.5／§2.6）：「這一段除了『這個文體是什麼／從哪來／怎麼寫才好／誰寫得好不好』之外，還說了關於人的什麼？」——**通讀確認才寫進 `CLAUDE.md` §2.8，前例是 §2.7 在風俗通義通讀後才定，不是之前。**

**52 章對不上 50 篇，處置一律在標註層，不動 `raw/original.txt`。**我一度把它寫成「必須先修的上游缺陷」，**那是錯的**：`hanfeizi` 的 `chapter_count 59 / expected_chapter_count 55` 至今保留〈八奸〉／〈八姦〉與〈揚榷〉／〈揚權〉兩組重出，`huainanzi` 的〈墬形訓〉也還在；`meta.json` 的 `expected_chapter_count` 欄存在的目的就是記這個差。`CLAUDE.md` §2.6 甚至拿〈揚榷〉／〈揚權〉當內建盲測。三項異常逐一：

- **〈諧讔〉／〈諧隱〉是同一篇的重出**（兩章的標題行都是 `## 諧讔第十五`）。三對逐字重出段的相似度 **0.939／0.841／0.813**，`--split-before 諧隱` 把兩章推進 b05 與 b06 兩個互相看不到的 API call——**這是本庫至今最好的跨批盲測材料**（呂覽唯一有效的一對只有 0.47，墨子最好 0.667，晏子全書最高 0.392 而主動不設）。**兩章都留、兩章都判**，前例是韓非子與淮南子
- **〈原文集注〉8 段是清人集注**，前例為韓非子〈韓非子全文〉18 段目次章（SPEC 整章點名判空＋`formalization`）。它自己還有爬取造成的內部重複（[2]＋[3] ⊂ [1]，[6]＋[7] ⊂ [5]），額外給一個章內一致性檢查
- **〈原道第一〉是全書唯一帶篇次的章名**，SPEC 與錨點一律逐字照抄（`原道第一` ≠ `原道`）。同型的還有 `頌讚`——標題行寫的是 `## 頌贊第九`，**以章名為準**

**底本其餘部分乾淨，兩件事查過可以省掉猜測**：全書 `〈〉` 夾注 **0 個**（戰國策／水經注那條「判空錨點只讀正文等於只覆蓋半個段落」在本書不適用），只有〈隱秀〉5 個 `（）` 校記與 3 個 `[N]` 標記；除〈原文集注〉外無其他章有內部包含型重複。另有兩件要寫進 SPEC：**約 99 段（23%）是「贊曰」韻語**（照它自己的文字判，前例列女傳頌曰，§5 不可空）、**63 處不帶括號的 `一作「X」` 校異散在 12 篇**（逐字照抄，不清理）。

**最大的詞面陷阱是「情」「心」「志」「氣」「性」「神」——在《文心》裡是文論術語不是心理狀態**，這是同一條規則的第五個體裁版本（前四個：醫書的神魂魄、兵書的占候、風俗通義的祀典、太玄的術數）。真正的例外是〈體性〉〈養氣〉。**寫這條時我自己就踩了一次**：引〈體性〉寫成「才有庸儁」，原文是「然才有庸**俊**」——正是 gate 自己警告的異體字破口（風俗通義首版 79 條錨點錯 16 條）。改掉後全檔 25 條逐字引句回批次檔對拍 0 FAIL。

**配額判斷曾經搞錯一次，錯法值得記下來。**我先讀 deskboard `llm_usage.read_chatgpt()` 得到「7D 已用 98%、重置約 2026-08-31」，據此決定整窗不發包。**那是錯的**：那筆讀數 `status='stale'`、`fetched_at` 是 3.25 小時前，而它的來源只是**最後一次 codex session 寫下的快照**，不是即時查詢；7D 窗在那之後已經滾過。發一個最小 codex 呼叫刷新後，session 檔的原始 payload 是 `primary 300m 0.0%` ／ `secondary 10080m 0.0%`——**兩個窗都是滿的**。

> **通則：撞牆判斷不可只讀 deskboard 快照。**`read_chatgpt()` 回傳的是最後一次 session 的殘留值，`status='stale'` 就代表它可能已經過期整整一個窗。**正確動作是先發一個最小 codex 呼叫（三千 token 以內）刷新，再讀 `~/.codex/sessions/**/*.jsonl` 的 `rate_limits` 原始欄位**（`primary`＝5H、`secondary`＝7D）。另注意 deskboard 只呈現用量 >0 的窗、全 0 時才 fallback 印 primary，**「某個窗沒出現在輸出裡」不等於它不存在**。

**發包順序**：`bash scripts/run-reading.sh wenxin-diaolong g01 b01 b02 b03` → `g02 b04 b05 b06 b07` → 寫 `SPEC.md` → `accept.py` ＋ `_selftest/` → `--check-spec` 到 0 FAIL → 變異測試 → `probe_s13.py` 擾動 → `run-delegation.sh` 串行發包 → 逐批驗 → `check-delegation-out.py` ＋ `check-reason-quotes.py` → `make-scaffold.py` → `apply-delegation.py` → `psych_survey` → `build-index.py` → `verify.py` → 對齊文件 → push。

## 未標存量（2026-08-24 全量盤點，扣除已標完的 `lushi-chunqiu`、`mozi`、`yanzi-chunqiu` 與 `wenxin-diaolong` 後餘 27 部／6,858 段／約 849,568 字）

> 數字由 `split_paragraphs` 現算，不是抄的。**分組是「動工前還要做什麼」，不是價值排序**——`CLAUDE.md` §1 禁止憑書名或類別判定標註價值。
>
> **切段健康度要用「整章＝一段的章數」＋「巨段（>2000 字）字數佔比」量，不要用書級中位數或字/段。**首版分組拿中位數與「>1200 字段落數」判，把淮南子放進 A 組——它中位數 240 字、只有 11 段超過 1200 字，看起來很正常，實際 **6/22 章整章崩成一段、佔全書 28% 字數**（俶真訓 6,074／時則訓 6,742／氾論訓 9,818／說山訓 6,534／說林訓 6,100／齊俗訓 8,789）。書級統計會被健康章稀釋，崩塌是**章級**現象，必須逐章看。全 31 部重掃後只有四部中招：`guiguzi` 巨段佔 39%、`huainanzi` 35.5%、`heguanzi` 15.2%、`yi-zhoushu` 13.3%（僅〈周月解〉一章），其餘 27 部全為 0%。
>
> 重算（未標＝沒有 `annotations.json` 者）：
>
> ```bash
> PYTHONIOENCODING=utf-8 python -c "
> import glob,os,sys; sys.path.insert(0,'scripts')
> from corpus_text import split_paragraphs
> from collections import defaultdict
> for m in sorted(glob.glob('translations/*/meta.json')):
>     d=os.path.dirname(m)
>     if os.path.exists(os.path.join(d,'annotations.json')): continue
>     ps=split_paragraphs(open(os.path.join(d,'raw','original.txt'),encoding='utf-8').read())
>     ch=defaultdict(list)
>     for _,c,_,t in ps: ch[c].append(len(t))
>     tot=sum(sum(v) for v in ch.values())
>     badn=sum(1 for v in ch.values() if len(v)==1 and sum(v)>2000)
>     mega=sum(x for v in ch.values() for x in v if x>2000)
>     print(f'{os.path.basename(d):24s} {len(ps):5d}段 {tot:7d}字 整章一段{badn:3d}/{len(ch):<3d} 巨段{mega/tot*100:5.1f}%')
> "
> ```

**A. 可直接排批次（切段健康、章名可當錨點）——6 部、2,131 段**

| 部 | 類 | 段 | 字 | 字/段 | 章 |
|---|---|---|---|---|---|
| `wuyue-chunqiu` | 雜史 | 493 | 49,942 | 101 | 38 |
| `wenxin-diaolong` | 文論 | 424 | 48,401 | 114 | 52 |
| `jinlouzi` | 人論 | 515 | 64,863 | 125 | 17 |
| `yuejueshu` | 雜史 | 307 | 38,206 | 124 | 19 |
| `yi-zhoushu` | 雜史 | 175 | 56,233 | 321 | 66 |
| `yulizi` | 雜著 | 217 | 50,298 | 231 | 182 |

**B. 序號章標籤佔多數，發包前先補錨點——3 部、653 段**：`wenzi`（12/12 序號）、`jiayi-xinshu`（10/11）、`lujia-xinyu`（12/15）。**這不是硬阻擋**——`kongcongzi`（19/19 序號）、`shuowen-jiezi`、`qianfulun`、`fengsu-tongyi`、`shuijingzhu` 都是帶著同款警告標完的。做法是在 `MANIFEST` 與 SPEC 裡把序號章補上實際篇名（`卷一` → `卷一（道原）`），**不動 `raw/original.txt`**（動了破 SHA-256）。

**C. 卡在上游「一章＝一段」缺陷，必須先做句讀切分——6 部、529 段**：`heguanzi` 19 段/19 章（巨段佔 15.2%）、`zhonglun` 35/25、`guiguzi` 72/21（巨段佔 **39%**，最長 2,917）、`yandanzi` 5/4、`sanzijing` 1/1（整書 1,411 字一段）、**`huainanzi` 397/22——16 章健康、6 章崩塌**（巨段佔 35.5%）。

  `huainanzi` 是部分崩塌，處理順序與其他五部不同：**健康的 16 章（353 段）可照常發包，崩塌的 6 章（44,057 字）等切分器**，該 6 章的段落先留 `null`＝未標（前例：`cai-zhonglang-ji`〈外集卷四〉110 段）。`yi-zhoushu` 只有〈周月解〉一章崩塌（巨段 13.3%），同樣做法，故仍留在 A 組。**重抓沒有用**——Wikisource 把整章包在單一 block element 裡，`extract_main_text()`（`scripts/download-*.py` 254–271 行）沒有 `\n` 可切。要寫一支標點＋語意雙軌的切分器，切完的段落序另存、不覆蓋 raw。**這是獨立工程，不要混進標註批次。**

  切分器的待辦清單另加 `mozi`〈經上〉（1 段 751 字）與〈經下〉（1 段 1,074 字）——同一個上游成因（Wikisource 整篇一個 block），但這兩篇的墨經條目本來就用「A，B 也」的定義句串成，切分器該優先拿它們當測試案例。**沒有阻擋 mozi 發包**（2026-08-25 已標完）：承重的德目定義在〈經說上〉65 條／〈經說下〉76 條已有條級粒度，此二段按整塊判讀、各命中 1 格，**切分器完成後要回頭重判這兩段**。

**D. 短小可成組發包——6 部、499 段**：`houheixue` 83、`qijing` 15、`lusheng-mobian-zhuxu` 4、`mu-tianzi-zhuan` 107、`xijing-zaji` 146、`songjingwen-gong-biji` 144。同體裁成組發包時試點只需做一次（§下一步第 5 條）。

**E. 動工前先判體裁閘門歸屬——7 部、3,342 段**：`yilin` 1,321 段（類書輯句，43 字/段）、`shanhaijing` 843（術數，55 字/段）、`zhushu-jinian` 351（編年，34 字/段）、`jiaoshi-yilin` 258（**上游截斷：4 章 vs 應有 16，先補全再說**）、`zhoubi-suanjing` 243、`sunzi-suanjing` 217、`chuci` 109。前四部都是超短段密集型，**判空率會很高，但不得憑此跳過通讀**（`CLAUDE.md` Anti-pattern 第二條）。`chuci` 與 `shanhaijing` 的 `category` 已知標錯，**必須標完再改**（§下一步第 2c 條）。

## 下一步

1. **~~`yanshi-jiaxun`~~ / ~~`shishuo-xinyu`~~ —— 皆已完成（2026-08-09）**。前者 255 段解除 V 過寬疑慮，後者 1,132 段兌現硬規則第 5 條。結論見上方兩節。**連帶定案：鹽鐵論與潛夫論不必回頭重判。**
2. **~~第九至十四部~~ —— 皆已完成（2026-08-09）**：`shenyijing`／`dongmingji`／`gu-sanfen`／`nanjing`／`shanghanlun`／`jinkui-yaolue`。結論見上方三節。**挑第十五部的準則**：十四部已經把「方法找不找得到東西」「說不說得出沒有」「離開算書還切不切得動」「V 是不是萬用桶」「XIII 靠什麼觸發」「敘事段能不能判領域」「沒有敘事者時判準會不會空轉」「同一部書內部體例分歧能不能分開讀」「IX 在它本來的語境裡靠什麼觸發」都問過了。**還沒被檢驗的軸**：
   - (a) ~~**phase 2 小學**~~ —— **四部全數完成（2026-08-09）**。說文 6,070 段／釋名 945 段／方言 385 段（`46d373b`）＋急就篇 282 段 v2（按卷分層抽查通過後回填）。結果與教訓見上方兩節。
   - (b) ~~**一部「已知會大量命中」的書當上界對照**~~ —— **已完成（列女傳 208 段，2026-08-09）**，結論見上方列女傳一節：96% 命中、12／13，**判準沒有漂向保守**。
   - (c) `chuci` 與 `shanhaijing` 的 `category` 修正（見已知風險）——**兩者都必須先標完再改分類**，順序同古三墳。

3. **發包流程已固定成四支腳本，取代原本每部書臨時重寫的做法**：`make-delegation-input.py --slug X` 切批（不拆章、字數軟上限 8000、錨點取自 `split_paragraphs`）→ 寫 `delegation/X/SPEC.md` → **先發 b01 當試點，校準出的規則補回 spec 才放行其餘批次** → `bash scripts/run-delegation.sh X b01 b02 ...` 串行發包 codex → `check-delegation-out.py --slug X` 做回填前雙向錨點與值域檢查 → **`make-scaffold.py --slug X` 建骨架**（`apply-delegation.py` 只改寫既有的 `annotations.json`，不會自己建；沒先跑這步會 `FileNotFoundError`，六韜就踩到了）→ `apply-delegation.py --slug X --tagged-by <model>` 回填 → `annotate.py stats X` 取數字寫 `psych_survey` → `verify.py` → `build-index.py` → 對齊 HANDOFF／MAP → commit + push。
   - **試點那一步不可省。** b01 是校準條文寫進 spec 之前判的，12/36 段疊到三個以上 `modes`、自述家規漏了 IV；b02 之後讀新版 spec 判出來 0 段疊加。**規則寫進 spec 就會生效，靠範例檔傳遞不會。**
   - **試點批不一定是 b01，挑「兩端 ＋ 最鋒利的陷阱都在裡面」的那一批。** 六韜挑 b02（同時含〈勵軍〉這種必須命中的與〈陰符〉〈軍用〉這種必須判空的，還有〈軍勢〉「猶豫最大」這個最容易誤填 VI 的地方），b01 的文韜武韜反而是最好判的密集命中區。**另外：驗收條件要在發包前逐條寫死**（六韜寫了六條），事後憑整體印象看「還不錯」抓不到跨批不一致——六韜的〈武車士〉〈武騎士〉就是靠回查原文比對同書〈王翼〉才抓出來的。
   - **校準要寫回 spec 而不是只留在腦裡。** 六韜試點通過後把四段命中與七處判空的判法寫成「試點已判結果」一節補進 spec，b01／b03 才有共用基準；同體裁的其餘四部也直接沿用這一節。
   - **spec 怎麼寫決定要不要校準回合。** 世說新語 23 批補了兩次；神異經／洞冥記／古三墳／醫家三部共六批零校準。五條可複用的寫法：① **內部異質的書按體例切 spec，不按批次切**（古三墳五種體例各給預設答案 + 明點三處易錯分界，結果三處全中）；② **事前列出「會命中的典型」與「不會命中的典型」兩張清單**（洞冥記 9 條命中有 8 條落在事前清單內），同時給正例反例比只寫閘門更防兩個方向的錯；③ **凡要限制數量就寫死數字**（`modes` 不超過三個、`domains` 預設一到兩個），沒有數字的規訓不會生效——但**更有效的是要求寫出「拿掉第三個領域就少講了什麼」**，醫家 1,767 段只有 2 段標三領域且都寫出了理由；④ **不確定的位置寫「可考慮 ＋ 判不出來就判空並在 reason 說明」，不要寫死「這裡多半判空」**——傷寒論〈伤寒例第三〉就是這樣浮出來的，它實得 9 段命中、超過 spec 點名為密度最高處的張仲景原序；⑤ **章名誘因與體例套語要指名道姓**（「不要因為章名有『婦人』就反射填 III／IV」「每難開頭的『……何謂也？』給 `observation` 不給 `expression`」「來源章名簡繁混用，逐字照抄」），三條在醫家三部全部生效。
   - **同體裁成組發包時，試點只需做一次。** 醫家三部只在傷寒論 b01 發試點（它同時含校序、原序與辨脉法，是判準最該分開的兩端），乾淨分開後難經與金匱共用同一道閘門直接全書放行。
   - **分類問題一律延到標註完成之後才處理。** 古三墳的 `category` 從一開始就是錯的（志怪→實為易類訓誥），但先改分類會影響判讀立場。順序是：標完 → 回報 → 改 `scripts/catalog/chinese-classics-ws.json` 與 `meta.json` 兩處。
   - 手工標註的舊五步（`tmp_ann_<slug>.py` 回填）仍記在 [`MAP.md` §標註工作流](./MAP.md#標註工作流標一部書就照這五步走)，只在不發包時用。
4. phase 2 小學：**4 部已於 2026-08-09 入庫並全部標完**（說文解字 530 章／6,070 段、釋名 27 章／945 段、方言 13 章／385 段、急就篇 4 章／282 段）。三件事寫進了 catalog，別再查一次：
   - `guangyun` **已 `excluded`**：zh.wikisource 只有「廣韻/上平聲卷」一個殘頁，內容僅上平一東的十個小韻，6,399 bytes，全書 206 韻缺 196。不符收錄判準第二條「有乾淨全文」。**條目留在 catalog 裡不刪，`excluded_reason` 寫明理由**——刪掉的話下一個人只會再抓一次同一份殘本，再查一次為什麼只有 6 KB。下載器 `main()` 已會過濾 `excluded`
   - `shuowen-jiezi` **落地 530／540 部（缺 11 部）**：卷一上的一部、丄部、示部、三部、王部、玉部、𤤴部、气部、士部、丨部、屮部用 `<table class="wikitable">`（小篆圖檔）排版，艸部以後才改用 `<dd>`，抽取器只讀後者。症狀是 14 個連續標題底下本文全空。**判斷不修**：它是 `text_role: reference`，缺口 2%，寫進 `coverage_note` 即可。`expected_chapter_count` 已由 15 改成 540
   - `shiming` `expected_chapter_count` 由 8 改成 **27**：27 篇分繫 8 卷，Wikisource 以篇為子頁，落地章數是篇數不是卷數
5. **兵家五部已於 2026-08-10 全數標完（299 段）**：`liutao` 60、`sima-fa` 67、`san-lue` 65、`wuzi` 43、`weiliaozi` 84。六韜先跑 b02 試點驗證新閘門，後四部依「同體裁成組發包時試點只需做一次」共用六韜的校準基準單批直發，一次到位無退回。
   - **第五道閘門（兵書）已在五部 299 段上成立，並於 2026-08-10 寫進 `CLAUDE.md` §2.5**：「這一段除了『怎麼打／用什麼打／怎麼傳令／怎麼看出勝負』之外，還說了關於人的什麼？」六韜試點六條事先寫死的驗收條件全過，四部逐一回查原文核對事前點名的錨點全數判對。配套四點（已在 §2.5）：① 占候術數不進 XII（**五部 XII 全數掛零**，含〈五音〉律管候敵、〈兵徵〉望氣、〈定爵〉龜勝、〈仁本〉告皇天上帝、〈治兵〉青龍白虎、中略〈禁巫祝〉、〈天官〉刑德）；② 但**否定占候的段落要命中 XI**（尉繚〈天官〉「黃帝者，人事而已矣」）——做占候 vs 否定占候是兩件事，不可判反；③ 器械規格／編制表／通訊協定／軍法條文一律判空＋`formalization`，但**同段末尾轉向治國生計就照末尾判**（六韜〈農器〉、司馬法〈嚴位〉[2]）；④ 「按人想要什麼分類」命中、「按人能做到什麼分類」判空（六韜〈練士〉vs〈王翼〉，吳子〈圖國〉[8] 已在第二部書上重現）。
   - **五部之間的差異遠大於預期，「兵書」不是一個判讀單位**：判空率從三略 15% 到司馬法 64%；領域覆蓋從吳子、司馬法 4／13 到尉繚子 8／13。三略幾乎不談戰術（下略 19／19 全中），司馬法論的是戰爭正當性邊界（VIII 掛零），尉繚子〈將理〉整篇論刑訊冤獄（貢獻兵家唯一的 IX），吳子敘事比重最高（narrative 5 段）。**六韜「一部書內部裂成兩半」的結論在尉繚子重現**（前半 76% vs 後半各「令」17%）。
   - **成組發包時最該防的是錨定效應，這次防住了**：三略的 spec 開頭明文寫「不要把六韜 63% 的判空比例搬過來當預期值，體裁差異不是判準差異」，結果判出 15%。**同組後續發包一律要在 spec 裡寫這一句。**
   - **六韜的兩段人工改判已寫進四部 spec 並生效**：〈武車士〉〈武騎士〉原判 VIII 改 `[]`，分界線「按人想要什麼 vs 按人能做到什麼」在吳子〈圖國〉[8] 上被正確套用（判 II+VIII）。
   - **三部巨型（`qunshu-zhiyao` 1.6M、`zhanguoce` 1.38M、`shuijingzhu` 1.13M）單獨規劃**，不要混進成組發包。

6. **法家三小部已於 2026-08-10 標完（204 段）**：`shenzi` 75、`shangjunshu` 125、`jian-zhu-ke-shu` 4。慎子 b01（14 段）先跑試點驗證新閘門，七條事先寫死的驗收條件全過後，其餘五批共用同一套校準基準直發，一次到位無退回。
   - **第六道閘門（法家）已在三部 204 段上成立，並於 2026-08-10 寫進 `CLAUDE.md` §2.6**：「這一段除了『該立什麼法／怎麼賞罰／官怎麼設、令怎麼下／名分怎麼定』之外，還說了關於人的什麼？」判準是**文本有沒有自己把「人是什麼樣的」當成依據說出來**。配套四點（已在 §2.6）：① 政策鏈套語是最大硬撈風險，要指名道姓（商君書〈墾令〉20 段同一模子，事前點名後只 3 段命中）；② 制度條文判空＋`formalization`（〈境內〉軍功爵制 0／5、〈定分〉[2][3] 符券禁室程序、慎子逸文[4][16][36]）；③ **罵德目仍是對品格的明確主張不可判空**（〈靳令〉六蝨、〈說民〉[1]、〈畫策〉[7]，這條 rider 一寫 VII 從個位數升到 20 段）；④ 主體判空的段落若有一句說出人的樣態就照那一句判（慎子〈威德〉[3]「百工之子不學而能者，非生巧也，言有常事也」→ VIII＋XI，試點就用它驗閘門切得準不準）。
   - **這一格與 §2.5 兵書恰好相反，spec 要寫死對照。**兵書拿仁義當致勝條件時判空（工具不是主張），法家把人性當統治依據時要命中（它說了人是什麼樣的）。**工具性動機不能拿來當判空理由**——慎子逸文[26]「匠人成棺不憎人死，利之所在忘其醜也」是樣板。
   - **判空率的落差來自文本形態與文體，不是判準鬆緊，本組連續出現兩次**：慎子七章正文 13／14 命中（7% 判空）vs 逸文 34／60（43% 判空，逸文是類書輯出的斷片，最短四字）；商君書則是同一部書裡論說章（〈農戰〉8／8、〈開塞〉5／5、〈弱民〉3／3、〈慎法〉3／3）與條文章（〈境內〉0／5、〈戰法〉0／2、〈立本〉0／1、〈墾令〉3／20）各佔一半。**報總判空率之前先逐章拆開，否則數字不可讀。**
   - **本組第一次拿到跨書與跨批的獨立一致性證據，之後成組發包都該這樣埋錨**：商君書〈定分〉[4] 與慎子逸文[21] 是同一個「一兔走，百人逐之」論證，由互相看不到的兩次發包分別判讀，**都命中 V**；商君書〈算地〉[1] 與〈徠民〉[1] 是完全相同的土地比例表、被切在不同批次，**都判 `[]`＋`formalization` 且 reason 措辭同型**。
   - **XI 是法家獨有的密度**：商君書 17 段（三世說當論證骨幹），兵家五部合計只有個位數。**〈畫策〉[3]「勢不能為姦，雖跖可信也；勢得為姦，雖伯夷可疑也」判 VII＋XI**，是法家人性論核心命題。**XII 三部全零**，與兵家五部同。
   - **踩到一次章名坑，已修**：慎子〈知忠〉〈君臣〉兩章的來源標題自帶校勘夾注（`知忠〈此篇原刻全脫，依《治要》補。〉`），codex 合理地把夾注剝掉，但 MANIFEST 以完整字串為 key，`check-delegation-out.py` 報 2 缺 2 多。修法是機械改 out JSON 的 chapter 欄 ＋ **把「章名要連同標題裡的校勘夾注逐字照抄」寫進 spec 硬規則並把完整章名列出來**。`raw/original.txt` 與 MANIFEST 一律不動（動了破 SHA-256）。**這是「章名誘因與體例套語要指名道姓」在標題層的新變體，之後遇到自帶夾注的章名照這樣處理。**

7. **韓非子已於 2026-08-10 標完（781 段，59 章，全庫最大單書）**：21 批發包 codex 分兩組串行，`b03`〈內儲說上七術〉62 段為試點（不是 b01——挑它是因為經／說結構佔全書 43%，兩端與最鋒利的陷阱都在同一批內），七項事前條件全通過後才放行其餘 20 批，零退回、零校準回合。
   - **spec 按體例組織，不按批次組織。** 本書內部七種體裁（論說篇／術治條文篇／儲說／說林／難篇／解老喻老／目錄章），一批常常橫跨兩三種，按批次寫 spec 會讓同一條規則在 21 個地方各講一次。**內部異質的書一律按體例切 spec。**
   - **§2.6 在 781 段上沒有鬆動，但術治條文篇這一格量到了灰區。**〈揚榷〉／〈揚權〉是同一篇的繁簡重出、被切在 b14／b15 兩個互相看不到的 API call：**二元閘門 10 段中 7 段一致，domain 集合 10／10 都有差**。分歧全落在同一條軸——形名參同／控權操作算不算「說出了人的樣態」（[3] 循名任事、[5] 君臣異道、[10] 披木剪枝，榷判空、權命中）。**依 §2.6 原文，b14 較忠實**（形名參同講的是怎麼考核，不是人是什麼樣的；「私門充實則公庭壅塞」正是 ①點的政策鏈套語）。**未重發 b15**——重發只讓兩個複本靠構造對齊，不解決灰區。這筆 **7／10 是術治條文這個最難體裁的跨 call 一致率基準**，之後判準爭議先對照它。
   - **另兩組重出章反而完全對齊，證明分歧不來自切段。**〈八奸〉／〈八姦〉切段不同（4 段 vs 3 段）但判斷一致，綱目段都 `[]`＋`formalization`、論「官祿依才能則賢者樂進」那段兩本都判 V＋VIII 且理由幾乎一字不差；〈忠孝〉／〈忠考〉同為 3／4。
   - **更正一條先前寫錯的解釋。** 我一度把術治條文篇高命中率歸因於「切段粒度粗，操作程序與人性依據被綑在同一段」——**這是錯的**，〈八經〉8 段細切仍 7／8 命中。真因是韓非自己的論證習慣：他的條文篇會把人性當依據明講出來（〈八經〉[1]「人情者，有好惡」、[5]「民之性」、[7] 民重名與賞），而同章 [4] 只列參伍察驗的操作術就判空了。**對照商君書〈境內〉0／5（純軍功爵秩表，零人性斷言）：兩書 B 類的落差是作者差異，不是判準飄移。**
   - **儲說的經／說分工是全書最大陷阱，守住了。** 經段一律 `[]`＋`formalization`（術目索引「其說在 X」），說段**絕不繼承經的判斷**、逐則走 §2.2 閘門。內儲上 31／62、內儲下 42／66、外儲左上 55／67、左下 38／47、右上 24／40、右下 21／42。
   - **〈解老〉37／37 全命中且 12／13 格（只缺 III），是全庫 domain 覆蓋最廣的單章。**離開法家語言後 VII 19、VI 13、XIII 8、X 6，spec 對這一篇的預測成立。
   - **XII 只有 2 段是刻意的。**〈飾邪〉[1]「龜筴鬼神不能決勝，恃占候者最愚」**判 XI 不判 XII**（成敗從天兆移回實事），與兵家 §2.5 ②點同一條規則在法家上重現。**I 只有 2 段**：老子式的「長生久視」「始於生而終於死」全被路由到 X，兩者擇一時 X 比 I 精確。
   - **目錄章要事先點名。**〈韓非子全文〉18 段是卷目次不是正文，spec 寫死「18／18 判空＋`formalization`，reason 統一寫『卷目次，非正文』」，回收完全照辦。**這是「章名誘因要指名道姓」在整章層的變體**，之後遇到來源把目次當一章的書照這樣處理。
   - **`guanzi`（472K／24 批／668 段）已於 2026-08-11 標完並回填，紀錄見下方第 8 條。**
   - 之後的順序：~~名家與黃老小部~~（已於 2026-08-11 標完，見第 9 條）→ 儒家著述十部。
   - `chuci` 與 `shanhaijing` 排進批次時要記得它們同時掛著 `category` 修正待辦（見已知風險），順序仍是標完才改。
8. **管子已標完並回填（2026-08-11）——668 段、13／13 全覆蓋、判空 159 段（24%）。本庫第一部由兩個判讀者分段完成的書。** `translations/guanzi/`（`annotations.json` ＋ `meta.json.psych_survey`）已落地，`verify.py` 0 errors／15 warnings，`build-index.py` 已重生。逐體例數字與四個結構預測的最終驗證全部寫進 `psych_survey.verdict`，此處只留可重用的方法結果。

   - **判讀者換人，逐段記在 `tagged_by`，這是本條最該被下一部書照抄的做法。** b01–b23（651 段）由 codex gpt-5.6-sol 判；b24（17 段：〈第84篇輕重戊〉7 ＋〈第85篇輕重己〉10）撞上 codex 7D 配額耗盡（97% → 100%，reset 2026-08-16 04:30），改由 claude-sonnet-4-6 依同一份 SPEC 補判。回填用 `apply-delegation.py --tagged-by codex-gpt-5.6-sol --tagged-by-batch b24=claude-sonnet-4-6`（`--tagged-by-batch` 為此新增），落盤後實測 651／17 分佈正確。**把兩個判讀者記在同一個 `tagged_by` 底下等於偽造校準資料**——往後任何跨視窗、跨模型接力的書一律走這個旗標。
   - **兩個判讀者在 E 類輕重上的判空率：codex 111 段判空 58（52%）vs claude 17 段判空 12（71%），差 19 個百分點——但這不能拿來比模型。** N=17 單段翻轉就動 6 個百分點；codex 自己在 E 類的章級判空率就從〈國准〉0% 橫跨到〈海王〉〈巨乘馬〉〈乘馬數〉的 100%，71% 完全落在區間內；且 b24 兩章恰是 E 類最極端的兩種形態（貿易戰謀略長敘事＋時令祀典）。**這筆數字的用途是登記「本書有兩個判讀者」，不是模型比較。**
   - **§2.6 ④ 的強制逐句掃描全程執行，零漏判，並拿到它有效的正面證據。** b20–b24 共 120 段裡 63 段判空逐段回讀原文（關鍵詞：知禮節／知榮辱／民之情／人之情／凡人／人之性／民之性／好利／惡勞／所欲／所惡／所願／所重／貪／廉恥／不肖，另加「某類人會如何」的分型句掃描），沒有一段藏著「物質條件 → 人的品行／知覺／好惡」型的斷言句。真正的證據在命中側：**〈輕重甲〉[14] 含「倉廩實則知禮節，衣食足則知榮辱」，b22 判 V＋VII，與〈牧民〉[1]（b01）完全相同**——同一句在本書出現三次（〈牧民〉[1]／〈事語〉[2]／〈輕重甲〉[14]），前兩次一中一漏，**寫進 SPEC 當強制動作之後第三次由一個看不到前兩批的 API call 判對了**。**rider 寫進 spec 就會生效，這條在管子上再次成立。**
   - **跨批錨點〈第79篇國准〉（b21）／〈第79篇國準〉（b22）：二元閘門 2／2，domain 集合 1／2。** 兩批切段不同（2 段 vs 3 段），可對齊的是 b21[1]↔b22[1] 與 b21[2] 後半↔b22[3]。分歧只落在「以人御人，逃戈刃，高仁義」該讀成領導者才德（VII）還是用人（VIII），V 兩側都有。b22 多出的 [2] 在 b21 沒有獨立判讀單位——**硬用三段切法做覆蓋映射會得到 2／3，但那第三格純由切段粒度造成，不是閘門分歧**。**未重發包抹平**（SCHEMA §5），兩個複本原樣保留在 `out/b21.json`／`out/b22.json`。與韓非子〈揚榷〉／〈揚權〉的 7／10 要一起讀：**跨 call 一致率是「體裁 × 灰區密度」的函數，不是判讀者的固定素質。**
   - **三個灰區已於 2026-08-11 一併裁定維持判空，分界是「分型句依不依賴政策槓桿」。**〈揆度〉[1]「山林之人刺其猛獸」、〈輕重甲〉[8]「能游者賜千金」都掛在政策槓桿後面（誘因反應強度，不是人的樣態）；〈輕重甲〉[7]「籍於萬民……是隱情也」與〈海王〉[1] 同一並列結構，改判會製造書內不一致。**這是它與 §2.5 六韜〈練士〉那一格的分界**，已寫進 `CLAUDE.md` §2.6。**b24 補判時出現第四個同型灰區並依前例判空**：〈輕重戊〉[4][5]「金幣者，人之所重也」——「重」在輕重諸篇是價格術語（同「術數語彙在兵書裡是技術術語」），且 codex 對結構相同的〈輕重甲〉[18] 已判空。
   - **兩個檢查點的結論（本書最重要的兩筆方法結果）。** 檢查點一（b17〈形勢解〉93 段，C 類解篇）：判空 8 段（9%）、11／13，**解篇既沒被當成經的附屬整批判空，也沒因逐句展開而整批命中**；確立「疏釋段依疏釋文字自己說了什麼判，不依它所引的那句經文」，最硬的證據是〈立政〉9 段判空 4 而〈立政九敗解〉9 段判空 0，**經解兩側判空方向相反，繼承若發生不可能出現這個形狀**。檢查點二（b19〈輕重〉，E 類）：**結論與事前預期相反**，擔心的政策鏈硬撈一次都沒發生，**漏的是反方向**——〈事語〉[2] 的「倉廩實則知禮節」被同段的數術密度蓋掉，已人工改判（`V`＋`VII`、modes 補 `proposition`）而非重發包（重發會把同批另 44 段一起重骰、成本約 0.75% 配額，而錯的只有一段且正解明確）。
   - **串行發包＋逐批 commit 讓撞牆只損失進行中那一批。** b24 開跑時 codex 立即回 `ERROR: You've hit your usage limit`，沒有產出檔案也沒有污染既存輸出（`run-delegation.sh` 的 clobber 偵測器全程零 `!!!`）。**當時刻意沒有半部回填**——半部書會讓 `verify.py` 與索引處於半殘狀態，比不回填更難收拾。這個取捨事後看是對的：換判讀者補完最後一批後，回填一次做完。
   - **G 類特殊章節的判空全部落在事前點名處**：〈地員〉27／27（土壤分類誌）、注釋本 2／2（尹知章《管子注》與章炳麟《管子餘義》的版本附記）、〈七法〉三個含斜線子節逐字照抄且各自判讀不繼承主章。
   - 管子標完後的順序不變：名家與黃老小部 → 儒家著述十部。

9. **名家與黃老小部四部已標完並回填（2026-08-11）——公孫龍子 36／尹文子 80／鄧析子 38／于子 28，共 182 段。** 四份 `translations/*/`（`annotations.json` ＋ `meta.json.psych_survey`）已落地，`verify.py` 0 errors／16 warnings，`build-index.py` 已重生。逐書數字寫在各自 `psych_survey.verdict`，此處只留可重用的方法結果。

    - **中性物件替換測試的鏡像陷阱：spec 事先寫死哪一格，那一格就判對；沒寫的那格會出事。**公孫龍子 II（含名與實）＝0 是本批最硬的正面結果——整本書的主題就是名實，spec 事前寫死〈白馬論〉正文與〈指物論〉判空（語詞如何指稱，換成任兩個中性符號論證照樣成立），回收 II 掛零。**同一份 spec 漏寫 IX，〈堅白論〉10 段就被撈了 5 段**：「目不能堅，手不能白」的目與手是偵測通道不是身體處境（換測溫計與磅秤論證不變），而當時領域表的 IX 註解只寫「身體限制」，判者照字面命中。補寫 IX 那一格後重發，**IX 5→0 且其餘五列位元組完全相同**——精準改一格，證明這是規格缺漏不是判讀噪音。已寫進 `CLAUDE.md` §2.1。**往後發包前先把該書最可能被詞面聯想撈到的兩三格逐一套替換測試，命中與判空兩面都寫進 spec。**
    - **同一條 II 判準在兩本書上判出 0 與 5，這才是它有區辨力的證據。**尹文子 II 5 段全是名聲 vs 實情的實質命中（人被名所累、名實不副），不是詞彙聯想。若公孫龍子的 0 是判準過緊，尹文子不會出 5；若尹文子的 5 是判準過鬆，公孫龍子不會是 0。
    - **注本不是均質的一層文本，統計要分注家算。**公孫龍子 17 個命中裡 16 個引注、**10 個純由謝希深注成立**，逢注幾乎不出斷言。**作為「這個本子」的紀錄為真，作為「公孫龍思想」為假**——處置是 reason 一律指名正文或注文並引該注句，`psych_survey.verdict` 寫明命中來自哪一注家。注文算數這件事沿用世說新語（1,132 段、劉孝標注）的既有先例，不是本批新開的口子。于子的 篇題注 5／14 vs 正文 11／14 是同一件事的另一種形狀。
    - **判讀者自己長出一條 spec 沒寫的分界並三次不動搖**：注文的比擬轉寫（只是把正文換句話說）判空、注文的主張（注家自己下的斷言）才命中。這條值得在下一部有注本的書寫進 spec 當明文。
    - **鄧析子 `narrative`＝0 是事前預測並驗證的負面結果**；黃老三部 146 段只出 1 個 XII，與兵家五部 XII 全數掛零同向——**談道談天不等於 XII**。
    - 四部標完後的順序：**儒家著述十部**，再來是三巨頭（`qunshu-zhiyao` 541,725 字、`zhanguoce` 466,710 字、`shuijingzhu` 379,469 字）。

10. 章標籤只有序號的 12 個警告，**已查明全屬「來源就沒有篇名」，重抓修不掉**（2026-08-09 逐部 `--survey` 覆查，見上方章標籤一節）。只能在標到那部書時人工補篇名對照表。優先度低：序號錨點仍然唯一且穩定，不影響標註正確性

11. **儒家著述組本輪 5 部 506 段全數標完（2026-08-15）：素書 6／忠經 18／新序 218／孔叢子 159／申鑒 105。** 五部的 `annotations.json` ＋ `meta.json.psych_survey` 均已落地，`verify.py` 0 errors，`build-index.py` 已重生，commit `c8cf14b`（素書＋忠經）、`eb9fffe`（新序）、`26fc75d`（孔叢子）、`6f52913`／`976b335`／`0303e1e`／`704e800`（申鑒逐批＋收尾）均已 push。

    - **§2.5 ②「否定占候要命中 XI」現在有四部書的證據，可以當成跨體裁通則寫。**尉繚〈天官〉、韓非〈飾邪〉之外，本輪再加素書〈安禮章第六〉「有道則吉，無道則兇……非其神圣，自然所鐘」（判 XI，全書 XII 掛零）與新序〈雜事/卷二〉[15] 晏子止禳彗星。**最有力的一筆是新序〈雜事/卷亖〉[27] 的同型段落——它被刻意排除在 spec 之外當盲測錨點，由一個看不到 b07 的 API call 獨立判出 XI。**
    - **新序的判空全部集中在善謀兩卷，這是事前寫死的第 9 條驗收條件。**218 段判空 25（11%），〈善謀/卷上〉62%、〈善謀/卷下〉50%，而〈刺奢〉〈節士〉〈義勇〉三章 0%。善謀是縱橫策謀敘事（誰用什麼計說動誰），過 §2.2 閘門後刪掉只少一件史事；其餘三章刪掉會少掉對人該怎麼活的判斷。**同一部書內判空率的四倍落差再次來自體例不是判準**（同慎子正文 vs 逸文、商君書論說章 vs 條文章）。
    - **X（死亡）的鏡像陷阱守住了，這是本輪最需要事前寫死的一格。**《新序》〈節士〉〈義勇〉兩章共 44 段幾乎段段有人死，spec 事前寫死「死在本書預設是節與義的展示手段，兩章的 X 應該是 0」，回收全書 X 只有 4 段且兩章確實掛零。**若不寫死這一格，光靠詞面聯想會把 44 段全撈成 X。**
    - **跨批盲測一致性：〈雜事/卷一〉[2] 與〈雜事/卷五〉[10] 是同一則沈猶氏材料的兩種敘述框架，被切在 b05／b08 兩個互相看不到的 call，都判 V＋VII。**加上前述〈雜事/卷亖〉[27]，本書拿到兩筆盲錨。做法沿用管子〈國准〉／韓非〈揚榷〉，**成組發包一律這樣埋錨**。
    - **忠經證否了「仿孝經體例＝滿是家庭倫理」的事前假設**：18 段裡 IV 只有 1 段（〈保孝行章第十〉），主軸是 V 15＋VII 13。**體例相似不能推定內容相似，守則 1 在體例層一樣成立。**
    - **試點結果零校準時，正確的動作是把試點寫成基準而不是改判準。**新序 b05（37 段）48 個事前錨點全對，於是我在 SPEC 補了一節「試點校準紀錄」記下它驗證了哪四格，並附 rider **「b05 的判空率不可搬去當別章的預期值」**——同 §2.5 三略那條。
    - **spec 的錯誤錨點是無害的，但要記下來。**新序結構調查回報的必中錨點裡有一條「孔子過泰山『苛政猛於虎』」，`grep` 全書 0 命中（那是禮記檀弓不是新序）。因為它是**必中**條件所以只是空轉、不影響任何判讀；已寫進 `eb9fffe` 的 commit message。**調查型 agent 的錨點清單一律先 grep 驗證再寫進 spec。**
    - **「否定占候命中 XI」再添兩部，累計六部書。**孔叢子 `16`[9] 中山天雨穀「天雖至神，自古及今，未聞下穀與人也」把因果推回「教民種嘉穀」判 XI；`19`[4]「其禍福吉凶大者在天，小者由人」是承認天有份的灰區，事前裁定判 XI 不判 XII，回收成立。申鑒〈俗嫌〉破除術數六段全落 XI。**這一格現在可以當跨體裁通則寫進 §2.5 ②。**
    - **孔叢子的 XII 兩個方向都在同一部書裡，只寫一面會全漏。**接受側 `02`[7]「人事之應乎天」、`02`[16]「樂和則天地猶且應之」判 XII；否定側 `14`[4] 華山長生、`14`[7] 道士學不死、`16`[9] 天雨穀、`16`[5] 獻雀、`05`[8] 獲麟、`19`[4] 恃天六段一律不得填 XII。**兩側都寫死，回收 XII 恰 2 段、零違反。**申鑒同型（`3`[4] 設問方認證感應判 XII，`3`[5] 荀悅答語判 XI，**設問與答覆立場相反時各判各的**）。
    - **孔叢子由我本人通讀 159 段寫 spec，不用調查 agent，且錨點引文回寫前逐段 grep 驗證**（承新序那條教訓），抓到兩處引文瑕疵：`18`[1] 把改寫拼貼當逐字引文、「太師」實作「大師」。**驗證器本身也要對——第一版把 markdown `**` 與多 ref 行的歸屬算錯，報 18 個假 FAIL。**
    - **申鑒把「兩個相反方向同書」推到最強的一次驗證。**XII 8 段全部是文本自己認證「誠／德 → 神明應」（`1`[7] 動天地應神明、`2`[3] 致精誠則神明應、`2`[15] 天人之應、`2`[21] 違天不祥、`3`[4] 合禮祈請通神、`4`[6] 鼎雉之異興殷之符、`4`[16] 善則祥、`5`[1] 心誠則神明應之），同一部書破除術數的六段全落 XI 且無一含 XII。**判者也沒把 XII 當詞面反射**：含天神詞面的 `2`[1]（提案目次）判空、`2`[22]（論名實榮辱）判 II＋VII。荀悅的立場本來就是否定術數占候而肯定天人感應，兩側並存不是判讀噪音。
    - **申鑒 12／13 是本庫覆蓋最廣的一部**（只 III 掛零），且四篇序跋 5 段中 4 段命中——理由都不是敘生平而是序者自己下的品格斷語（「清虛沈靜，未嘗效一言於操」「其亦善處濁世者矣」），**序跋照 §2.2 刪除閘門判就對，不必另立一道**。IX 只 2 段且都在〈俗嫌〉，導引蓄氣、關息、九鍼炙炳那些醫理術語沒被反射填成 IX 或 XII，與 §2.4 那一格一致。
    - **本輪最大的預測誤差是 mode 與覆蓋率的數量帶寬，不是判準，而且連錯兩部書。**孔叢子 `ritual` 我連錯兩次（4–9 → 8–16 → 實得 17），零段 domains 下限寫 ≥4 而實得 2（覆蓋 11／13，同新序）；**申鑒三條帶寬全錯**——XII 猜 2–5 實得 8、零段 domains 猜 ≥4 實得 1、零段 modes 猜 ≥3 實得 2。**五條都逐段查證過是真的（薄領域 I 1 段、IX 2 段、XIII 5 段抽查皆為真斷言，判者甚至自寫「三格不可再減」的取捨理由），處置是改條件並記錄誤差，不是鬆判準。**相對地，同兩部書的體裁陷阱類條件（孔叢子 14 條命中＋4 條判空錨點、申鑒 11 段必判空＋35 段必命中＋〈俗嫌〉XII 至多 1 段且只能是 `3`[4]＋〈俗嫌〉XI ≥5＋〈時事〉判空率須高於〈雜言下〉）**零校準全過**。教訓已可定案：**體裁陷阱（哪一格會被撈走）可以事前寫死並且會生效；數量帶寬（會有幾段）事前寫不準，只能當提示不能當硬條件。**
    - **`run-delegation.sh` 的 stdin 繼承 bug（已修）**：`codex exec` 未關 stdin，用 `git commit … && bash run-delegation.sh` 串接呼叫時會繼承永不關閉的 stdin，卡在「Reading additional input from stdin...」——申鑒 b01 因此空轉 53 分鐘、零 CPU、連 session 都沒建。診斷靠 `~/.codex/sessions/` 有沒有對應 rollout 檔＋ `Get-Process` 的 CPU 累計。已補 `< /dev/null`。**單獨呼叫不會觸發，所以孔叢子五批全程正常——這種 bug 只在特定呼叫形態下現形。**
    - **配額實測 0.67%／批**（codex 7D 76% → 78%／3 批）。本輪 5 部共 23 批已全數跑完。
    - **下一步（本輪已收尾，可直接接手）**：~~`fengsu-tongyi`~~、~~`duduan`~~、~~`cai-zhonglang-ji`~~、~~`taixuanjing`~~、~~`jingshi-yizhuan`~~ 皆已標完（見第 14／15／16 條）；`zhonglun` 卡在上方「一章＝一段」上游缺陷，未解不排批次。**三巨頭已開工，見第 17 條。**

17. **三巨頭事前勘查（2026-08-22，開工中）。** 三部都已切批、都寫了通讀指引。三部形態彼此完全不同，**不可互相推定**。

    **當前進度（2026-08-24 收工）**

    | 書 | 切批 | READING-GATE | 通讀 | SPEC | 驗收器 | 標註 |
    |---|---|---|---|---|---|---|
    | `zhanguoce` | ✅ 65 批 | ✅ | ✅ g01–g05 全（611 列全查、歸屬錯 0） | ✅ | ✅ 現場解析、七種變異驗過 | ✅ **全書 65 批 1,034 段已入庫**（見下） |
    | `shuijingzhu` | ✅ 65 批 | ✅ | ✅ g01–g05 全（469 列 488 引句，修正 3 處後 0 錯） | ✅ 137 錨點列／131 段，五表零手抄 | ✅ 現場解析、三層反向驗證過 | ✅ **全書 65 批 933 段已入庫**（2026-08-24，見下） |
    | `qunshu-zhiyao` | ❌ **2026-08-24 整部移出本庫**，83 批與 raw／meta 已刪（見下方移除紀錄） | — | — | — | — | — |

    **配額排序決定（2026-08-22 立，2026-08-23 執行完畢並實測）**：原判斷是
    「通讀報告是中間產物，標註才是交付物」，所以本週期剩餘配額全給戰國策試點＋全書標註，
    群書治要通讀延到下個週期。**執行結果證明這個排序是對的**——戰國策全書落地，
    而通讀若先跑會把配額吃光、一段標註都拿不到。

    **實測配額成本（每批遞增，不是常數）**：codex 7D 55% → 69%（第一梯 24 批，14pp）
    → 86%（第二梯 24 批，**17pp**）→ 98%（第三梯 14 批，12pp）。
    **同樣批數第二梯比第一梯貴 3pp，所以「已跑 N 批花了 X%」不能線性外推剩餘批數。**
    做法是**分梯發包、每梯之間重新量一次**，不要一次把 38 批全丟出去。
    第二梯結束時 86%，我判斷把剩下的花完而不是留存——配額到期作廢、且每批都即時寫盤，
    撞牆最多只損失進行中那一批。

    **水經注試點已跑完（2026-08-24），校準成立、無需調 SPEC。**用窗尾殘餘 2% 發了
    兩批：`b09`（10 段，錨點 `05`[2]／`05`[8]／`05`[10]）與 `b08`（6 段，錨點 `04`[10] XI），
    **兩批 A 類 FAIL 0／WARN 0**，合計命中 7／16、判空 9，13 領域已落 8 格。
    經文段機械判空與注文承重層兩道閘門在真實回收上都成立。**剩 63 批可直接放量，
    不必再跑試點。**

    **窗尾單價實數（本庫第一次量到）**：b09 4,273 字元＝1pp、b08 3,773 字元＝1pp，
    **約 4,000 字元/pp**；對照戰國策整窗平均約 11,000 字元/pp，**窗尾貴 2.6 倍**。
    這替第 17 條「每批遞增、不可線性外推」補上了兩端的實數。**推剩餘 63 批
    （約 364,000 字元）：按整窗均價約 33pp、按窗尾單價約 85pp，實際落在中間且窗初較便宜，
    保守估 35–50pp——即水經注全書可在單一 7D 窗內跑完並留有餘裕。**（此為推估，
    照本庫紀律不當硬預期值，分梯發包時每梯重量一次。）

    **codex 7D 已於 2026-08-24 重置（快照 0%）**，前一週期 98% 的殘餘已按上述用盡。
    原訂四步的 ①水經注 SPEC 與 ②驗收器＋反向驗證**已於 2026-08-24 完成**（commit `907f2cb`），
    ③**水經注標註 65 批已於 2026-08-24 同日跑完並入庫**（見下）。
    **原訂的 ④群書治要 7 群通讀已取消**——2026-08-24 使用者裁定整部不收（見下方移除紀錄）。
    **三巨頭實得兩部，巨型書階段就此結束。**

    **水經注全書實測配額（2026-08-24，一日內三梯跑完）**：7D 0% →（第一梯 24 批，
    b01–b07＋b10–b26）16% →（第二梯 24 批，b27–b50）34% →（第三梯 15 批，b51–b65）45%。
    **三梯單價 0.67／0.75／0.73 pp/批，本窗 63 批共 45pp**（b08／b09 兩批試點在前一窗，
    不計入），落在事前保守估的 35–50pp 區間內，且**沒有出現戰國策那種
    「第二梯比第一梯貴 3pp」的單調遞增**——第三梯反而比第二梯略便宜。
    合併戰國策與水經注兩部的數據，可修正第 17 條的說法：**遞增的是「同一個 7D 窗內
    越接近上限越貴」，不是「批數越多越貴」**；45% 這個位置還在便宜區，
    戰國策那三梯（55%→69%→86%→98%）整段落在窗尾才會看到遞增。
    **推論：一個 7D 窗大約能吃兩部巨型書，前提是別跑到 85% 以上。**

    ~~群書治要 561,212 字元／83 批，按字元均分成 7 群~~ —— **批次與分群已隨整部移除刪除（2026-08-24）**，
    要看原始分法翻 git 歷史。下方的通讀方法論（串行發包、兩層對拍、`check-reading-attribution.py`）
    **與書無關，是三巨頭共用的通用流程，往後任何長書照用。**

    **通讀一律串行發 codex**（一次一群），每群產出 `delegation/<slug>/reading/gNN.md`；
    發包腳本 `scripts/run-reading.sh <slug> <gNN> b.. b..`（三巨頭共用，輸出已存在就跳過）。
    **回收後不採信 codex 自檢，一律自己回批次檔對拍一次**，而且要對拍兩層：
    ①章數／段數／經文注文比／超長段清單；②**每一條引句要對它被掛的那一個 `(章名, 段序)` 比對，
    不是對整群語料比對**——蔡中郎集那次假 FAIL 的引句「在語料裡確實存在」，只是存在於**別章**。

    **第②層已做成可重跑的腳本 `scripts/check-reading-attribution.py`（commit `0cb9db2`），
    往後每群回收都跑一次，不要再手寫一次性檢查。**用法
    `PYTHONIOENCODING=utf-8 python scripts/check-reading-attribution.py <slug> [gNN ...]`。
    它把不合的引句三分流：**掛錯段**（存在但在別段）／**異體字**（本段內、`爲`／`為` 之類）／**查無**。
    兩件事是寫這支腳本才發現的：

    - **表格格式漂移是常態，解析器一律寫寬——同一天踩到兩次，兩次都是「零列＝假合格」。**
      ①章名欄在各群分別寫成 `「X」`／`` `X` ``／裸 `X`，用 `^「X」$` 硬比對時
      戰國策 g04（裸）與 g05（backtick）**整份被靜默跳過、報 0 列 0 錯**；
      剝殼後兩份各 138 列全數解析，戰國策五群合計 **611 列 611 引句、歸屬錯 0**。
      ②水經注 g04 把經注別併進段序欄寫成 `[2]（注）`，被 `^\[\d+\]$` 拒掉，
      **§3／§4／§5／§6 四整節靜默跳過**，初跑只認出 53 列；放寬後 108 列 109 引句、0 錯。
      **檢查器報「0 錯」時要先看它認出幾列**，列數與該群篇幅不相稱就是解析器沒咬到。
    - **蔡中郎集的失敗模式在水經注 g03 重演了**，證明它不是單書偶發。
      codex 自檢宣告「249 個引號字串對拍失敗 0」，但它查的是「引句在語料裡存在」——
      腳本按 `(章名, 段序)` 查，抓到「有老子廟。」掛 `17`[7] 實在 `17`[6]、
      「在官地爲官蝦蟇」掛 `19`[4] 實在 `穀水`[5]（跨章）。已改，改後 0 錯。
      **結論：codex 的自檢欄一律不採信，因為它做的是弱檢查。**

    水經注 g01–g05 共 469 列 488 引句，修正 3 處（皆在 g03）後歸屬錯 0。
    g04（b40–b52）結構自行對拍全符：45 章（去重前 47）、253 段、經 75／注 178、
    逐批 13 組經注比與章數零誤差、超長段 2 段（`淄水`[3] 3,580／`渠沙水`[2] 3,041）。
    g05（b53–b65）同樣全符：39 章（去重前 41）、266 段、經 111／注 155、
    §1 逐章 41 列的章名／段數／經注比零誤差、超長段 2 段（`溫水`[6] 6,460／`漸江水`[2] 7,241）、
    88 表列 93 引句歸屬錯 0，跨批續章兩組（`33` b54→b55、`漸江水` b63→b64）段序未重編。

    - **`zhanguoce` 戰國策（雜史）：65 批 511 章 1034 段，474,061 字元。** 走 §2.2「刪掉這一則」閘門（已在世說新語 1,132／新序 218／列女傳 208 三部驗過），**不新立閘門**。
        - **夾注佔 61.6% 字元（8,513 條：鮑本 6,157／姚本 2,199／札記 116）。**姚本與札記是純校勘異文（「曾作『例』」「今本誤重『侯』字」），零語意；鮑本多為訓詁地理職官注，**但「鮑本彪謂／正曰」型是真的在對人下斷語**，統計要分注家算（同公孫龍子謝希深注那條）。照本庫慣例**不先剝夾注再判讀**。
        - **事前未定的最大灰區：遊說辭裡的人性斷言算哪一側。**兵書側（§2.5）說「拿仁義當致勝條件是工具不是主張」判空；法家側（§2.6）說「把人性當統治依據」命中。說客搬出「人情」「利之所在」來說服人主，兩條先例指向相反方向。**通讀指引 `delegation/zhanguoce/READING-GATE.md` 明令兩側各蒐足證據、不准先關掉一側**，裁定留給 SPEC。
        - **通讀本身也要切群**——65 批一次讀不完，切成 g01–g05 各 13 批（約 95K 字元）串行發 codex，產出 `delegation/zhanguoce/reading/gNN.md`。發包腳本 `scripts/run-reading.sh`（可重用，三巨頭共用）。
        - **SPEC 與驗收器都已完成並驗過（2026-08-22，commit `47a9c82`）。**`accept.py` 1,293 行，**錨點全部現場解析 SPEC 表格、零手抄**（對照 SPEC 裡 93 個反引號名稱，只有 13 個通用欄位名出現在程式碼裡）。反向驗證做了兩層：由 SPEC 合成一份 1,034 段的「完美輸出」跑得 **0 個 A 類 FAIL**，再注入七種故意變異各被對應條款抓到。`--check-spec` 自檢 FAIL 0（65 批 511 章 1034 段；整章判空 1 章、逐段判空 47 列／46 distinct 段／7 子表、必須命中 52 列、禁填 4 段）。
        - **反向驗證的可重用寫法在 `delegation/taixuanjing/_selftest/make_cases.py`（已入庫），戰國策這次沒照它做、是用 `/tmp` 的一次性腳本，結果驗完就沒了。**`.gitignore` 已寫死 `delegation/**/_selftest/*/`（合成輸出不入庫、由同目錄 `make_cases.py` 重生），**所以 `_selftest/` 底下那 520K 是設計如此、不是該清的殘留**。往後每部書的反向驗證一律建 `delegation/<slug>/_selftest/make_cases.py`，不要再寫 `/tmp` 腳本——這次就因為 Git Bash 的 `/tmp` 與 Windows Python 的 `C:\tmp` 不是同一個目錄，吃到一次假 FAIL。
        - **SPEC 的 99 條帶引句錨點另用 `check-reading-attribution.py` 的載入器獨立複驗一次**（不經 `accept.py`）：
          逐條按 `(章名, 段序)` 比對，**掛錯或查無 0、異體字 0**。這是「驗收器本身也要驗」的第三層——
          `--check-spec` 說 0 FAIL 是驗收器自己說的，這一次是用另一份程式碼從批次檔重算。
        - **A11 是本書新加的一條、且是可機器檢查的版本**：夾注出處規則（命中理由若只引夾注卻沒指名注家就 FAIL），把 SPEC 硬規則 8 那句沒法執行的散文變成自動判 FAIL。變異測試訊息為「A11 秦興師臨周而求九鼎[1] 命中理由引句只出現在夾注，卻未指名注家」。
        - **試點批已選定：`b01` ＋ `b26` ＋ `b63`（13 條錨點，涵蓋全部三種條件類型與 7 張判空子表中的 4 張）。**選法不是挑錨點最多的批，是挑**打中最大灰區**的批：103 條錨點散在 52／65 批，其中「德目工具化（§2.5 兵書側）」8 條分佈於 b01／b07／b08／b10／b15／b26／b53／b63／b64，而「政策鏈套語（§2.6①）」全書只有 1 條、就在 `b63`。b01 另含全書僅 4 條禁填條件中的 2 條與 `劉向書錄` 序文體。指令：`bash scripts/run-delegation.sh zhanguoce b01 b26 b63`，回收後跑 `accept.py` 與 `check-delegation-out.py --slug zhanguoce`。
        - **✅ 全書標註完成（2026-08-23）。65 批 511 章 1,034 段，判空 579 段（56.0%），13／13 領域全覆蓋。**
          `I 5／II 25／III 7／IV 44／V 250／VI 22／VII 228／VIII 101／IX 4／X 23／XI 23／XII 9／XIII 6`；
          modes `proposition 663／narrative 579／prescription 371／observation 107／expression 81／formalization 14／ritual 2／worked_instance 0`。
          `verify.py` 72 部 11,304,281 bytes、0 error 16 warning，`build-index.py` 已重生。
          **V 250 與 VII 228 是雙主軸**：說客論的是權力該怎麼用（V），評的是人可不可信（VII）。
        - **A 類 FAIL 全書只有 1 個，而且錯的是我不是判者——這是本輪最重要的一筆。**
          `莊辛謂楚襄王`[7] 被我列進「敘事套語．必須判空」，回收判 V＋XIII。查證後判者對：
          正文確實只是套語加封賞交代（判者的 reason 甚至明說「不標 VI」），
          **但同段鮑彪注寫了「襄王雖失之東隅，而收之桑榆……此策為有力焉」**，那是對納諫後果的評斷。
          **根因是我寫錨點時只讀正文沒讀夾注。**已撤列、記入 SPEC〈錨點誤差紀錄〉，
          並把通則寫進 CLAUDE.md §2「位置錨點的可靠性前提」。
          **同表其餘 9 列逐條回查夾注（b55／b59／b61 全是校勘／繫年／異文，無評斷語）＝孤例不是系統性。**
          **這條對水經注是硬需求**——戰國策 1,034 段裡 95 段的命中由注文成立，水經注 `〈〉` 佔 98.5% 字元。
        - **撤一列錨點後 `--check-spec` 立刻報 S2／S8（表格 45 distinct 段 vs 條文宣告 46）**，
          改完才回 0 FAIL。**條文裡的數字與表格是兩份真相，改一邊必漂，靠自檢器抓不靠記性。**
        - **A12 是本輪新增的機械檢查：整批引號分工寫反。**b03 曾把 25 條逐字引句全寫成 『』、「」 掛零，
          驗收器按約定會判成一整批假 FAIL；原本是我手數發現的，現在寫成
          `quote_convention_inversion()`（判準＝該批 「」 掛零，不用比例，因為合法巢狀在單批只會有一兩條）。
          **六個對抗性探針（3 個該響全響、3 個不該響全靜、跨批隔離確認）驗過，真實資料上第二三梯全程零觸發。**
        - **B 類 9 條實測：6 條成立、3 條沒中，全部逐段查證後判定是我的預測錯不是判讀錯，照舊改條件不鬆判準。**
          位置類條件（哪幾段必須判空／必須命中）零校準全過，數量帶寬類再次失準——
          **這是「位置能寫死、數量寫不準」的第 6 次確認**，已可當定論用。
        - **注本統計分注家算（CLAUDE.md §2 末條）：命中理由由注文成立的有 126 段**
          （鮑彪 89、其中 61 段單獨成立；正曰 40／補曰 27／鮑本 7，一段可涉多家故相加大於總數）。
          `psych_survey` 新增 `commentary_hits` 欄位記這件事，`verdict` 明寫
          **約 28% 的命中屬南宋鮑彪／元吳師道而非戰國策本文**——「戰國策說了什麼」與
          「戰國策注家說了什麼」是兩個不同的主張，不可合併成一個數字報。
        - **待補（非阻塞）**：反向驗證仍是 `/tmp` 一次性腳本，未照 taixuanjing 範本建
          `delegation/zhanguoce/_selftest/make_cases.py`。要補就照 `taixuanjing` 那份改。
    - **`shuijingzhu` 水經注（地理）：65 批 137 章 933 段，372,376 字元。** **這是第二部兩層文本，而且層界已經在段級分好了**：`〈〉` 佔 98.5% 字元，因為來源用 `〈〉` 標酈道元的注、未標的是《水經》本文。段落交替出現「東南過晉陽縣東，晉水從縣南東流注之。」（經文段，純水道登錄）與整塊注文段（含軼事、碑刻、傳說、地名沿革，如 `汾水`[1] 劉曜遇管涔童子獻劍）。**照太玄的做法要寫兩道閘門而不是一道**：經文段走 §2.3「登錄之外」幾乎必然全判空，注文段才是承重層。
        - **章名有 18 章是純數字**（`01` `02` `03` `04` `05` `07` `08` `13` `17` `18` `19` `21` `27` `28` `30` `33` `34` `35`）——來源沒給水名。寫 SPEC 錨點時章名仍唯一可用，但**不可寫成「第 1 章」之類的自創稱呼**，一律逐字照抄。段數最多的是 `禹貢` 56 段與 `08` 33 段。
        - **本書真正的灰區不是兩層，是引書段。**酈道元幾乎不自己說話（`《》` 出現 3,131 次）。當他引的那段話裡有一句對人的斷語、而他引它只為證明某水某地的名字時算不算命中？兩條先例方向相反：命中側是公孫龍子謝希深注／戰國策鮑彪注（注家的話是文本的一部分）加 §2.6「工具性動機不能拿來當判空理由」；判空側是風俗通義〈山澤〉23 段名義訓詁命中率 4%。**`READING-GATE.md` 明令 A／B 兩側各蒐足證據、不准先關掉一側**，裁定留給 SPEC。
        - **g01（b01–b13，13 章 128 段）已回收並自行對拍**：經文 34／注文 94，超長段 4 段，255 個引號 token 零 FAIL。三件對 SPEC 直接有用的結果：①**§2.7 四側裡最難找的破除側在第一群就出現**——`01`[7]「余考釋氏之言，未爲佳證。」是酈道元對釋氏崑崙說的正面否定；②A／B 兩側的分界被切成可裁的形狀（「引書中的人事是否因服務地名、水名、經文措辭而失去獨立承重」）；③**4 段超長段各含 7–9 個可分辨主題**，與 `domains` 上限的張力已具體化，報告直接問「按整段最承重部分，還是容許段內異質材料另作處理」——**這一條要在寫 SPEC 前先裁**。
        - **超長段是本庫沒有先例的新情形，已量過（2026-08-22）。**43 部已標書 18,101 段裡**最長的一段只有 2,641 字**（蔡中郎集〈明堂月令論〉[1]，判空），全庫只有 2 段破 2,000 字。水經注有 **13 段破 3,000 字**，最長的 `穀水`[5]（b30）**9,118 字，是全庫歷來最長段的 3.5 倍**。段長與領域數的關係也量了：判空率隨長度單調下降（<100 字 88% → 800–2000 字 15%），但**命中段的平均領域數在 2.06 就飽和**（0–100 字 1.52／100–300 字 1.79／300–800 字 2.04／800–2000 字 2.06），全庫 4 格以上只有 50 段。**也就是說「≤3」從來不是被規則擋住的，是文本沒逼到過**——不能拿既有分布當水經注的預期值。
        - **裁定方向（待 g02–g05 證據到齊後定案，但先例是一致的）**：CLAUDE.md §2.3 碑誄那條寫死「判的是這一段有沒有承重句，不是這一段有幾成是登錄」，§2.6④ 寫死「主體判空的段落若有一句說出人的樣態就照那一句判」——**兩條都是「照承重句判，不照比例判」**。套到超長段就是**不擴充領域數上限，改要求 `reason` 逐字引出承重句**（g01 §6 已照這個形狀交件）。反面風險要一併寫進 SPEC：一段 9,118 字掛 5 個領域，等於在反向索引裡給讀者一個 9,118 字的稻草堆，**領域索引的可用性本身就是不該放寬上限的理由**。
        - **✅ 已定案（2026-08-22，g01–g05 五群證據到齊）。** 五份報告的 §6 全部照同一形狀交件：`溫水`[6] 6,460 字拆 24 題／列 3 條承重句，`漸江水`[2] 7,241 字拆 37 題／列 4 條承重句——**「多主題」與「承重句可數且有限」同時成立**，這正是裁定的經驗前提。三條寫進 SPEC：
            1. **領域數不設上限也不擴充，照承重句實得。**「≤3」在本庫從來不是被規則擋住的（命中段平均領域數 2.06 就飽和），所以不存在「放寬」這個動作；水經注若某段實得 5 格，那是文本逼出來的。
            2. **一段掛 N 個領域，`reason` 就要有 N 條可指認的逐字承重句。**掛得出領域卻引不出句子＝該領域不成立，直接判 FAIL。這是把 §2.6④ 從散文變成可機器檢查的形狀（同戰國策 A11 的做法）。
            3. **超長段的引句必須自帶段內定位錨**（人名／地名／事件名，如「孟伯周」「劉寵一錢」），否則 9,118 字裡讀者找不到它。**這一條只對 >3000 字的 13 段強制**，短段不必。
          反面風險（稻草堆）由第 3 條處理，不由壓低領域數處理——**壓領域數會讓真有 5 格的段漏掉 2 格，那是資料錯誤；定位錨只是檢索體驗**，兩者不同量級，不可互相交換。
        - **引書段的 A／B 灰區同時定案（g05 §8 列了 12 組兩讀）**：照 §2.6「工具性動機不能拿來當判空理由」——**酈道元引它是為了證地名，不構成判空理由**；判的是被引的那句話本身有沒有對人下斷語。所以 `33`[6]「夷中最仁，有仁道，故字從人」取 B 讀判空（名義訓詁，同風俗通義〈山澤〉），`沅水`[2]「女聞之，以爲信不可違」取 A 讀命中（守信優先於婚配意願，是對人的斷語）。**分界句：被引句離開它所服務的那個地名還成不成話——成話則命中，只剩訓詁則判空。**這是 §2.1 中性物件替換在引書段上的版本，被替換的對象是地名。
        - **✅ SPEC 與驗收器完成（2026-08-24，commit `907f2cb`）。**`delegation/shuijingzhu/SPEC.md` ＋ `accept.py`（約 590 行，錨點全部現場解析 SPEC 表格、零手抄）。`--check-spec` FAIL 0（65 批 137 章 933 段；錨點 137 列／131 段；經文段 301；超長段 13）。
            - **閘門用 §2.3＋§2.2＋§2.7 的組合，沒有新立一道**，與獨斷、太玄同樣的作法。閘門 A（經文層 301 段）是本庫第一次用**純機械判準**寫判空錨點：整段不含 `〈` → `domains` 填 `[]`。**這一條同時解掉了莊辛[7] 那個教訓**——判空錨點只讀正文會漏夾注評斷，但經文段依定義零夾注，沒有可漏的東西，所以它零抄寫風險地覆蓋了全書 32%。唯一例外 `原序`[1] 已逐字寫進 SPEC。
            - **B 側候選寫成六類「禁止理由」而不是判空錨點**（聲訓／名義訓詁與俗稱訛變／地望校正與里數異說／碑文品題與譜系官歷／祠廟位置登錄／陵墓稱謂沿革）。原因是抽樣 18 段發現 84 個 B 側段裡有 32 段同時帶 A 側承重句——列成判空錨點會製造假 FAIL。**SPEC 一併寫死反面**：「只是不得單獨作為理由，不是出現字樣就整段判空」，例句 `沁水`[6]。
            - **五張錨點表（必須命中 100／破除 13／認證 10／過度 1／技術 13）全部由生成器產出、零手抄**，引句先由 `scripts/extract-reading-anchors.py` 對 `raw/original.txt` 驗過（434 條、掛錯 0、查無 0），`accept.py` 再回 `b*.md` 用另一條 code path 重驗。這是風俗通義 16 條對不上那次事故的常態化對策。
            - **三層反向驗證全過**：①由 SPEC 合成 933 段完美輸出 → A 類 FAIL 0／WARN 0；②注入 15 種輸出層變異，各自被對應條款抓到（A3 那次額外觸發 A11，屬共同錨點的預期重疊）；③注入 8 種 SPEC 層變異，全數被對應 S 碼抓到。
            - **A12 是本書新加的一條，且第一版寫錯了。**初稿寫「超長段引句必須帶段內定位錨（人名／地名／事件名）」——那是散文，機器查不了。改成「每一組 `「」` 去標點後在本段內只出現一次」才可執行；變異測試的實際訊息是 `FAIL A12 穀水[5] 超長段引句在段內出現 29 次，無法定位：「又東」`。**寫驗收條件時若一句話沒法變成一個布林運算式，那條就還沒寫完。**
            - **硬規則第②條與戰國策相反**：夾注符號 `〈` `〉` 本身不抄進引句，但**夾注內部的文字可以引**。水經注的注文是承重層，禁引夾注等於禁掉 67.7% 的段。
            - **✅ 反向驗證已改成可重跑腳本（2026-08-24，commit `8109c84`）**：`delegation/shuijingzhu/_selftest/make_cases.py --verify` 一次合成完美輸出＋15 個輸出層變異＋8 個 SPEC 層變異並自己對答案（perfect 0 FAIL、23 個變異全數命中）。**預期寫成完整的 FAIL 碼集合而非「至少含」**，多出一個沒宣告的碼就算不符。
            - **太玄的 `make_cases.py` 範本不能照抄**：它按「章屬於哪一批」分派 row，本書有 14 章跨批續接，同章的段會被同時寫進兩個批次檔，A1 立刻報一片重複與多出（初跑 perfect 219 個 FAIL）。改成逐段照 `batch_of` 歸批。**往後複用這份範本的書，第一件事是看它有沒有跨批續章。**
            - **A9 原本寫錯，是同一份 SPEC 內部自相矛盾（2026-08-24 修，commit `8109c84`）。**配套 (7) 寫「領域數不設上限、照承重句實得」（超長段的定案），A9 卻寫「`domains` 長度不超過 3」、硬規則 2 又補「不得標到四個」——**三處互相矛盾而 `--check-spec` 全綠**，因為自檢器只對拍數字與引句、不讀立場。若照發，9,118 字段落實得 4 格的正確輸出會被自己的驗收器判 FAIL。已改成 **「`domains` 有 N 格就要有至少 N 組 `「」` 逐字引句」**，擋湊格而不擋格數；教訓寫進 CLAUDE.md §2.1。
        - **✅ 全書標註完成（2026-08-24）。65 批 137 章 933 段，命中 278 段（29.8%）、判空 655 段（70.2%），13／13 領域全覆蓋（本庫第 8 部）。**逐書數字寫在 `psych_survey.verdict`，此處只留可重用的方法結果。**A 類 FAIL 0／WARN 0，零校準、零 SPEC 修改，試點兩批通過後即全量放行。**`verify.py` 0 errors，`build-index.py` 已重生。
            - **A9 的配對型條件在真實長段上通過了，這是「不用數量上限」的第一個實證。**`穀水`[5] 9,118 字判 **8 領域**、`漸江水`[2] 7,241 字判 **10 領域**，兩段的 `reason` 都逐格各附一條逐字承重句（穀水[5]：劉楨名實對答→II、黃帝明堂堯衢室舜告善之旌→V、王戎七歲觀虎不動→VI、楨曰荊山玄巖之石堅貞→VII、張純堰洛通漕公私穰贍→VIII、第五子陵無田宅十日不炊→IX、石經淪缺物不停故→X、陸機遇王弼→XII）。**若沿用初稿的「≤3 上限」，這兩段的正確輸出都會被判 FAIL。**全書領域數分布：1 格 130 段、2 格 82、3 格 28、4 格以上 38 段——**4 格以上 38 段已超過此前全庫 18,101 段累計的 50 段之七成**，證實「命中段平均領域數 2.06 飽和」確實是文本沒逼到過、不是規則擋住的。
            - **XII 95 段是本庫絕對數最高，比第二名（潛夫論 32 段）高兩倍，而它正好落在最易假陽性的一格。**地理書滿是祠、廟、神、靈、怪，按詞面反射能刷出數百段。抽查六段全部在 `reason` 裡明文交代越過 §2.3 閘門的理由（`汾水`[12]「不只登錄岳廟位置，而直接認證其靈驗及祭祀感應」、`04`[8]「屬文本認證的超越感應而非單純祠廟登錄」、`濁漳水`[16]「議者以爲神」是人們把不可解的援助理解為神明介入）。**判空的 655 段裡大量是祠廟山川登錄，正是這道閘門擋下來的**——`XII 95` 與 `判空 70%` 是同一道閘門的兩面，不可只引一面。
            - **機械判空錨點的效力已驗到底：經文 300 段零命中、零違反。**這是本庫第一次用純機械判準（整段不含 `〈`）取代手抄清單，覆蓋全書 32%。**它比列表更不會腐**——SPEC 改批次、改章名都不影響它，而手抄清單每改一次就有漂移風險（獨斷 84／81、戰國策 46／45 兩次都是這樣抓到的）。唯一例外 `原序`[1] 由 SPEC 條文點名，回收判 II＋VIII＋XII 正確。**這是「能寫成機械條件就不要寫成清單」的第一個完整案例。**
            - **278 個命中段有 278 段屬酈道元，不屬《水經》。**注文 632 段命中 277（43.8%）＋〈原序〉1 段，經文 300 段命中 0。使用反向索引時「水經注命中 XII」的實際意思是「酈道元認證了超越」。這比戰國策更極端（該書 1,034 段中 126 段由注文成立，占命中的 28%；本書是 100%）。**注本要分層算統計這條規則，在本書上得到最強版本。**
            - **mode 分布是體裁指紋的又一證據：`observation` 815 段（87%）壓倒性居首，`prescription` 只有 10 段。**對照戰國策（`proposition` 663 為首、`prescription` 371）——同一套 mode 值域、同一個判者、同一週內判的兩部書，分布完全不同。**mode 分布可以拿來當「判者有沒有換體裁思維」的健檢指標。**
    - **❌ `qunshu-zhiyao` 群書治要（類書，83 批 347 章 2,697 段）：2026-08-24 使用者裁定整部不收，raw／meta／83 批／catalog 條目全數刪除。** 保留本條當方法紀錄，資料翻 git 歷史。
        - **裁決依據是來源書組成，不是原本待裁的 225 段。**原文只有 `=== 序號 | 章名 ===`、**沒有來源書欄位**，來源由章名＋抽讀首段推斷：**兩庫已有全本的書佔 2,435 段（90.3%）**（含正史 862 段，本庫定義本來就不收；含 religions-history 的淮南子 62／文子 52／新語 18／賈子 14／墨子 13）；**兩庫都沒有的只有尸子約 26 段**；**原書亡佚僅存輯佚**約 214 段（傅子 38／桓範政要論 70／體論 30／桓譚新論 20／仲長統昌言 19／崔寔政論 18／陸景典語 12／曹丕典論 7）。
        - **⚠️ 這條數字第一版寫錯過（84.4%），錯因值得記：我只查了本庫 `translations/`，沒查 religions-history，就把淮南子／文子／墨子／新語／賈誼新書五部說成「兩庫未收」，五部其實都在另一庫。**本庫的收錄判斷天生是雙庫問題，**任何「我們有沒有 X」的斷言都必須同時 `ls` 兩個庫的 `translations/`**，只查本庫等於預設答案。
        - **這是本庫最貴的一次教訓：「逐字重出率」不是重複度指標，「來源書重出率」才是。**我先前拿 21 部既有藏書做前 24 字正規化比對，2,697 段只有 **72 段（2%）**逐字命中，據此寫下「重出風險已量過，結論是不必排除、`--exclude-dup-of` 在這部書上等於沒用」。**兩個數字都是真的，結論卻錯了**——魏徵節抄改寫，所以逐字比對必然低；但改寫過的《牧民》仍然是《管子》的內容，檢索層會拿它跟已標的管子 672 段撞在一起。**類書、選本、輯佚本一律先算來源書組成再談收不收，逐字比對只能證明「不是照錄」，不能證明「不重複」。**
        - **同一件事的第二層：送裁前要把範圍量到底。**我原本只送了「論語／孝經／孟子／老莊列 225 段（8.3%）」請使用者裁，因為那是我循 CLAUDE.md 分界規則第一眼看到的一塊。使用者反問「除了這些還有什麼」，往下做完才發現實際是 84%。**只送發現的第一塊，等於讓使用者在錯誤的量級上做決定。**
        - **仍然成立、可帶走的**：①`--exclude-dup-of` 的正規化比對腳本本身沒問題，錯的是它被拿來回答的問題；②「來源→閘門路由表」（章名決定走哪一道閘門）這個設計沒被否定，只是這部書用不上了，往後遇到選本／類書照樣適用；③通讀方法論（串行發包、兩層對拍、`check-reading-attribution.py`）與書無關，已保留。

12. **風俗通義 230 段 85 章全數標完（2026-08-15）——13／13 全覆蓋，判空 77 段（33%），並逼出第七道體裁閘門 §2.7「來源之外」（考辨體）。** `translations/fengsu-tongyi/`（`annotations.json` ＋ `meta.json.psych_survey`）已落地，`verify.py` 0 errors／16 warnings，`build-index.py` 已重生。逐書數字與證據寫在 `psych_survey.verdict`，閘門本身寫進 CLAUDE.md §2.7，此處只留可重用的方法結果。

    - **A 類 79 條錨點零校準全過，是本庫至今單部書最大的一組硬條件**（27 段必須判空、52 段必須命中、其中 11 段不得含 XII，另加 5 條章群與比例條件）。閘門效力可直接量：四篇（愆禮／過譽／十反／窮通）76 段命中 98%、〈山澤〉23 段 4%、器物 19 段 21%——**同書同基準同判者相差 25 倍**。
    - **驗收器改成現場解析 SPEC 的兩張表，不再手抄清單**（`delegation/fengsu-tongyi/accept.py`）。這是孔叢子 18 個假 FAIL 的直接對策，而且立刻有回報：解析出的段數與我在 SPEC 正文宣告的數字不符，抓到「必須命中 51→實為 52」「不得含 XII 10→實為 11」兩處自己數錯。**驗收器本身也要驗**——我另建了一份由 SPEC 錨點合成的「完美輸出」加五個故意突變（該判空卻命中、混入 XII、掉了 XII、AND 模式只給一格、指定必中卻判空），確認乾淨版 0 fails、五個突變各自被正確條款抓到，才拿去跑真實回收。
    - **spec 引文必須用腳本逐條對原文 substring 比對，肉眼看不出來。**首版 79 條錨點裡 16 條引文對不上：批次檔 b05–b07 是簡轉繁用 `為` 與 `“”`，我寫的時候手動正規化成 `爲`／「」；另有引文跨越了引號、以及來源本身標點錯亂的段落。還有兩條把長章名寫成簡稱（`燕太子丹…` 實際章名 34 字且含結尾的 `。`）。**全部修完再發包，回收零假 FAIL。**
    - **XI／XII 兩側同書並存，本書是至今最系統的一次證據。**〈正失〉五章呈現整齊的段位交替：[1]（世傳靈驗說法）判 XII、[2]（應劭「謹按」辨正）判 XI，**同章相鄰兩段判反方向、五章零例外**。〈怪神〉篇破除側 8 段落 XI、認證側 6 段落 XII，`石賢士神`[9] 同段同時給 XI＋XII（事前寫死的「同段兩向並存兩格都給」rider 唯一一次被觸發）。
    - **閘門在無指引處自己成立過一次。**〈世間多有精物妖怪百端〉整章沒寫任何錨點，判者自行拆成 [1][2][3][5] XII 與 [4] 郅伯夷照見老狸 XI＋VII。
    - **數量帶寬第三次猜錯，方向與前兩次相反。**孔叢子與申鑒是把薄領域猜多了，本書是把覆蓋猜窄了——「零段 domains 預期 ≥1」實得 0，13 格全部有段。**230 段是本庫達成 13／13 的最小樣本**（前次最小鹽鐵論 346）。其餘 6 條帶寬全中。處置照舊：逐段查證薄領域為真斷言後，改記誤差不鬆判準。
    - **通讀的分工改了**：230 段由我本人讀完寫 spec，吃掉一整個 Opus 視窗。下一部起改成派 Sonnet agent 通讀產出「逐章摘要＋候選錨點＋詞面陷阱」，我只寫閘門與裁定灰區。獨斷已照此派發，報告在 `delegation/duduan/READING.md`。
    - **獨斷 152 段逐字內嵌於蔡中郎集**（另有 2 段在集內重出，實扣 154）。兩部都是標準書目實體、都保留；重複段不重複發包，標註待獨斷標完後沿用。`make-delegation-input.py` 加了 `--exclude-dup-of`，蔡中郎集已重生為 317 段／9 批（commit `06fbdff`）。**已解**：扣重後 `b09` 是〈蔡中郎集外集卷四〉單章 110 段、不拆章規則下切不開；切分器補了「單章自己超過上限就章內切塊」的分支，切出的後續片段 `para_index` 不從 1 起、批次抬頭明寫「照抄不要重編」。蔡中郎集現為 **317 段／10 批**。
    - **配額**：本部 7 批。codex 7D 用量重置在 2026-08-21 14:10。

13. **獨斷 152 段 36 章全數標完（2026-08-15）——4／13、判空 143 段（94%），A 類 118 條錨點零校準全過。** `translations/duduan/`（`annotations.json` ＋ `meta.json.psych_survey`）已落地，`verify.py` 0 errors／16 warnings，`build-index.py` 已重生，commit `0214598` 已 push。逐書數字寫在 `psych_survey.verdict`，此處只留可重用的方法結果。

    - **第一部不立新閘門的書。**獨斷是漢代典章名物登錄簿，本來看起來要第八道閘門，實際用 §2.3「登錄之外」的問句＋§2.6② 制度條文判空＋§2.7 聲訓 rider 三者合成就切得乾淨。**遇到新體裁先試既有七道的組合，組合不成再現寫一道。**
    - **新界線：制度用意句要落在「人會怎麼樣」才承重。**登錄體最大的硬撈風險是條文後面那句「所以…也」。承重（命中）：「訓人民事君之道也」「能以善道改更已也」「使人望見則加畏敬也」「使為社，以自儆戒」；不承重（判空）：「以定天下之歡心」「所以風化天下也」「以節觀者」「示滅亡也」「所以尊鬼神也」「示其潔也」。**兩側都寫進 spec，兩側都判對。**
    - **管道新增一個發包前動作：驗規格本身。**`accept.py --check-spec` 把 SPEC 宣告的每個章名、段數、批次歸屬、引句逐條回批次檔對拍（S1–S9），跑三次才 0 FAIL——修掉章表 regex 漏 `段` 字、以及整章判空表漏列 `五祀之別名` 導致段數合計 81≠84（我自己數錯）。**孔叢子 18 個假 FAIL 與風俗通義 16 條對不上的引文，兩次教訓現在都由這個動作在發包前擋掉。**
    - **驗收器逆向驗證已成常規**：由 SPEC 命中表合成「完美輸出」跑 accept.py 得 0 FAIL，再注入六種變異（整章判空章命中／必中段判空／祭祀章填 XII／避諱章多命中／填 expression／雙生章判反）全部被對應條款抓到，才拿去跑真實回收。
    - **命中恰好等於寫死的 9 段、零額外，查過不是規格餵答案。**34 段未被錨點指名的自由段 reason 都引到本段自己的文字並逐句處理（`天子正號之別名`[17]「是議事文書的名義與流程，不是對異議人格或政治正義的評判」），逐條核對無假陰性。**這種高吻合度要主動查，不能當成好結果直接收。**
    - **B 類數量帶寬 7 條首次全中**（判空 ≥120 實得 143、`formalization` ≥80 實得 123、`ritual` ≥12 實得 23、`narrative` ≤5 實得 1、XII＝0、命中領域 ≤6 格實得 4、零段 domains ≥7 格實得 9）。**這不推翻 §2.1「數量事前寫不準」**：登錄體 81% 是同一個 mode，形態單一到數量也可預測；文體越雜（風俗通義 85 章橫跨考辨、志怪、名物、人物品評）數量越不可寫死。帶寬仍只寫成下限提示。
    - **章名只差一個括號的雙生章是本部最高風險。**`五祀之別名`（3 段全判空）vs `五祀之別名（祀臣五義）`（1 段必須命中），同批相鄰。SPEC 分表寫死＋accept.py 的 A7 雙向檢查，判者沒混淆。
    - **通讀分工照新做法執行**：由 `duduan-reader`（Sonnet agent）通讀產出 `delegation/duduan/READING.md`（逐章摘要＋候選錨點＋詞面陷阱），我只寫閘門、裁定灰區、寫 SPEC 與驗收器。這是分工改動後的第一部，結果支持繼續這樣派。
    - **發包切分器補了單章超限時的章內切塊**（`make-delegation-input.py`），解掉蔡中郎集 b09 那個 110 段不拆章死結；切出的後續片段 `para_index` 不從 1 起，批次抬頭會明寫「照抄不要重編」（commit 見 `make-delegation-input.py` 那次）。蔡中郎集已重生為 **317 段／10 批**。
    - **配額**：本部 4 批。codex ChatGPT 7D 在發包前為 94%，重置 2026-08-21。

14. **蔡中郎集已標完（2026-08-21）——本次判讀 207 段／133 章，另沿用《獨斷》逐字重出 154 段、〈外集卷四〉110 段留 `null`。12／13（只 I 掛零），本次判讀判空 68／207（33%）。** `translations/cai-zhonglang-ji/`（`annotations.json` ＋ `meta.json.psych_survey`）已落地，`verify.py` 0 errors／16 warnings，`build-index.py` 已重生。b01–b05 於 2026-08-15 跑完後撞 codex 7D 配額，b06–b08 在配額重置（14:10）後於同日 16:40–17:03 續跑，同一模型 `gpt-5.6-sol`（發包前比對 `~/.codex/sessions/` 的 model 欄確認），故整書統一歸屬、不必 `--tagged-by-batch`。逐書數字寫在 `psych_survey.verdict`，此處只留可重用的方法結果。

    - **碑誄體是連續第二部不立新閘門的書。**用 §2.3「登錄之外」＋ §2.2「純品題語判空」再加一條新界線：**德目排比句判空，除非文本自己把德目拆成行為標準、給出定義、或連到一個後果**。碑文的譜系公式、官歷、卒葬年月、喪儀套語、「山岳降靈」型頌辭都是事前點名的詞面陷阱，八批零校準全過。**段落級的操作句是：碑文一段常是「譜系＋官歷＋德目＋喪儀」混在一起，判的是這一段有沒有承重句，不是這一段有幾成是登錄。**已寫進 `CLAUDE.md` §2.3。
    - **X 這一格是本部最強的正面結果，而且量到了它的兩端。**整部文集段段有卒年、有哀辭，靠詞面聯想 X 會吞掉半部書；**實得 22／207**，逐段查證全是「有人在面對死亡，而文本在寫那件事」（〈西鼎銘〉[1] 橋玄「可以生，可以死」仍盡臣節、〈墳前石碑〉[1] 父臨終戒子與子的安頓哀思、〈《劉鎮南碑》〉[4]「耕夫罷耜，織女投杼，老幼哀號」）。**通篇夭逝哀辭的童子碑反而判空**（〈袁滿來碑銘〉〈童幼胡根碑銘〉——品題加套語，沒有一句在寫那個人面對死亡）。**與《新序》〈節士〉〈義勇〉44 段而 X＝0 是同一格的兩端**：新序的死是節與義的展示手段，碑誄的死是被書寫的處境本身。同一格判出 0 與 22，是它有區辨力的證據（同公孫龍子 II＝0 vs 尹文子 II＝5）。
    - **災異對策要三分，只寫兩側會判反；〈荅詔問災異〉10 段是最乾淨的一次驗證。**判空＋`formalization` 5 段（[1] 受詔程序、[2] 奉詔謙辭、[3][6][8] 逐條占釋與救法條目）；**XII 只在文本把意圖給了天**（[4]「仰見上帝之厚德」）；**XI 在人能改變結果的那一側**（[5] 顏子知過不復行、[7] 宋景公三有德言而熒惑退舍）；**同段兩向並存兩格都給**（[9][10]）。這是 §2.5②／§2.7④ 在對策體上的第四個版本，也是「同段兩向」rider 第二次觸發、且一次觸發兩段。〈王子喬碑〉[1] 是認證側的完整形態（IX＋XI＋XII）。
    - **總判空率不可讀，必須分層報。**361 段有判讀的段落判空 213（59%）是兩種文本混算：本次判讀 207 段判空 33%，沿用《獨斷》的 154 段判空 94%（登錄體）。**同一部書裡典章登錄簿與碑誄文集各佔一半，合併平均沒有意義**——同慎子正文 vs 逸文、商君書論說章 vs 條文章、新序善謀 vs 節士義勇。書內落差也重現：〈月令問荅〉14 段判空 12（86%，曆法經義考證）vs〈陳政要七事疏〉8／8、〈《劉鎮南碑》〉5／5 全中，**同一位作者、同一部集子，判空率從 0% 到 86%**。
    - **110 段留 `null` 是負面結果不是漏標，並量到一條界線：逐字比對對「重排過的重出」無效。**《獨斷》被文集收了兩次——一次以本名分章（36 章 154 段，逐字相同，`--exclude-dup-of` 扣得掉、`--inherit-from duduan` 沿用判讀），一次併成〈蔡中郎集外集卷四〉一章 110 段（切段粒度與異體字都不同，逐字咬不到），由 `--exclude-chapter` 扣出發包單位、`--leave-null-chapter` 留 null。內容已由 `duduan` 那份標註涵蓋，要合併需先寫一道異體字正規化的段落對齊，是獨立工項。
    - **`--inherit-from` 第一版只咬到 154 段裡的 71 段**：拿 `annotations.json` 的 `excerpt` 比對而 `excerpt` 只留 40 字，長段全漏。改成兩書都用 `split_paragraphs` 回 `raw/original.txt` 重算全段文字（與發包時同一來源）後修好。**凡跨書比對段落文字一律回 raw 重算，不要用 annotations 裡的任何摘要欄位。**已寫進 `CLAUDE.md` §2.1。
    - **通讀報告的「引號約定」在理由欄失守，SPEC 只能抄逐字引句欄。**`READING.md` 開頭宣告「」內一律逐字、且自稱 129 條引句已回批次檔對拍全過；但〈誤撈風險與理由〉欄的引號實有三種來源——本段逐字、**他章的對照引句**（《太傅祠前銘》[1] 那列引的「巖巖山岳，配天作輔」其實在 b06 的胡廣黃瓊頌[1]）、以及**作者自己的概括**（「山岳降神生此人」全書不存在）。自檢腳本按段對拍時這兩種都會變成假 FAIL 或假 PASS。已寫進 `CLAUDE.md` §2.1。
    - **一次跨批改寫事故（已還原），並更正 `7a5f39c` commit message 裡我自己的誤述。**b03 的執行改了已驗收 commit 的 `out/b02.json` 一列（`難夏育上言鮮卑仍犯諸郡`[1] V → `[]`），違反發包 prompt 明文禁止。**該 commit message 寫「clobber 偵測沒抓到、真正抓到它的是 git status」——這句是錯的。**偵測器抓到了也印出了檔名，只是印在該批結束時；git status 只是我更早看到。已 `git checkout` 還原、重跑 check ＋ accept 0 FAIL，該列不是錨點，b02 驗收不受影響。**留下的真問題是時機不是能力**：偵測在批末、跨批改寫在批中，所以成組發包時每批之間要看一次 `git status`——b06–b08 三批全程照做，零事故。
    - **A 類 67 條錨點八批 0 FAIL；B 類數量帶寬第四次猜錯。**判空預估 100–150／207（48–72%），實得 68／207（33%）。**§2.1 的 A／B 分法又添一組證據，處置照舊：記誤差、不鬆判準。**
    - **配額**：本部 8 批（b01–b05 於 08-15、b06–b08 於 08-21），每批約 6–8 分鐘、97–102K tokens。b01–b05 跑完後曾撞 codex 7D 耗盡，b06–b08 各約 3 秒硬失敗（`You've hit your usage limit … try again at Aug 21st, 2026 2:10 PM`），依規則立即停派不重試、不排自動恢復；配額重置後同日 16:40–17:03 手動續跑完成。

15. **太玄經 880 段 93 章全數標完（2026-08-22）——13／13 全覆蓋，判空 658 段（75%），A 類 0 FAIL。** `translations/taixuanjing/`（`annotations.json` ＋ `meta.json.psych_survey`）已落地，`verify.py` 0 errors／16 warnings，`build-index.py` 已重生，commit `28ea7e5` 已 push。逐書數字寫在 `psych_survey.verdict`，此處只留可重用的方法結果。

    - **連續第三部不立新閘門，但它是第一部需要「一書兩道閘門」的書。**太玄是兩層文本疊起來的：贊辭層 810 段（81 首 × 9 贊＋測辭）、玄傳層 70 段（玄攡／玄瑩／玄棿／玄圖／玄告／玄數／玄文）。**兩層要用兩句不同的閘門問句，不可共用**——玄傳層走 §2.3「登錄之外」，贊辭層走 §2.1 中性物件替換而**被替換的對象是序號本身**，操作句是「把這一贊從序號上拆下來，句子還成不成話」。「初一：昆侖旁薄，幽」離開位次不成話判空；「厚不厚。比人將走」「子序不序。先賓永失主」拆下來仍是對人的斷語，命中。已寫進 `CLAUDE.md` §2.3。
    - **書自己提供了判空側的自證，這是本部最省事的一條 rider。**`玄圖`[3]「九虛設闢，君子小人所為宮也。自一至三者，貧賤而心勞……」——君子小人是位次的名目、貧賤富貴是位階的屬性，不是在寫人。贊辭裡大量的「君子」「小人」由此一律先當位次標籤處理，除非該贊另有一句落到人身上。**發包前先在原文裡找有沒有這種「體例自述段」，比自己造 rider 可靠。**
    - **XII 這一格本書要四分，不是兩分——§2.5②／§2.7④ 的第五個版本。**事前把四側全部寫進 SPEC 並全部判對，全書 XII 只有 4 段、術數詞面零外溢：**破除側**（`聚`[2]「鬼神以無靈」、`玄攡`[8]「天豈去人哉？人自去也」、`失`[8]「禍可轉也」→ XI 且不得含 XII）、**認證側**（`交`[3]、`周`[7]、`唫`[9] → XII）、**過度側**（`聚`[7]「畏其鬼……過其正也」→ XII，寫的是人畏鬼過了頭）、**技術側**（`玄數`[1]「爰質所疑于神于靈」、`交`[5]、`玄棿`[6] 祀典由來 → `[]`，一格都不得填）。**四側寫死後，同一首之內破除（`聚`[2]）與過度（`聚`[7]）並存也判對了。**教訓是：寫 spec 前先問這部書對神明有幾種立場，不要預設只有兩側。
    - **`accept.py --check-spec` 抓到我自己三處自相矛盾，全在發包前修掉。**①`玄瑩`[2] 在配套表與命中表被下了相反指令（該段 495 字，前十三條是清單、段尾「非精其孰能之」轉向論說，照 §2.5③ 必須命中）；②兩張判空表有 8 段重疊而正文宣告 71 實為 63；③B 類兩條帶寬端點互斥。**寫 SPEC 的人自己數不準，這個動作現在每部都要跑。**
    - **B 類數量帶寬四條錯了四條，是本庫連續第六部猜錯。**贊辭層判空預估 ≥700／810 實得 600；XI 預估 8–25 實得 30；命中領域預估 8–10 格實得 13；零段 `domains` 預估 3–5 格實得 0。**兩側都抽樣查證過判得對**——命中側 reason 逐條引本段自己的文字與測辭，判空側 reason 明說「這是幾位取象」而非籠統套語。處置照舊：記誤差，不回頭鬆判準。
    - **贊辭層 26% 的命中率是這道閘門的結果，不是判準鬆。**同書玄傳層 70 段只有 12 段命中（17%），贊辭層 810 段命中 210 段——**測辭是太玄的「小象傳」，它把位次翻譯成對人的斷語，這一層本來就承重。****遇到高命中率先問體裁，同遇到高判空率時一樣**（§2.2 末條的反向用例，已寫進 `MAP.md`）。
    - **試點對的選法：先跑兩層各一批。**b04（玄傳層）＋b02（贊辭層）當試點對校準，A 類全過後才放行 b01＋b03。**一書兩層時試點必須兩層各取一批**，只跑一層等於只校準了半部書。
    - **配額**：本部 4 批，每批約 8–9 分鐘、約 106K tokens。判讀者 `gpt-5.6-sol`（發包前後都比對 `~/.codex/sessions/` 的 model 欄確認，整書統一歸屬）。

16. **京氏易傳 784 段 67 章全數標完（2026-08-22）——6／13，判空 764 段（97%），XII 全書掛零，A 類 0 FAIL。** `translations/jingshi-yizhuan/`（`annotations.json` ＋ `meta.json.psych_survey`）已落地，`verify.py` 0 errors／16 warnings，`build-index.py` 已重生，commit `9faeff7`（試點）／`67bd421`（b02）／`c77046b`（收尾）均已 push。逐書數字寫在 `psych_survey.verdict`，此處只留可重用的方法結果。

    - **連續第四部不立新閘門，而且太玄經那套「一書兩道閘門」直接搬得動。**欄位層（飛伏／世應／建候／積算／五星／宿／爻變／轉卦）走 §2.3「登錄之外」，卦爻斷語層走 §2.1 中性物件替換**但替換的對象是爻位**（同太玄替換序號）。`革`[4]「天地革變，人事隨而更也」、`明夷`[12]「當世出處，為眾疑之所及，反傷於明」拆下爻位仍成立，命中。**同體裁的第二部書可以直接沿用第一部的閘門構造，這是本庫第一次驗到。**
    - **一部占筮書 XII 掛零，是「術數語彙在術數書裡是技術術語」的第四個體裁版本。**前三個是 §2.4 醫理的神魂魄、§2.5 兵書的占候、§2.7③ 的祀典神名考證。SPEC 的「不要做的事」逐條點名六親、君臣、身體部位、性命、遊魂皆為欄位名目，回收零違反。**注意這與太玄經恰好相反**——太玄要把 XII 四分，京氏要把 XII 全禁；**同屬象數的兩部書在同一格上是兩個極端，spec 不可互相推定**（守則 1 在體裁內部一樣成立）。
    - **詞面陷阱的判空側自己長出了與太玄相同的 rider。**`復`[12]「君子進，小人退」判空，reason 寫「兩種人稱仍是陰陽代號」——**與太玄經 `玄圖`[3] 的位次標籤是同一條規則，但京氏的 SPEC 沒寫這句，是判者在另一部書上獨立長出來的**。`家人`[2]「閑邪存誠」判空、reason 寫「在此是卦德訓詁」，擋掉了章名（家人／咸／困／明夷／歸妹／訟／蒙／旅）反射填領域這個最大陷阱。
    - **驗收器派給 Claude sub-agent 寫，我獨立重跑四層驗證，它立刻抓到我自己 SPEC 的三處缺陷。**①A5 把章名寫成短名 `卷下`（實為 `京氏易傳卷下`，硬條件只能靠後綴匹配矇到）；②配套(4) 把**我自己的白話轉述**放進「」，違反本庫引號約定——**與蔡中郎集 `READING.md` 同型，差別在這次是我犯的**；③A8 要求 `總結`[1]–[8] 全判空但錨點表只列了 [1][2]，補列六列後宣告數 41→47。**驗收器外包＋自己重驗是划算的：寫的人看不見自己的錯，這三處我讀過三遍都沒看到。**
    - **B 類五條數量帶寬全中，是本庫第二部全中的書（第一部是獨斷），而且複驗了獨斷給的解釋。**判空 ≥600 實得 764、`formalization` ≥420 實得 607、命中 8–25 實得 20、XI 4–10 實得 8、命中領域 3–7 格實得 6 且零段 domains 6–10 格實得 7。**這不推翻 §2.1「數量事前寫不準」**——兩部全中的書都是單一 `discourse_mode` 佔到 77–81%，形態單一到數量也可預測；文體越雜（風俗通義、蔡中郎集、太玄經）越不可寫死。**我自己在 b03 落盤前還預測有三條會爆掉，也錯了**（b03 的 228 段只多出 1 個命中）。帶寬仍只寫成下限提示。
    - **命中不是零額外，所以獨斷那條警訊不適用。**20 個命中裡 `姤`[14] 大象號令、`豫`[2]「聖人順動則刑罰清而民服」、`井`[9] 夾注「井以德立，君正民信」三段**不在任何錨點上**。另有 5 個命中純由注文成立（25%）——作為「這個本子」的紀錄為真，作為「京房易學」須扣掉。
    - **配額**：本部 4 批。太玄經與京氏易傳兩部曾同時各跑一支 `run-delegation.sh`（不同 slug、不同 `out/` 目錄、各自的 clobber 快照），牆鐘時間減半且零干擾——**成組發包時不同書可以並行，同一書的批次仍須串行。**

**其餘各部要不要照中性物件替換判準重判？結論：不重判。** 該測試打的是「出題包裝被當成內容」，那是算書特有的體裁問題；海島算經本來就 0／13。孫子、人物志、潛夫論、鹽鐵論、顏氏家訓是論說或訓誡書，文句直接對人事下判斷，場景本身承重，替換測試咬不到。世說新語是敘事體，該測試根本用不上，改走「刪掉這一則」閘門（見上）。**鹽鐵論 V 佔 88% 這個獨立警訊也已排除**：顏氏家訓在同一套判準下判出 IV 86 > V 47，證明 V 這一格有區辨力，88% 是「60 篇朝廷辯論」這個文本特徵，不是 V 寬到吞掉一切。

## 已知風險

- **ctext.org 是紅線**：明文禁自動批量下載。本庫現在完全不碰它
- **`expected_chapter_count` 已從粗估改成驗證後凍結的觀測值**，用途變成回歸護欄。之後若下載器行為改變導致章數變動，verify 會叫——那時要判斷是修好還是弄壞，**不要反射性再同步一次數字**
- **焦氏易林上游殘缺**：全書 64 卦、傳統書目 16 卷，Wikisource 只有 乾之／坤之／屯之／艮之 4 個子頁、18,321 bytes ≈ 全書 6%。已寫進 catalog `coverage_note`。catalog 的 expected=4 是「來源有多少」，meta 那份 16 才是書目值。**`verify.py` 的截斷偵測器 2026-08-11 修好後會永久對這部叫一次**（`only 4 chapters against an expected 16`）——**這是真陽性，留著不要消音**。內容值得標，但要標之前得另尋來源補完；未補完前不排進標註批次
- **`verify.py` 的截斷偵測器從 `361be49` 起死了將近全程，2026-08-11 才修**。死因：那次下載把 catalog 的書目卷數覆寫成實際抓到的章數，於是拿實際值跟實際值比，永遠相等。meta 那份也不全可信（19 部被另一次寫入改過，例如 shuowen meta=540 vs catalog=15、zhanguoce meta=511 vs catalog=33）。**現行修法是取 `max(catalog, meta)` 當下界、且只在短缺時報**（多出來是粒度差異不是問題：六韜 6 卷 66 篇兩個數字都對；用 `abs()` 會讓 34 部長期噪音，噪音多了就沒人看）。**真正的修法是重查 72 部的書目卷數，那是研究不是修補**，沒做
- **五部因「一章＝一段」暫緩標註，根因在上游 HTML 不是下載器**：`heguanzi` 19 段／19 章／段長中位 **989 字**（最長 2,874）、`sanzijing` 1 段 1,411 字、`yandanzi` 5 段中位 742、`zhonglun` 35 段中位 534、`guiguzi` 72 段中位 127（最長 2,917）。對照論說書正常值（尹文子中位 91 字）差一個數量級。**Wikisource 把整章包在單一 block 元素裡，`extract_main_text()`（`scripts/download-*.py` 第 254–271 行）沒有 `\n` 可切**，所以**重抓救不了**。段落級標註在 989 字的單位上等於章級標註，判準會失效。要標得先寫一道句讀切分（標點＋語意雙軌），那是獨立工項，未做
- **`古三墳`、`竹書紀年（今本）`標 `contested`**：偽託／存疑，標註時要記版本立場
- **`category` 欄還有兩處可疑，但未讀原文所以沒動**：`chuci`（楚辭）現歸 `術數`，楚辭是辭賦不是術數，這一條在任何讀法下都不對，但本庫沒有合適的既有類別（`文論` 收的是文心雕龍＝文學批評），要嘛新增 `辭賦` 要嘛併入 `雜著`；`shanhaijing` 現歸 `術數`，四庫入子部小說家類、傳統多歸地理，本庫已有 `地理`（水經注）與 `志怪` 兩個更貼近的類別。**兩者都要讀過才動**——古三墳的教訓正是「讀完 78 段才敢說它不是志怪」。`category` 只驅動 `--category` 下載選取與索引分組，不影響標註，所以不急
- ~~**判空率連續偏高，需要一部反向樣本**~~ —— **已解除（2026-08-09）**。第九至十四部判空率 77%／86%／55%／98%／98%／99%，六連之後無法只憑內部證據排除「判準整體漂向保守」；列女傳在同一道閘門、同一管道、同一模型下判出 **4% 判空、96% 命中、12／13**，是本庫判空率最低的一部。**九章 99% 判空與列女傳 96% 命中之間的差距是內容差距，不是判準差距。**
- **`shanghanlun` 來源缺〈辨發汗吐下後病脈證並治第十八〉**（〈辨可吐第十九〉之後直接跳到第二十）。已寫進該部 `psych_survey` 與 spec 硬規則。`raw/original.txt` 不動（動了破 SHA-256）
- **未涵蓋的第二批候選**：藝文類聚、太平廣記、太平御覽、通典、康熙字典、墨子閒詁、墨經校釋

## 已明確排除（不要再問一次）

| 書 | 去向 | 理由 |
|---|---|---|
| 爾雅 | religions-history `er-ya` 已收 | 不重複收，用 cross-ref 指過去。《釋親》例外抽取見 SCHEMA §3 |
| 四書章句集注、論語注疏、孝經注疏 | religions-history | 經注，屬「被當成經典」那一半 |
| 列仙傳、高士傳 | religions-history | 神仙傳記＝道教敘事 |
| 詩說 | religions-history | 詩經注 |
| 小說、正史 | 兩庫都不收 | 使用者明確排除 |
| 申不害 | 不收 | ctext 路線整條移除 |
| 移轉宗教庫既有文本過來 | 不做 | 會破 SHA-256 鏈、143 部完整三軸標籤、GitHub 歷史，收益近零 |

## 尚未做、不可當結論的推測

pilots 檔 §5 記了兩條：說文解字 540 部首是否構成一套世界分類、釋名聲訓是否編碼民俗世界觀。**兩者均未讀原文**——全文已於 2026-08-09 入庫（說文 530／540 部、釋名 27 篇），但都還沒標。用第一批的探針跑一次即可證否。

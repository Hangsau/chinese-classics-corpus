# HANDOFF — chinese-classics-corpus

> 狀態快照。行為規範見 [`CLAUDE.md`](./CLAUDE.md)，結構導航見 [`MAP.md`](./MAP.md)，資料契約見 [`SCHEMA.md`](./SCHEMA.md)。
> 最後更新：2026-08-09

## 現在在哪

**第一批 68 部全文已入庫。標註管線已跑通，已標 11 部（3,267 段）。**

`verify.py`：68 部、10,528,533 bytes、**0 errors、12 warnings**（警告數 6 → 14 是因為擴檢，不是變差；14 → 12 是重抓戰國策與群書治要修掉的，見下方章標籤一節）。

| 部 | 段數 | 已判讀 | 命中領域 |
|---|---|---|---|
| `sunzi-bingfa` | 91 | 91 | 11／13（未命中 I、XIII） |
| `jiuzhang-suanshu` | 720 | 720 | **1／13**（XII 1 段；2026-08-09 重判，原 3／13 已撤銷） |
| `haidao-suanjing` | 24 | 24 | **0／13**（全空） |
| `renwuzhi` | 229 | 229 | 10／13（未命中 I、IX、XII） |
| `qianfulun` | 268 | 268 | 12／13（未命中 XIII） |
| `yantielun` | 346 | 346 | **13／13**（全覆蓋，但 V 305 段＝88%） |
| `yanshi-jiaxun` | 255 | 255 | 12／13（未命中 I；**IV 86 > V 47**） |
| `shishuo-xinyu` | 1,132 | 1,132 | **13／13**（全覆蓋且尾巴不薄；**V 316 只佔 28%**） |
| `shenyijing` | 61 | 61 | 4／13（**VII 13 段獨大**；77% 判空） |
| `dongmingji` | 63 | 63 | 7／13（**每格只有 1–3 段**；86% 判空，本庫最高） |
| `gu-sanfen` | 78 | 78 | 4／13（**V 34 段＝命中的 97%**；55% 判空） |

其餘 57 部 `psych_survey` 仍是 `null`——依 SCHEMA §5，`null` 是「未通讀」不是「沒有」。

**十一部的意義各不相同**：孫子證明方法找得到東西（連自己試點都漏標 3 個領域）；九章證明方法說得出「沒有」（719/720 段為空，2026-08-09 重判後）；海島是刻意挑的第三部，用來判斷九章的 `discourse_mode` 缺口是不是算書通例——是（見下方待決事項 1），順帶給出第一個 **0／13 全空**的結果；人物志是第一部論說書，用來檢驗判準離開算書語境還切不切得動；潛夫論是第一部批判性論政書，用來檢驗 V 這條判準在負面診斷式論述下是否還穩；顏氏家訓是刻意挑來**證否 V 的**；世說新語是第一部敘事體，用來兌現硬規則第 5 條；神異經是第一部**作者不在場**的文本；洞冥記是神異經的對照組（同類別但有情節）；古三墳是第一部**內部體例高度異質**的書。後三部見下方各節。

**人物志（229 段）的三個結果**：

1. **10／13 不代表覆蓋廣**。VII 83 段、VIII 81 段、II 64 段、V 57 段吃掉絕大部分，而 IV、X 各只 1 段，XIII 2 段，III、XI 各 4 段。尾巴薄到不能當本書特徵，只能記「有觸及」。往後讀 `domains_hit` 一律要配 `domain_para_counts` 看，光看命中數會高估。
2. **`formalization` 首次出現在非算書**。〈材能〉4–11 共 8 段是「某能→某材→在朝某任→為國某政」的對照表，把「誰該任什麼職」寫成可查表的程序。判準同 vocab 舉的「儀禮喪服」。這證明該值綁的是「規範被寫成可執行程序」，不綁數學。
3. **`Z-wisdom` 吸走內容的第二個實例**。25 段 psych_domains 為空，多數不是沒東西，是內容屬「如何察人／察人為何難／辯論技術」的方法論外殼。與九章的差別在本書外殼底下仍有大量對人的實質主張，整體不會被讀成「沒思想」——但這再次支持待決事項 2。

**潛夫論（268 段）的三個結果**：

1. **12／13，唯一真空是 XIII（安頓·修復·平安）**。這個零不是漏標：王符寫的每一種痛（邊民內遷、寡婦被強嫁、賢者不得用）都收在「該怎麼改制度」上，沒有一處轉向「人可以如何與之相處」。孫子也未命中 XIII——**XIII 在兩部世俗實務書都缺，可能是本庫這一類文本的通例**，往後留意。
2. **空白的第三種成因：體例摻雜**。69 段空白裡屬 `Z-wisdom` 外殼的只是少數，多數來自〈誌氏姓〉的姓氏譜系（18 段）與附錄的版本層陳述（42 段）。九章是被 `Z-wisdom` 吸走、人物志是方法論外殼、本書是「這些段根本不是同一種文本」——**下結論前要先分清空白屬哪一種**。
3. **附錄佔 19%，必須從分母裡拿掉**。268 段有 52 段是後漢書本傳、清人序跋、歷代著錄與佚文。本文 216 段空白只有 27 段，附錄 52 段空白 42 段。算領域密度一律用 216。

**鹽鐵論（356 段）的四個結果**：

1. **13／13 全覆蓋，但這個數字最需要配 `domain_para_counts` 讀**。V 305 段（本文 346 段的 88%），I 只 1 段、XIII 3 段。全覆蓋來自體裁不是思想廣度——60 篇朝廷辯論把「國家該做什麼」一路推到人性、鬼神、孝養、刑獄、邊民家人，每個領域被掃到一次，但絕大多數段落回頭仍落在 V。
2. **XIII 命中，推翻上一部立的假設**。潛夫論曾推測「XIII 在世俗實務書是通例性真空」。本書 3 段命中（〈毀學〉包丘子、〈貧富〉「不遇，退而樂道」、〈地廣〉「回也不改其樂」）。差別不在體裁而在**發言位置**：本書的文學／賢良是在野一方，被迫替自身貧賤辯護，於是必須說出一套自處之道；王符雖也不遇，卻整部朝制度說話。**XIII 的有無取決於文本裡有沒有人替自己的處境說話。**
3. **「人能不能改變」第三次出現，結構完全不同**。人物志與潛夫論都是作者自己前後矛盾；本書是**陣營對立且反覆**：〈殊路〉3／5 vs 4／6、〈疾貪〉3 vs 4、〈和親〉3 vs 4——三對無一例外，大夫／御史恆取不可改，文學／賢良恆取可化。此命題已從「某書內部的不一致」升格為「有陣營歸屬的立場分歧」。
4. **辯論體子準則守住了，但要補成三分法**：辯論**技術**→`Z-wisdom` 不標；辯論**倫理**作為規範→VII（〈論誹〉6）；**進言不被聽見／被誤解**→III（〈箴石〉2、〈相刺〉5／6／8）。三者原文外觀相近，沒有這條三分法就會全部標空或全部標滿。

六次全文讀相對於**試點**的錯法一致：都只往「補上漏標」修（人物志漏掉 V 的 57 段；潛夫論預期 V 為主成立，但完全沒預期到 XII 有 32 段、IV 有 16 段，也沒預期到 I 會命中）。**但「偏誤方向是穩定的低估」這個結論已於 2026-08-09 被九章重判推翻**——見下節。低估與高估是兩種獨立的失敗，判準過寬時會系統性高估。

**顏氏家訓（255 段）：V 過寬的疑慮已解除**

前六部裡 V 從未低於半數（鹽鐵論 88%、潛夫論 65%），無法分辨那是文本特徵還是判準過寬。顏氏家訓是**為了證否 V 而挑的**：它談南北朝士族的家內事，若 V 的浮濫來自判準，家內事會被大量讀成階級與權力。

結果 **IV 86 段 vs V 47 段**，V 沒有壟斷。逐條檢視 47 段 V，全部落在朋友、賓客、擇友、我群他群偏見、為政與領導，**沒有一段把父子兄弟婚嫁讀成階級權力**。

> **結論：鹽鐵論與潛夫論的高 V 是文本特徵，不是判準過寬。那兩部本來就在論朝政。**
> **但前提是 spec 必須明寫「家內事歸 IV、朝廷事歸 V」——不寫的話這道分工不會自己發生。**發包 spec 用一張分流表把 13 個領域逐格指定歸屬（見 `delegation/yanshi-jiaxun/SPEC.md`），這是本次沒有重蹈覆轍的直接原因。

其餘四個結果：

1. **XIII 的假設再次被支持**。15 段，且分布不是散的：〈终制第二十〉4 段全中、〈止足第十三〉3 段全中、〈省事第十二〉3 段、〈序致第一〉與〈勉学第八〉各 1 段——全是顏之推第一人稱交代自身遭遇，或教子孫在改不了的處境裡如何安身。鹽鐵論立的「XIII 取決於文本裡有沒有人替自己的處境說話」在第二部書上成立。
2. **`Z-wisdom` 陷阱這次沒踩**。〈勉学第八〉1–17 段命中（VIII 12 段為主，兼 II 名實、X 失所、XI 晚學能否改變、XIII 亡國後父子分定供養與教學之責），18–30 段的文字音義考證尾巴幾乎全空。人物志與潛夫論都在這一格整篇判空過；差別在 spec 逐句舉了四個正反例，要求分辨「哪一句在談方法、哪一句在談人」。
3. **三篇特殊體例寫成「預設 `[]` 但逐段確認」而非「整篇 `[]`」，撈出 7 段真例外**。書證第十七 91% 空，但 4 段離開考據去評人（斥俗儒臆造病因、南方學士視北方傳記為偽書而不讀、譴責文士把三婦添鄭衛淫辭、小學者拘守小篆）；音辭第十八 70% 空，3 段是南北士庶口音相非笑與家內督正兒女。**整篇一刀切會全部漏掉。**
4. **本庫第一個密集 VI 叢集**。全書 VI 只 14 段，11 段在〈風操第六〉居喪一節——那節在處理哀情該不該發、發到什麼程度、真哀還是假哀。把喪親換成中性物件整節就垮，場景承重。另〈歸心第十六〉19 段 XII 全中，是目前 XII 密度最高的篇章：一部世俗家訓裡夾一篇護法論，再次說明書級單一標籤打發不了內部異質性。

唯一落空的領域是 **I（存在與意義）**：這部書從頭到尾在講人該怎麼活，卻從不問人為何而活。

**世說新語（1,132 段）：敘事體第一次被檢驗**

前七部全是論說、算書或訓誡，沒有一部靠故事承載判斷。硬規則第 5 條（敘事段也要判領域，故事是載體）從未被大規模檢驗過。世說新語整部由人物軼事構成，是這條規則第一次要在 1,132 段上兌現。

`discourse_mode`：`narrative` 903（80%）、`proposition` 210、`observation` 200、`expression` 180、`prescription` 44、`ritual` 3、`formalization` 0、`worked_instance` 0。

1. **規則 5 成立。** 74% 的段落判出領域，13 格全中，且尾巴不薄——最小的 I 有 8 段、III 16、IX 17、XII 21、XIII 25，都成群而非孤例。〈傷逝〉19 段 X 中 18、〈惑溺〉7 段 III 中 6、〈棲逸〉17 段 XI 中 10，這些都不是命題句寫出來的，是從情節讀出來的。**敘事體不需要另一套判準，需要的是另一道閘門。**
2. **中性物件替換測試在敘事體上不能用，改用「刪掉這一則」閘門。** 軼事裡的人就是內容本身，換掉就沒有這一則了。改成：**把這一則刪掉，讀者少知道了什麼？少的是史料事實 → `[]`；少的是對人的看法 → 給領域。** 這道閘門在〈賞譽〉切得最乾淨：純品題語（「勁松下風」「廊廟之器」）判空並走 `observation`，評語導向薦官任用才給 V。全書 200 段 `observation` 高度集中在 賞譽 93、容止 23、品藻 19、文學 15，正是「只有一句人物評語、沒有事件」那一類。
3. **V 316 段只佔 28%，是目前 V 佔比最低的一部有規模的書**（鹽鐵論 88%、潛夫論 65%）。主軸是 V 316、II 229、VII 226、VI 139。
4. **「拿門類名當答案」的警告，一半落空一半成立。** 落空的：〈賞譽〉156 段提示 II，實得 V 30 > II 25 且 48% 判空；〈容止〉提示 II，實得 II 12 但 48% 判空。成立的：〈品藻〉II 40 主導、〈傷逝〉X 18、〈惑溺〉III 6、〈棲逸〉XI 10、〈捷悟〉〈巧蓺〉〈術解〉如預期多半判空。**提示表寫成「提示不是對映」是對的——照抄會在最大的那一門上錯得最重。**
5. **I（存在與意義）第一次成群命中，8 段**（前七部合計只有鹽鐵論 1 段）。分布不散：〈文學〉4 段全是玄學本體之問，〈任誕〉3 段全是「什麼樣的一生對自己已經夠了」，〈棲逸〉1 段是阮籍以無為與盛衰窮達叩隱者。**I 的觸發條件因此可以初步寫出來：要嘛文本在做形上學，要嘛有人替自己的一生定價。**兩者前七部都沒有——論政書問「該怎麼治」、家訓問「該怎麼活」，都不問「為何而活」。
6. **XIII 的既有假設第三次成立，且支持面最寬**。25 段散在 12 個門類（言語 5、雅量 5、黜免 4、賢媛 3，其餘各 1）：不是集中在某一篇，是凡有人在改不了的處境裡開口就會出現。〈黜免〉9 段中 4 段命中（被廢黜者的自處）是密度最高的一格。
7. **III（愛與親密）16 段，本庫第一次不是零星**（前七部最多 4 段）。集中在〈惑溺〉6 與〈賢媛〉5。差別在體裁：論說書談夫婦是談倫理位置，軼事書才寫得出「他實際上怎麼對待她」。
8. **`formalization` 與 `worked_instance` 全書零命中，`ritual` 只 3 段**。八值表在敘事體上實際只用到六值。這不是缺陷是體裁指紋——算書那兩部反過來是 `worked_instance` 壓倒性。
9. **spec 卡了 `modes` 上限卻沒卡 `domains`，codex 就從那個縫鑽。** b01 8 段有 6 段疊三個領域，b22 42 段只有 2 段。補上「預設一到兩個，第三個要在 `reason` 裡寫得出拿掉它少講了什麼」並點名兩種典型湊法（把背景條件當領域：因家族勢大而隱忍 → IV；政爭裡有人死 → X）之後，其後各批 3+ 領域幾乎歸零。**與顏氏家訓同一個教訓：沒有數字的規訓不會生效。**

**神異經 61 段／洞冥記 63 段：作者在不在場，與同類別不同輪廓**

這兩部同批送出，是一組對照。神異經仿山海經逐條記荒外物產，**沒有情節、沒有敘事者立場**，是本庫第一部作者不在場的文本；洞冥記體裁近親，差別只有一項——**有人物與情節**（武帝貫穿全書、東方朔反覆出場）。要驗的是判準會不會空轉，以及敘事框架會不會把判準往上推。

1. **判準沒有空轉。** 神異經 77% 判空、洞冥記 86% 判空（本庫最高），但每一條判空都寫得出它少了什麼。反向證據更關鍵：兩書合計出現「食之令人益壽」「飲此酒人不生死」「望仙」「羽化」數十處，**一條都沒有被填進 X 或 XII**；「其國大旱」沒有被填進 IX；出現「人民」「君」的地理條目沒有被填進 V。本草式功效與地理陳設沒有觸發任何反射命中。
2. **敘事框架沒有把判準往上推，反而暴露它與內容無關。** 洞冥記 44% 段落是 `narrative` 或 `expression`（28+10 於 63 段），遠高於神異經（`narrative` 1 段），**判空比例反而更高**。「有情節不等於有內容」——獻獸後獸骨飛去、青鴨化為三小童都有人物有事件，讀者少知道的只是一件異事。**`narrative` 是體裁指紋，不是命中訊號。**
3. **兩部同 category 的書，領域輪廓幾乎不相交。** 神異經 VII 13 段獨大（用怪物給人格類型取名字：饕餮、渾沌、窮奇、善人），XII 1；洞冥記 **VII 與 XII 都是零**，命中散在 II 2、III 3、VIII 3、X 2、IV 1、V 1、XIII 1，每格 1–3 段。交集只有 IV 與 V 各一段。**`category` 在領域層面沒有預測力**——這是守則 1「禁止用書名或類別決定標註價值」在同一類別內部再次成立。
4. **神異經的命中在地理上壓在西方三卷。** 西南／西／西北 19 段中 9 段命中（47%），東荒／東南／南荒 28 段只有 3 段（11%），北荒與東北荒全空。原因是《春秋》四凶（渾沌、檮杌、饕餮、窮奇）連同苗民、共工全被安置在西方。**道德內容是隨四凶傳說一起進來的，不是均勻散在全書**，書級單一標籤會整個抹掉這件事。
5. **兩個邊界案例已記在各自的 `psych_survey` 裡，不要當穩固前例引用。** 神異經西南荒經三則第 1 則（名曰聖／哲／賢的全知異人）判 `[]`——它羅列的是能力與效驗，不是人該怎麼活；這是「聖」字沒有觸發反射的證據。洞冥記卷四第 6 則（巨靈化青雀）判 III 是全書最薄的一條，形式上屬判空類「奇物變形飛去」，只靠「愛悅於帝」與「帝乃起青雀臺」兩句收束撐住。
6. **`expression` 在洞冥記 10 段是本庫最高，但只有 4 段判出領域。** 第一人稱不等於自我揭露——東方朔自陳遠國見聞仍是一串地名物名。

**古三墳 78 段：內部體例高度異質，spec 要按體例切而不是按批次切**

本書由五種體例混成（序 1／太古河圖代姓紀 2／三易卦位方陣 24／傳（1）（2）（3）25／皇策辭與兩政典 26），`text_role=contested`。spec 因此改成**逐體例給預設答案**，並明點三處最易整批判錯的分界。

1. **三處分界全部分開了。** (a) 易表 24 段滿是「君臣民后妻夫父子」字眼但只是在排格子 → 全判 `[]` + `formalization`；(b) 傳（1）9 段與傳（2）8 段是訓詁 → 判 `[]`，傳（3）8 段把每個卦象轉成聖人職責（求諫、察姦佞、正疆界、設溝洫）→ **全部命中 V**；(c) 太古河圖代姓紀第 1 段判 **I**（宇宙生成論），第 2 段判 `[]`（譜系紀年），**「混沌」「太極」沒有反射給 XII**。三傳若被一視同仁，要嘛少 8 段真命中，要嘛多 17 段假命中。
2. **這其實是兩本書釘在一起。** 41 段在建體系（判空），37 段是訓誥政典（判 V）。`discourse_mode` 同樣乾淨分裂：`formalization` 52 對 `prescription` 28。命中 35 段中 34 段是 V＝**97%，本庫單一領域集中度最高**（超過鹽鐵論 88%）。
3. **`formalization` 52 段（67%）是本庫首見量級，且與 `domains=[]` 高度共現**（52 段中 41 段判空）。它是 `Z-wisdom`（方法與體系）在 `discourse_mode` 層的投影。**但共現不等於等價**——傳（3）那 8 段就是同時 `formalization` 又滿載 V 的反例。
4. **I 第二次命中。** 加上世說新語的 8 段，I 的觸發條件可以再收窄：**要嘛文本在做形上學，要嘛有人替自己的一生定價。**這兩類在論政書、算書、家訓、博物誌裡都不出現。
5. **XII 只有 2 段，都不在宇宙論那一章**，而在兩篇政典裡把祭祀當統治責任的地方，因此與 V 同現。這正是要的：XII 給的是人與神聖的關係或信仰實踐，不是宇宙論本身。
6. **`category` 已由 志怪 改為 術數（2026-08-09）。** 讀完全書才確認它沒有任何志怪成分。**分類修正刻意延到標註完成之後才做，以免影響判讀**——這個順序往後照用。已同步改 `scripts/catalog/chinese-classics-ws.json`（canonical 來源）與 `meta.json`。

**這三部共同的方法結論：三批都是零校準回合。** 世說新語 23 批做了兩次 spec 補丁，這三部一次到位。差別不在模型，在**體裁的判準表面積**：博物誌只有一道閘門要卡；古三墳體例多但每種體例的規則單純，按體例寫就窮盡了；敘事體有情節、有評語、有背景條件三種湊法要防，所以非補不可。另外，洞冥記的 spec 事前列了五類「會命中的典型」，實得 9 條中 8 條落在其中（另 1 條是我漏列而 codex 抓到的）——**事前列命中典型同時給出正例與反例，比只寫閘門更能防兩個方向的錯，往後 spec 照這樣寫。**

## 2026-08-09：九章算術全書重判（本庫第一次撤銷既有命中）

**28 段命中 → 1 段。** 撤銷 27 段（V 26、VIII 1），只留劉徽序首段的 XII。

**新判準（往後所有書都適用）**：

> **把題目／文句裡的社會場景換成中性物件，它還成不成立？**
> 成立 → 場景只是包裝，填 `[]`。不成立（垮掉，或問的東西整個變了）→ 場景是本質，才給領域。

**為什麼原判準錯**。原判準是「題目若陳述『誰該承擔多少』就標 V」，看起來嚴謹，實際是聯想式的：看到爵次、戶口、賦稅這些社會詞彙就命中，沒有問**場景是否承重**。「大夫、不更、簪褭、上造、公士五人共獵五鹿，以爵次分之」換成「五個容器按 5:4:3:2:1 分水」之後，題目一模一樣、術一字不改——爵位不承重。均輸按戶數×路程攤賦稅同理，本質是加權平均。九章算術是一部數學書，官員、分地、賦稅是作者用來出題的方式，正如現代課本用「小明小華分蘋果」，沒人會說那題在探討兒童社會關係。

**負對照組的地位變強不是變弱**。原本 HANDOFF 寫「九章證明方法說得出沒有」；重判後 719/720 全空，那 28 段命中本身就是雜訊，這個結論比原本更硬。

**過程與可稽核性**：spec 在 [`delegation/jiuzhang/SPEC.md`](./delegation/jiuzhang/SPEC.md)，10 卷逐卷發包外部 agent（codex）判讀，輸出對照表在 `delegation/jiuzhang/out/*.json`（含全部 720 段的 reason），回填由本庫用既有錨點機制做，外部 agent 不碰 `translations/`。撤銷的段落在 `annotations.json` 的 `note` 留有「2026-08-09 重判撤銷（原標 X）」。

**發包時記錄的兩次失敗，往後校準判準時當反面樣本**：

1. 第一輪：看到「這是數學題」就整卷判空，沒讀懂題目在做什麼 → 720 段只中 28
2. 第二輪：矯枉過正，用「依不依賴社會性前提」去撈，把買賣耗損利息全標成 VIII → 76 段只判空 1 段

兩次都錯，因為兩次都在「猜要標多少」而不是「這一段在做什麼」。正解是讀懂之後照實記——讀懂之後發現它就是數學，那就記 `[]`。

## 已完成

| 項目 | 檔案 | 說明 |
|---|---|---|
| 方法論試點 | `pilots/2026-07-28-sunzi-jiuzhang.md` | §1–5 是試點，**§6／§7 是全文讀後的修訂，以修訂為準**。§7 另記兩個 vocab 層發現（見下方「待使用者決定」） |
| vocab 驗證 | `pilots/2026-07-28-discourse-mode-worked-instance.md` | 九章＋海島兩部驗證 `discourse_mode` 覆蓋缺口。含建議定義與邊界，**表未動，等定案** |
| 段落切分單一來源 | `scripts/corpus_text.py` | `make-scaffold` 產錨點、`verify` 驗錨點都用它。**兩邊算法若分家，para_index 會靜默漂移、每條標註指向錯段** |
| 標註骨架 | `scripts/make-scaffold.py` | 由本文生成錨點，人只填 null 欄位。手寫 `annotations.json` 是壞錨點的來源 |
| 標註 | `translations/{sunzi-bingfa,jiuzhang-suanshu,haidao-suanjing,renwuzhi,qianfulun,yantielun,yanshi-jiaxun,shishuo-xinyu,shenyijing,dongmingji,gu-sanfen}/annotations.json` | 3,267 段全數判讀完 |
| 發包 | `scripts/make-delegation-input.py`（切批）→ `run-delegation.sh`（串行發包 codex）→ `check-delegation-out.py`（回填前雙向錨點檢查）→ `apply-delegation.py`（回填） | 錨點三支都以 `split_paragraphs` 為唯一來源，對不上就整批不寫。`run-delegation.sh <slug> b01 b02 ...` 吃 slug 參數，**取代 `delegation/shishuo-xinyu/run-batches{,2}.sh` 兩支寫死 slug 的版本**，往後發包一律用它 |
| 資料契約 | `SCHEMA.md` | 三層標註模型、`discourse_mode` 八值（v0.2 加 `worked_instance`）、`text_role: reference`、`annotations.json` 格式、負面結果欄位、跨庫對齊。§1.1 補了段落級的白話說明 |
| 行為規範 | `CLAUDE.md` | 與 religions-history 的分界、六條工作守則、七條 anti-pattern |
| 書目 catalog | `scripts/catalog/chinese-classics-ws.json` | **73 部**（phase 1 共 68、phase 2 共 5）。全部走 Wikisource |
| 下載器 | `scripts/download-wikisource.py` | 含 `--survey` 預檢模式（1 請求／部）先掃結構再花內容請求 |
| 驗證 | `scripts/verify.py` | SHA-256／CRLF／過小檔／章標籤唯一性／同文章節／`psych_survey` 欄位存在 |
| 索引 | `scripts/build-index.py` → `00-overview/INDEX.{json,md}` | 由資料生成，**不得手改** |
| 全文 | `translations/`（68 部） | 已 commit |

### 使用者已定案的三項

1. `discourse_mode` 七值 —— 通過（定案當時是七值；v0.2 加 `worked_instance` 後為八值，見 SCHEMA）
2. 標註粒度下放段落級 —— 通過（說明已寫進 SCHEMA §1.1）
3. 小學 5 部 —— **改排 phase 2，不是排除**。理由寫進 SCHEMA §3：排除＝憑書名判定無價值，那正是本庫要修正的錯誤
4. **ctext 路線整條移除** —— 使用者指示「取得簡單一點」。`chinese-classics-ctext.json` 已刪，申不害不收

## 抓取過程修掉的六類真問題（都會毀掉段落級錨點或污染正文，別再退回去）

| 問題 | 症狀 | 修法 |
|---|---|---|
| 篇名標題被丟棄 | 孫子兵法抓回來只有 1 章 | `extract_main_text` 補收 h2/h3/h4，前綴 `## ` |
| 平行上傳整卷重複 | 韓非子 864,988 bytes／80 章（實際重了三套） | catalog `subpage_drop_pattern` + 內容雜湊去重 |
| 卷級子頁沒往下切到篇 | 鹽鐵論只有 10 個「卷N」，60 篇全埋在裡面 | 子頁本文再切一層標題；竹書紀年以 `no_subpage_split` opt-out（698 條逐年紀事切下去全是「元年」互撞） |
| 重定向對切分後才撞成同文 | 外儲說右上／右上 只差一行標題，前置雜湊擋不住 | 切分**之後**再依本文雜湊去重，保留較具體的標籤 |
| **一個卷級子頁只含一篇時，篇名整個丟掉** | 顏氏家訓〈勉学第八〉〈書證第十七〉的章標籤是「卷第3」「卷第6」 | `split_by_headings` 要 ≥2 個標題才肯切，所以獨篇子頁保留了卷名。新增 `adopt_sole_heading()`：只有一個內層標題時不切、改拿它當標籤（2026-08-09） |
| **維基文庫授權樣板被當成正文抓進來** | 章末多一段「此**南北朝**作品在全世界都属于公有领域……」，18 部、48 段 | `CHROME_PATTERNS` 原本寫死 `此作品在全世界都`，但樣板會把朝代名插在「此」與「作品」之間，一路漏接。改抓宣告本身：`公有(領\|领)域` ＋ `此.{0,8}作品在全世界都`。1931 年以前的古典漢語正文不可能出現「公有領域」四字，誤傷風險為零（2026-08-09） |

另外處理過：6 部抓到消歧義／目錄頁（用 `action=parse&prop=links` 查真正目標後改 catalog 標題）、3 部同頁內標題重複（自動補全形序號）。

第五類是標顏氏家訓當天才發現的，當時骨架已產、尚未標註，重抓＋重生骨架零損失；若已標完再發現，255 段錨點要全部重對。

原本這裡寫的是「這一類問題只有在標到那部書時才會浮現」——**現在不必再靠人工發現**。verify 舊版只擋純數字標籤（`1`、`01`），擋不住「卷第3」，顏氏家訓才會漏到標註前一刻。2026-08-09 把它擴成擋所有「只有序號」的標籤（`卷三`／`第三卷`／`卷第3`／`篇第7`），警告數 6 → 14（重抓兩部後為 12）。

## 目前剩下的 12 個警告（章標籤只有序號）

**12 個全部同一類：來源就沒有篇名。重抓修不掉。**

> **更正（2026-08-09 稍晚）**：本節原本寫成兩類，說其中六部（`dongmingji`、`jinlouzi`、`mu-tianzi-zhuan`、`qianfulun`、`wuyue-chunqiu`、`yuejueshu`）是「篇名被爬蟲丟掉、重抓就會好」——**錯**。用 `download-wikisource.py --slug X --survey`（1 請求／部）逐部查了子頁標題，來源端本來就叫「卷一」「卷第一」「第001卷」，爬蟲沒有丟掉任何存在的東西。**正確版是：12 個警告全屬「來源就沒有篇名」，處理方式只有一種——人工補篇名對照表，標註那部書時再做。**

實際查到的來源子頁標題（可覆查）：

| slug | 來源子頁標題 |
|---|---|
| `dongmingji` | 卷第一 / 卷第二 / 卷第三 / 卷第四（4/4 皆序號） |
| `mu-tianzi-zhuan` | 卷一–卷六 ＋ 序一 / 序二 |
| `yuejueshu` | 卷一–卷十五 |
| `qianfulun` | 卷一–卷十 ＋ 附錄一–四 |
| `jinlouzi` | 卷一–卷六 ＋ 序 / 書後 |
| `wuyue-chunqiu` | 9 個有名的外傳／內傳 ＋ 第001卷…（兩套命名混在同一來源） |

部分警告是分數形式（`jinlouzi 1/18`、`yuejueshu 11/19`、`qianfulun 2/40`）是因為爬蟲會再進子頁切內層篇名——**切得出來的那些頁有標題，切不出來的那幾頁是原頁就沒有**，不是爬蟲挑食。

**連帶撤銷一條假警告**：原本寫「別在不知情的狀況下重抓潛夫論，否則〈卷九〉〈卷十〉標籤會改名、既有標註對不上」。既然來源就叫卷九卷十，重抓不會改名，**潛夫論可以安全重抓**。

`zhanguoce 卷12` 與 `qunshu-zhiyao 6/347` 重抓後警告消失是真的，但那兩部屬另一個成因（來源有篇名、舊版爬蟲沒取到），不能外推到這 12 個。**外推前先跑 `--survey` 看來源實際長什麼樣，這一節就是沒跑就分類的後果。**

## 靜默失效：2026-08-09 抓到並修掉的三處

三件事共通點是**沒有任何東西報錯**。管線全綠、腳本 exit 0、檢查全過，但資料是壞的。往後新增檢查時優先想「哪種壞法不會叫」。

1. **重抓會整份覆寫 `meta.json`，`psych_survey` 因此被沖掉。**
   鹽鐵論重抓後書級 survey 變成 `null`，是修回填腳本時撞到 `TypeError` 才發現的。verify 只檢查該欄「存在」不檢查是否為 `null`，所以過得了。
   *修法兩邊都做*：`download-wikisource.py` 覆寫前把既有 `psych_survey` / `verified` 讀出來保留；`verify.py` 新增「有 `annotations.json` 且有段落已判讀，但 `psych_survey` 是 null」→ **error**（不是 warning）。
   **書級 survey 是讀完全書才生得出來的，沖掉等於白讀一部。撞到時從 git 撈回，不要重新推導。**

2. **codex 把整批判完了，卻沒有把檔案寫到磁碟。**
   b22 第一次執行的收尾訊息是「43 段已逐段判完；我正在把判定寫入輸出檔」，然後 process 正常結束，`out/` 是空的。exit code 0。
   *修法*：發包 prompt 明寫「本次交付物就是那個 .json 檔本身」「寫完後把該檔讀回來確認存在且 rows 數正確」，並把這次事故當反例寫進去。driver 每批結束檢查檔案是否存在。

3. **codex 把別批已完成的輸出整份重判覆寫。**
   多鏈並行時，某個實例把 `out/b22.json` 改掉了（11 段領域變、3 段 modes 變、42 條 reason 全改）。成因是發包 prompt 只禁「`out/` 以外」，而 b22 正在 `out/` 底下，而且我還請它拿 b22 當格式範例。
   *修法*：prompt 點名「你唯一可以建立或修改的檔案是 `out/<本批>.json`」並說明別批是已驗收成果；driver 每次呼叫前後取 SHA-256 比對，動到別批就在 log 裡吼。
   **順帶得到一個免費的重測**：兩次獨立判讀〈雅量〉，VI 都是 31/42。那個偏高是該門的真實形狀，不是拿門類名當答案。

4. **codex 印出一份執行計畫、問「確認後可以說『開始』」，然後結束。** b03、b10、b15 三次，磁碟上沒有檔案，exit 0。非互動式呼叫沒有人會回它「開始」。
   *修法*：prompt 開頭寫「**直接執行，不要先提出計畫徵求確認**——這是非互動式呼叫，你回問就等於整批白做」，並把這次事故當反例寫進去。三批重跑後全部成功。

**driver 的世代**：現行版是 `scripts/run-delegation.sh`（吃 slug 參數，一支通用）。它取代了 `delegation/shishuo-xinyu/` 底下兩支寫死 slug 的版本，**那兩支已於 2026-08-09 刪除**（v1 的 clobber 偵測會把「別的鏈剛建立的新檔」誤報成被覆寫，且 prompt 缺上面第 4 條的反例；v2 修好了但仍要逐書複製）。要看事故現場原貌就翻 git 歷史，**不要再在 `delegation/<slug>/` 底下放 driver。**

## 待使用者決定（原兩條，2026-08-09 皆已定案；保留紀錄）

1. ~~**`discourse_mode` 加不加「題例／worked instance」值**~~ —— **已定案並落地（2026-08-09）**。`vocab/discourse-modes.json` 升到 **v0.2**，加入第八值 `worked_instance`，並回填九章 491 段＋海島 13 段（海島 idx 16 是李淳風校勘記，不屬題例，排除）。驗證見 [`pilots/2026-07-28-discourse-mode-worked-instance.md`](./pilots/2026-07-28-discourse-mode-worked-instance.md)。加值當下 religions-history 完全沒有引用該表（無 `vocab/` 目錄、零 `discourse_mode` 欄位），所謂「共用表」是單邊的，無既有標註需重判——這點寫進了 changelog。
2. ~~**`Z-wisdom` 排除是否照舊 / 要不要加 `crosscurrents_hit`**~~ —— **已定案：照舊，不加欄位（2026-08-09 使用者裁決）**。

   裁決原文：「零命中就零命中 沒東西就沒東西 沒必要硬湊」。

   **一併撤回的還有「附錄段從密度分母拿掉」那條機制**（潛夫論 52 段附錄佔 19%）。附錄本來就在書裡，不另設分母。

   **提案被否決的理由值得記住，因為它會再犯**：`crosscurrents_hit` 的真正動機是「怕九章算術被讀成沒思想」——那是替結果找補，不是資料需要。本庫守則本來就寫明負面結果必須寫入、`domains_null` 要記，零命中是有效結果不是缺陷。**往後遇到「這個結果看起來不好看」的衝動，預設答案是照實記，不是加欄位。**

   原始觀察保留備查（不再是待辦）：九章思想密度最高的一段（〈方田〉割圓術劉徽注）零命中因它屬 `Z-wisdom` 支流；人物志 25 段空白同因；潛夫論 69 段空白裡只有 4 段屬 `Z-wisdom`，其餘是譜系與版本層。這些寫在各書 `psych_survey` 的敘述欄即可，不進 schema。

## 下一步

1. **~~`yanshi-jiaxun`~~ / ~~`shishuo-xinyu`~~ —— 皆已完成（2026-08-09）**。前者 255 段解除 V 過寬疑慮，後者 1,132 段兌現硬規則第 5 條。結論見上方兩節。**連帶定案：鹽鐵論與潛夫論不必回頭重判。**
2. **~~第九、十、十一部~~ —— 皆已完成（2026-08-09）**：`shenyijing`（作者不在場）／`dongmingji`（同類別但有情節的對照組）／`gu-sanfen`（內部體例高度異質）。結論見上方兩節。**挑第十二部的準則**：十一部已經把「方法找不找得到東西」「說不說得出沒有」「離開算書還切不切得動」「V 是不是萬用桶」「XIII 靠什麼觸發」「敘事段能不能判領域」「沒有敘事者時判準會不會空轉」「同一部書內部體例分歧能不能分開讀」都問過了。**還沒被檢驗的兩條軸**：(a) **phase 2 小學 5 部**（見第 4 點）——字書韻書是 `text_role: reference`，探針現成，成本極低且是刻意的證否；(b) **醫家 3 部**（`nanjing`／`shanghanlun`／`jinkui-yaolue`）——IX 苦難·疾病·身體 在已標十一部裡合計不到 20 段，醫書是唯一可能讓 IX 成群的體裁，同時也是「本草式功效記載一律判空」這條在**它本來的語境裡**會不會過度殺傷的檢驗。(b) 比 (a) 資訊量高，建議先跑醫家。
3. **發包流程已固定成四支腳本，取代原本每部書臨時重寫的做法**：`make-delegation-input.py --slug X` 切批（不拆章、字數軟上限 8000、錨點取自 `split_paragraphs`）→ 寫 `delegation/X/SPEC.md` → **先發 b01 當試點，校準出的規則補回 spec 才放行其餘批次** → `bash scripts/run-delegation.sh X b01 b02 ...` 串行發包 codex → `check-delegation-out.py --slug X` 做回填前雙向錨點與值域檢查 → `apply-delegation.py --slug X --tagged-by <model>` 回填 → `annotate.py stats X` 取數字寫 `psych_survey` → `verify.py` → `build-index.py` → 對齊 HANDOFF／MAP → commit + push。
   - **試點那一步不可省。** b01 是校準條文寫進 spec 之前判的，12/36 段疊到三個以上 `modes`、自述家規漏了 IV；b02 之後讀新版 spec 判出來 0 段疊加。**規則寫進 spec 就會生效，靠範例檔傳遞不會。**
   - **spec 怎麼寫決定要不要校準回合。** 世說新語 23 批補了兩次；神異經／洞冥記／古三墳三批零校準。三條可複用的寫法：① **內部異質的書按體例切 spec，不按批次切**（古三墳五種體例各給預設答案 + 明點三處易錯分界，結果三處全中）；② **事前列出「會命中的典型」與「不會命中的典型」兩張清單**（洞冥記 9 條命中有 8 條落在事前清單內），同時給正例反例比只寫閘門更防兩個方向的錯；③ **凡要限制數量就寫死數字**（`modes` 不超過三個、`domains` 預設一到兩個），沒有數字的規訓不會生效。
   - **分類問題一律延到標註完成之後才處理。** 古三墳的 `category` 從一開始就是錯的（志怪→實為易類訓誥），但先改分類會影響判讀立場。順序是：標完 → 回報 → 改 `scripts/catalog/chinese-classics-ws.json` 與 `meta.json` 兩處。
   - 手工標註的舊五步（`tmp_ann_<slug>.py` 回填）仍記在 [`MAP.md` §標註工作流](./MAP.md#標註工作流標一部書就照這五步走)，只在不發包時用。
4. phase 2 小學 5 部：探針已現成，用同一套跑一次證否，成本極低
5. 章標籤只有序號的 12 個警告，**已查明全屬「來源就沒有篇名」，重抓修不掉**（2026-08-09 逐部 `--survey` 覆查，見上方章標籤一節）。只能在標到那部書時人工補篇名對照表。優先度低：序號錨點仍然唯一且穩定，不影響標註正確性

**其餘各部要不要照中性物件替換判準重判？結論：不重判。** 該測試打的是「出題包裝被當成內容」，那是算書特有的體裁問題；海島算經本來就 0／13。孫子、人物志、潛夫論、鹽鐵論、顏氏家訓是論說或訓誡書，文句直接對人事下判斷，場景本身承重，替換測試咬不到。世說新語是敘事體，該測試根本用不上，改走「刪掉這一則」閘門（見上）。**鹽鐵論 V 佔 88% 這個獨立警訊也已排除**：顏氏家訓在同一套判準下判出 IV 86 > V 47，證明 V 這一格有區辨力，88% 是「60 篇朝廷辯論」這個文本特徵，不是 V 寬到吞掉一切。

## 已知風險

- **ctext.org 是紅線**：明文禁自動批量下載。本庫現在完全不碰它
- **`expected_chapter_count` 已從粗估改成驗證後凍結的觀測值**，用途變成回歸護欄。之後若下載器行為改變導致章數變動，verify 會叫——那時要判斷是修好還是弄壞，**不要反射性再同步一次數字**
- **焦氏易林上游殘缺**：全書 64 卦，Wikisource 只有 4 個。已寫進 catalog `coverage_note`。expected=4 是「來源有多少」不是「全書多大」
- **`古三墳`、`竹書紀年（今本）`標 `contested`**：偽託／存疑，標註時要記版本立場
- **`category` 欄還有兩處可疑，但未讀原文所以沒動**：`chuci`（楚辭）現歸 `術數`，楚辭是辭賦不是術數，這一條在任何讀法下都不對，但本庫沒有合適的既有類別（`文論` 收的是文心雕龍＝文學批評），要嘛新增 `辭賦` 要嘛併入 `雜著`；`shanhaijing` 現歸 `術數`，四庫入子部小說家類、傳統多歸地理，本庫已有 `地理`（水經注）與 `志怪` 兩個更貼近的類別。**兩者都要讀過才動**——古三墳的教訓正是「讀完 78 段才敢說它不是志怪」。`category` 只驅動 `--category` 下載選取與索引分組，不影響標註，所以不急
- **未涵蓋的第二批候選**：藝文類聚、太平廣記、太平御覽、通典、康熙字典、墨子閒詁、墨經校釋

## 已明確排除（不要再問一次）

| 書 | 去向 | 理由 |
|---|---|---|
| 爾雅 | religions-history `er-ya` 已收 | 不重複收，用 cross-ref 指過去。《釋親》例外抽取見 SCHEMA §3 |
| 四書章句集注、論語注疏、孝經注疏 | religions-history | 經注，屬「被當成經典」那一半 |
| 列仙傳、高士傳 | religions-history | 神仙傳記＝道教敘事 |
| 詩說 | religions-history | 詩經注 |
| 小說、正史 | 兩庫都不收 | 使用者明確排除 |
| 申不害 | 不收 | ctext 路線整條移除 |
| 移轉宗教庫既有文本過來 | 不做 | 會破 SHA-256 鏈、143 部完整三軸標籤、GitHub 歷史，收益近零 |

## 尚未做、不可當結論的推測

pilots 檔 §5 記了兩條：說文解字 540 部首是否構成一套世界分類、釋名聲訓是否編碼民俗世界觀。**兩者均未讀原文**，且這 5 部現在排 phase 2 還沒抓。用第一批的探針跑一次即可證否。

# SCHEMA — 資料契約

> 本檔是本庫的資料真相定義。**動任何 downloader 或標註管線之前先讀這份。**
> 設計依據見 [`pilots/2026-07-28-sunzi-jiuzhang.md`](./pilots/2026-07-28-sunzi-jiuzhang.md)（孫子兵法 × 九章算術雙本試點）。

## 0. 為什麼先寫 schema 再收文本

claudehome 知識規範第 1 條：**先設計檢索，再擴寫內容**。本庫的直接教訓是——試點證明既有的「領域標籤」單軸不足以區分《孫子》與《九章》對同一問題的處理方式。若先抓 74 部再補維度，等於全部重標。

## 1. 三層標註模型

| 層 | 欄位 | 粒度 | 說明 |
|---|---|---|---|
| L1 結構 | `category`、`language`、`era`、`text_role` | 書級 | 客觀事實，收錄當下即可填 |
| L2 領域 | `psych_domains` | **段落級** | 這段在回應哪個人生問題（13 領域，共享詞彙） |
| L3 姿態 | `discourse_mode` | **段落級** | 這段**用什麼方式**處理它（本庫新增，共享詞彙） |

### 1.1 「段落級」是什麼意思（白話）

問題是：**標籤要貼在整本書上，還是貼在書裡的每一段上？**

貼在整本書上（書級）長這樣：

```
孫子兵法 → 標籤：情緒、恐懼、領導、我群他群、信仰（否定）…
```

貼在段落上（段落級）長這樣：

```
孫子兵法·軍爭第七·第 6 段 → 情緒
孫子兵法·九地第十一·第 12 段 → 恐懼
孫子兵法·用間第十三·第 3 段 → 信仰（否定面）
```

差別在**檢索的時候**。假設有人問「古人怎麼看恐懼」：

- 書級：系統回「孫子兵法」。使用者拿到一萬字，要自己找。而且孫子那五個標籤裡只有一個是恐懼，另外四個是雜訊——書愈厚，標籤愈被稀釋，愈不準。
- 段落級：系統直接回〈九地〉那一段「投之亡地然後存，陷之死地然後生」，一百多字，就是答案。

換句話說，**書級標籤只能告訴你「去哪本書找」，段落級標籤直接給你答案**。這也是為什麼 anchor 用「篇名＋第幾段」而不是字元位置——換一個版本，字元位置全部失效，篇名不會。

代價是標註工作量大很多（要讀全文、逐段判），好處是標完就能用。試點時孫子〈用間〉的「先知不可取於鬼神」和〈九地〉的恐懼悖論被迫掛在同一個書級標籤上互相稀釋，兩邊都檢索不到——這就是不做段落級的實際後果。

L2 與 L3 **正交**：任一領域可搭配任一姿態。驗證案例——《儀禮·喪服》把哀悼期限按親等算成可推導制度，是 `formalization` × `X 無常·老·死·失去`；《九章·均輸》是 `formalization` × `V 公義·權力`。同姿態、不同領域，成立。

## 2. `discourse_mode` 受控詞彙（v0.2）

| 值 | 定義 | 判準 | 樣例 |
|---|---|---|---|
| `observation` | 陳述人實際如何 | 可被事實否證的描述句 | 孫子〈行軍〉徵候清單 |
| `proposition` | 主張某個因果或原理 | 有 A 故 B 的結構 | 孫子〈兵勢〉論勇怯生於勢 |
| `prescription` | 規定應該如何 | 祈使或當為句 | 孫子〈行軍〉論文武並用之令 |
| `formalization` | 把規範化成可計算程序 | 有可執行的算法／制度 | 九章衰分術、均輸術；儀禮喪服 |
| `narrative` | 用故事承載 | 有人物與情節 | 列女傳、福音書 |
| `ritual` | 用儀節承載 | 規定動作、時序、器物 | 禮記、利未記 |
| `expression` | 第一人稱抒發而非第三人稱描述 | 說話者即當事人 | 楚辭、詩篇 |
| `worked_instance` | 以具體數字陳述一個問題實例及其答案，本身不主張命題、不下規範，功能是示範某個程序如何套用 | 拿掉數字之後還剩什麼？只剩程序空殼→`worked_instance`；還剩一個可爭論的主張→`proposition` | 九章「今有田廣十五步，從十六步。問：為田幾何？／荅曰：一畝。」；海島「今有望海島，立兩表……」 |

`worked_instance` 於 2026-08-09 加入（v0.2），驗證見 `pilots/2026-07-28-discourse-mode-worked-instance.md`。與 `formalization` 的分工：**術是程序，題答是該程序的一次實例**。不涵蓋校勘記與版本層陳述（那是關於文本本身而非關於世界），也不涵蓋醫案、判例、公案（實例本身承載可爭論內容時該用 `narrative`／`proposition`）。

**紀律**：
- 一個段落可多標（常見組合：`proposition` + `prescription`）
- 判不出來就留 `null`。**`null` 代表「未標」，不代表「沒有」**
- 新增值必須先在 pilot 驗證，經確認才進正式詞彙表（依 knowledge-hub 規則，未定案關係一律 `proposed`）

### 為什麼不叫 `stance`
`stance` 在 NLP 有既定意義（stance detection＝支持／反對的立場），會誤導。定名 `discourse_mode`。

## 3. `text_role` 擴充

沿用 religions-history 既有 enum，**新增一個值**：

| 值 | 說明 | 本庫處置 |
|---|---|---|
| `original` | 成書原典 | 進標註管線 |
| `translation` / `transliteration` / `contested` | 見 religions-history 定義 | 同左 |
| **`reference`**（新增） | 語言基礎設施而非內容：字書、韻書、類書 | **排除在心理學標註管線外**，改掛 tagger 的同義詞資源層 |

`reference` 適用：說文解字、釋名、方言、急就篇、廣韻。理由——這些是受控詞彙表本身，硬跑標註只會產出噪音；真價值在於替古典漢語標註做**術語正規化**。

**2026-08-09 更新**：這 5 部已抓，其中 **`guangyun` 上游殘缺，依收錄判準第二條排除**（zh.wikisource 只有上平聲卷的十個小韻，206 韻缺 196；catalog 條目保留並標 `excluded` ＋ `excluded_reason`）。其餘 4 部入庫，證否探針對它們跑一次。

**這 5 部排 phase 2，不排除收錄。** 差別要說清楚：排除收錄＝憑書名判定它沒價值，那正是本庫存在要修正的錯誤（pilots §5 明記兩條相關假設**未讀原文**）；排 phase 2＝先不佔第一批成本，等第一批標註管線跑順、有現成探針之後，用同一套探針對它們跑一次證否，成本極低。若探針證實無物，那時寫進 `psych_survey` 的 `domains_null`，才是有依據的「沒有」。

### 3.1 探針結果：上面那句「只會產出噪音」已部分證否（2026-08-09）

探針跑完了，**結論不是四部一致，而是分成兩端，分界不在類別在方法**：

| slug | 段數 | 判空 | 命中 | 對原判的意義 |
|---|---|---|---|---|
| `shuowen-jiezi` | 6,070 | 99.3% | 42 | 支持 |
| `fangyan` | 385 | 98.7% | 5 | 支持 |
| `shiming` | 945 | 89.4% | **100（12／13 領域）** | **推翻** |
| `jijiupian` | 282 | 96.1% | 11（7／13 領域） | 支持（命中集中在勸學正文與注敘，字表全空） |

**登錄式字書（說文、方言）逐字說「這個字是什麼意思」，命中只發生在序跋，原判成立。釋名不同：它用聲訓，解字時必須說出「為什麼這樣叫」，而那個理由往往就是對人的判斷。**「昆孫；昆，貫也，恩情轉遠，以禮貫連之耳」是世代親情遞減的命題，「鰥…愁挹不寐目恒鰥鰥然」寫喪偶失眠，「孝子之意澹然衰思益衰」直接觀察哀傷隨時序遞減。同一部書內，登錄名物的章判空，解釋人倫與喪制的章命中。

所以 `text_role: reference` **仍然是正確的 L1 事實標記**（它們確實是語言基礎設施），但「排除在心理學標註管線外」**不能再當成類別層的通則**。改成：`reference` 預設不排入標註批次，但**不得憑此欄位宣告一部書沒有內容**——要宣告，得跟其他書一樣通讀後寫進 `domains_null`。守則 1 在類別內部一樣成立。

`jijiupian` 探針 v1 判讀不合格（spec 陷阱清單漏掉整個卷二名物篇），改 spec 後重跑，v2 已回填：**282 段判空 271（96%），命中 11 段全在開篇勸學正文、命名制度與顏師古注敘，143 段姓名字表與卷三卷四字表全空**。四部至此標完，`reference` 的分界確定是**方法**（聲訓 vs 登錄）而非類別。詳見 `HANDOFF.md`。

**例外抽取**：`reference` 文本中若有整篇構成內容的，單獨抽出標註。已知案例：《爾雅·釋親》是完整親屬稱謂系統（宗族／母黨／妻黨／婚姻四類），屬 `formalization` × `IV 家庭與傳承`。（爾雅本身已收在 religions-history `er-ya`，本庫不重複收，用 cross-ref 指過去。）

## 4. 段落級標註的落地形式

不動 `raw/original.txt`（動了會破 SHA-256）。標註另存：

```
translations/<slug>/
├── meta.json              ← 書級 L1（沿用 religions-history meta_template schema）
├── raw/original.txt       ← 原文，唯讀
├── raw/checksums.sha256
└── annotations.json       ← 段落級 L2 + L3
```

`annotations.json` 每筆：

```json
{
  "para_id": "sunzi-bingfa#07-p06",
  "anchor": {"chapter": "軍爭第七", "para_index": 6},
  "psych_domains": ["VI"],
  "discourse_mode": ["proposition", "prescription"],
  "confidence": "high",
  "note": "士氣的時間曲線與四治（氣心力變）",
  "tagged_by": "model-id",
  "tagged_at": "2026-07-28T00:00:00+08:00"
}
```

`anchor` 用章名 + 段序，**不用字元 offset**（原文若換版本，offset 全失效）。

## 5. 負面結果必須入庫

書級 `meta.json` 增列：

```json
"psych_survey": {
  "surveyed_at": "2026-07-28",
  "domains_hit": ["V"],
  "domains_null": ["I","II","III","IV","VI","VII","VIII","IX","X","XI","XII","XIII"],
  "verdict": "低密度：全書無任何人物內在狀態描寫；命中僅來自分配規範的形式化"
}
```

**理由**：九章算術 13 領域只命中 1 個。這個「幾乎全空」的結果本身是資料——不記錄，半年後又會有人憑書名猜「算書應該有吧」而重跑一次。方法能說「沒有」，才證明它不是在幻覺。

### 5.1 段級也要分得出「沒讀」與「讀了沒有」

同一個道理往下推到段落級：`null` 與 `[]` **不是同義**。

| 值 | 意思 | 誰會寫 |
|---|---|---|
| `null` | 尚未讀這段 | `make-scaffold.py` 產骨架時 |
| `[]` | 讀了，本段不觸及任何領域／判不出任一姿態 | 標註者 |

沒有這個分別，一部書標完之後就分不出「剩下的段落是空的」還是「剩下的段落沒人碰」，等於書級的 `domains_null` 沒有證據支撐。

工具已經吃這個約定，不需要改碼：`verify.py` 接受空陣列，`make-scaffold.py` 的保留判準是 `is not None`，所以 `[]` 的列在重跑時會被留下。

## 6. 跨庫對齊

| 項目 | 位置 | 規則 |
|---|---|---|
| 13 人生問題領域 | 共享層（不放任一庫內） | 兩庫引用同一份 |
| `discourse_mode` 詞彙 | 共享層 | 同上 |
| `semantic_tags` | **各庫自有** | 比較宗教學 14 類不適用本庫；本庫另立（治理／謀略／名辯／術數／醫理／語言…） |
| 跨庫引用 | `專案:slug`，如 `religions-history:tao-te-ching` | 依 knowledge-hub 規則，跨庫關係預設 `proposed`，不因名稱相似即合併 |

**回填策略（重要）**：religions-history 現況為 4683 部中僅 143 部有完整三軸標籤，全庫回填 `discourse_mode` 不可行也不必要。做法是——本庫從第一天標；宗教庫隨日常標註自然長出，不另開回填工程；缺欄位以 `null` 表示且不算驗收失敗（hub export 走 `schema_version` minor bump + 相容策略）。
